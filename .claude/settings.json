{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "d=\"${CLAUDE_PROJECT_DIR:-$PWD}\"; while [ \"$d\" != \"/\" ]; do [ -x \"$d/bin/capture-response\" ] && exec \"$d/bin/capture-response\" --client claude --hook-json - --silent; d=$(dirname \"$d\"); done; exit 0"
          }
        ]
      }
    ]
  }
}
