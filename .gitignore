# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.venv/
venv/

# OS
.DS_Store

# IDE
.idea/
.vscode/

# State (gitignored — per-machine runtime data)
state/

# Missions (per-machine learner fork clones — large + personal)
missions/

# Corpus backups (Y3 created .bak when fixing concept_graph broken edges)
corpus/*.bak

# Reports — maintainer Mode B measurement output, local only.
# This is a public learner-facing repo; per-machine latency/cohort/phase
# snapshots have no value to learners and add clone weight. Headline
# numbers live inline in docs/verification-results.md instead.
reports/

# Build artifacts
artifacts/
