{
  "id": "spring/spring-integration-channel-adapter-message-boundary-primer",
  "title": "Spring Integration Channel Adapter Message Boundary Primer",
  "category": "spring",
  "level": "intermediate",
  "summary": "이 문서는 Spring Integration의 Message, Channel, Endpoint, Channel Adapter를 처음 구분하는 primer다. HTTP controller나 service method를 직접 연결하는 대신 message boundary를 두어 file, queue, mail, TCP 같은 외부 시스템과 application flow를 느슨하게 잇는 상황에 맞다. 단순 method 호출을 메시징 프레임워크로 과하게 바꾸지 않도록 사용 신호와 회피 신호를 함께 다룬다.",
  "body_markdown": "# Spring Integration Channel Adapter Message Boundary Primer\n\n## 핵심 모델\nSpring Integration은 application 안의 작업을 Message로 표현하고 Channel로 전달한다. Endpoint는 message를 처리하고, Channel Adapter는 file, HTTP, mail, queue, TCP 같은 외부 시스템과 channel 사이를 연결한다. 목적은 외부 I/O와 내부 service flow 사이에 명시적인 message boundary를 두는 것이다.\n\n## 언제 쓰나\n파일이 들어오면 파싱하고 저장하는 flow, queue message를 받아 여러 단계로 처리하는 flow, polling으로 외부 시스템을 읽어오는 flow처럼 integration boundary가 뚜렷할 때 적합하다. 단순 controller-service 호출이나 같은 process 안의 작은 분기는 보통 과하다.\n\n## 흔한 오해\nSpring Integration은 비동기를 자동으로 안전하게 만드는 도구가 아니다. retry, idempotency, transaction boundary, error channel을 따로 설계해야 한다. message channel을 만들었다고 domain boundary가 저절로 생기지도 않는다.\n\n## 실전 시나리오\n특정 폴더에 들어온 CSV 파일을 읽어 검증하고 서비스로 넘긴다고 하자. Inbound adapter는 파일 시스템이라는 외부 세계에서 message를 만든다. MessageChannel은 처리 단계를 연결한다. Transformer는 CSV row를 command 객체로 바꾼다. Service activator는 실제 application service를 호출한다. Outbound adapter는 처리 결과를 다른 시스템, 파일, queue로 내보낼 때 쓴다.\n\n```text\nfile -> channel -> transformer -> service activator\nFileReadingMessageSource\n  -> inputChannel\n  -> CsvToCommandTransformer\n  -> ReservationImportService.import(command)\n  -> errorChannel 실패 메시지 분리\n```\n\n## 다음 행동\n- 외부 시스템과 만나는 지점을 inbound/outbound adapter로 표시한다.\n- message payload와 header에 무엇을 담을지 정한다.\n- 실패를 호출 스택 예외로만 보지 말고 error channel, retry, dead letter 후보를 분리한다.\n\n## 확인 질문\n지금 필요한 것은 service 메서드를 직접 호출하는 구조인가, 외부 이벤트/파일/메시지를 application boundary로 안전하게 들여오는 흐름인가?\n\n## CS 배경: message passing과 integration pattern\nSpring Integration은 Enterprise Integration Patterns를 Spring programming model로 옮긴 것이다. 핵심은 direct method call 대신 message passing으로 boundary를 표현하는 데 있다. Message는 payload와 header를 가지며, channel은 producer와 consumer를 decouple한다. Queue channel은 buffering과 backpressure 관점이 생기고, publish-subscribe channel은 fan-out semantics가 생긴다.\n\nAdapter, transformer, router, splitter, aggregator, service activator는 integration flow의 기본 building block이다. 이 모델은 외부 시스템의 latency, retry, duplicate delivery, partial failure를 application service 안쪽으로 흘리지 않기 위해 사용한다. 즉 Spring Integration은 “복잡한 호출 DSL”이 아니라 distributed boundary를 명시하는 CS 패턴이다.\n\n## 깊게 볼 포인트\nMessage boundary를 두면 call stack이 끊기므로 observability와 error semantics를 새로 설계해야 한다. DirectChannel은 같은 thread에서 동기 호출처럼 보이지만, QueueChannel이나 executor가 들어오면 ordering, backpressure, retry 위치가 바뀐다. Header에는 correlation id, reply channel, content type, source file name 같은 routing context가 들어갈 수 있다. Transformer는 payload shape을 바꾸고, router는 메시지를 어느 channel로 보낼지 고른다. Aggregator는 여러 메시지를 다시 묶기 때문에 timeout과 partial result 정책이 필요하다. 이 개념들은 queue, stream, actor model을 배울 때도 이어지는 message-passing 기본기다.\n",
  "aliases": [
    "Spring Integration",
    "MessageChannel",
    "channel adapter",
    "Spring Integration flow",
    "message boundary",
    "integration endpoint",
    "스프링 인테그레이션",
    "메시지 채널",
    "채널 어댑터",
    "외부 시스템 연동 flow"
  ],
  "expected_queries": [
    "Spring Integration channel adapter message channel은 언제 써?",
    "Spring Integration은 Controller Service 호출과 뭐가 달라?",
    "MessageChannel과 Channel Adapter를 처음 배우는데 큰 그림을 알려줘",
    "파일이나 queue 입력을 Spring에서 flow로 처리하려면 어떤 개념을 봐야 해?",
    "Spring Integration을 쓰면 비동기 retry와 transaction이 자동으로 해결돼?",
    "외부 시스템 연동에 message boundary를 둔다는 게 무슨 뜻이야?"
  ],
  "symptoms": [
    "단순 service method 호출을 Spring Integration flow로 과하게 바꾸려 한다",
    "message channel이 있으면 retry와 idempotency가 자동 해결된다고 생각한다",
    "외부 시스템 adapter와 domain service 책임을 섞는다"
  ],
  "relations": {
    "prerequisites": [
      "software-engineering/async-nonblocking-background-work-bridge",
      "system-design/queue-vs-cache-vs-db-decision-drill",
      "spring/bean-di-basics"
    ],
    "next_docs": [
      "software-engineering/outbox-inbox-domain-events",
      "database/queue-consumer-transaction-boundaries",
      "spring/eventlistener-transaction-phase-outbox"
    ],
    "confusable_with": [
      "software-engineering/async-nonblocking-background-work-bridge",
      "software-engineering/outbox-inbox-domain-events",
      "spring/eventlistener-transaction-phase-outbox"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-27",
    "last_modified": "2026-05-27",
    "revision_history": [
      {
        "date": "2026-05-27",
        "source": "y14-batch1"
      }
    ]
  }
}
