{
  "id": "security/xss-csrf-samesite-csp-perspective-map",
  "title": "XSS, CSRF, SameSite, CSP Perspective Map",
  "category": "security",
  "level": "advanced",
  "summary": "XSS와 CSRF를 cookie 전송, SameSite, synchronizer token, CSP 관점으로 구분해 방어층을 설계하는 security perspective map이다.",
  "body_markdown": "# XSS, CSRF, SameSite, CSP Perspective Map\n\n## 이 문서가 맡는 질문\nXSS와 CSRF는 모두 브라우저 보안 문제지만 공격자가 얻는 능력이 다르다. 스크립트 실행을 막는 문제인지, 사용자의 cookie가 붙은 요청을 막는 문제인지 구분해야 한다.\n\n## 빠른 분기\n공격자가 JS를 실행하면 XSS와 CSP/escaping, 다른 site에서 cookie 포함 요청을 보내면 CSRF와 SameSite/token, cross-origin 응답 노출은 CORS를 본다.\n\n## 실전 시나리오\n관리자 브라우저가 악성 사이트를 열었고 예약 삭제 POST가 날아갔다. JS가 우리 사이트에서 실행된 것이 아니라면 XSS보다 CSRF다. SameSite와 CSRF token, Origin/Referer 검증을 본다.\n\n## CS 배경\nBrowser는 origin과 site를 구분한다. Cookie는 요청에 자동 첨부될 수 있고, SameSite는 cross-site context의 cookie 전송을 제한한다. CSP는 XSS payload 실행을 줄이는 defense-in-depth이며 CSRF token을 대체하지 않는다.\n\n## 방어 설정 예시\n```http\nSet-Cookie: SESSION=...; HttpOnly; Secure; SameSite=Lax\nContent-Security-Policy: script-src 'self' 'nonce-r4nd0m'; object-src 'none'\nX-CSRF-Token: per-session-or-per-request-token\n```\nHttpOnly는 JS token 탈취를 줄이고 SameSite는 cross-site cookie 전송을 제한한다. CSP nonce는 script 실행면을 줄이지만 state-changing request에는 CSRF token 검증이 필요하다.\n\n## 깊게 볼 포인트\nXSS가 있으면 CSRF token을 읽거나 같은 origin 요청을 만들 수 있어 CSRF 방어가 약해질 수 있다. 그래서 escaping, CSP, token, SameSite를 대체 관계가 아니라 layered defense로 본다.\n\n## 다음 행동\n1. 공격자가 JS 실행 권한을 얻었는지 먼저 구분한다\n2. state-changing method에 CSRF token과 Origin 검증을 둔다\n3. cookie SameSite/Secure/HttpOnly를 확인한다\n4. CSP report-only로 rollout 후 blocking 정책을 적용한다\n\n## 확인 질문\n이 공격은 우리 origin에서 script를 실행하는가, 아니면 다른 site에서 사용자의 cookie를 실어 요청만 보내는가?\n\n## 추가 주의: SameSite와 CSP nonce\nSameSite=Lax는 일반적인 top-level safe navigation에는 cookie가 붙을 수 있어 모든 cross-site 상황을 막지 않는다. third-party iframe/login 같은 맥락은 `SameSite=None; Secure`가 필요할 수 있다. CSP nonce는 재사용하면 의미가 약해지므로 response마다 새로 만들고 template/render 경로에서 일관되게 주입해야 한다.\n\n## 판단 매트릭스\n- XSS: untrusted input이 script로 실행되는 경로를 막는다\n- CSRF: browser가 자동 첨부하는 credential로 state change가 일어나지 않게 한다\n- SameSite/CSP: 각각 cookie 전송과 script 실행면을 줄이는 보조 방어다\n- 리뷰 질문: CORS, CSRF, XSS를 서로의 대체재로 착각하지 않는가\n\n## 측정 지표와 레이턴시\nCSP violation report, CSRF validation failure, SameSite cookie drop, reflected input sink count, security test latency를 본다. 품질은 exploit 재현 차단, 성능은 header/token 검증 overhead, 레이턴시는 auth/security filter p95로 본다.\n\n## 자주 하는 오해\n- CORS를 켜면 CSRF가 막힌다고 본다\n- CSP가 있으면 escaping이 필요 없다고 생각한다\n- SameSite=Lax 하나로 모든 CSRF가 사라진다고 본다\n\n이 오해를 줄이려면 용어를 먼저 고르지 말고, 실패 모드와 관찰 증거를 분리해야 한다. 같은 단어가 여러 계층에서 쓰일 때는 code path, runtime signal, user-visible effect를 함께 놓고 판단한다.\n\n## 학습자가 답변에 포함해야 할 구조\n답변은 XSS, CSRF, CORS를 공격 능력별로 나눠야 한다. 그 다음 SameSite, token, CSP, escaping의 역할을 설명하고 품질/성능/레이턴시는 violation report, 차단율, filter p95로 검증한다고 말한다.\n\n## 실전 검증 루프\n1. 공격 시나리오를 XSS와 CSRF로 따로 재현한다\n2. state-changing endpoint의 token 검증을 테스트한다\n3. cookie 속성과 cross-site form/fetch 동작을 확인한다\n4. CSP report-only로 false positive를 줄인다\n5. 보안 filter overhead와 차단 지표를 기록한다\n\n이 루프를 지키면 XSS, CSRF, SameSite, CSP Perspective Map 문서는 단순 용어 카드가 아니라 실제 증상을 CS 개념, 구현 경계, 운영 지표로 연결하는 학습 도구가 된다.\n\n## 공격자가 가진 능력으로 먼저 나누기\n\n이 주제는 방어 이름을 외우면 더 헷갈린다. 먼저 공격자가 무엇을 할 수 있는지 나눠야 한다.\n\n| 공격자가 가진 능력 | 대표 공격 | 주 방어 | 보조 방어 |\n|---|---|---|---|\n| 우리 origin에서 script를 실행한다 | XSS | output escaping, input validation, 안전한 DOM API | CSP nonce/hash, Trusted Types, HttpOnly |\n| 다른 site에서 사용자의 cookie가 붙은 요청을 보낸다 | CSRF | CSRF token, Origin/Referer 검증 | SameSite, re-auth, idempotency |\n| cross-origin 응답을 읽으려 한다 | SOP/CORS 문제 | CORS allowlist, credentials 정책 | preflight, fetch credentials 점검 |\n\nCORS는 브라우저가 cross-origin 응답을 JS에 노출할지 정하는 정책에 가깝다. CSRF는 응답을 읽지 못해도 state-changing 요청이 성공하면 문제가 된다. 그래서 `CORS를 막았으니 CSRF도 막힌다`는 결론은 위험하다.\n\n## layered defense로 조합하기\n\n실무에서는 하나의 헤더로 끝내지 않는다. 예를 들어 세션 쿠키 기반 관리자 페이지라면 다음 조합을 생각한다.\n\n```text\n1. 쿠키: HttpOnly; Secure; SameSite=Lax 또는 Strict 검토\n2. 상태 변경 요청: CSRF token + Origin/Referer 검증\n3. 출력 경로: HTML escaping, 안전한 template 사용\n4. script 정책: CSP nonce/hash를 report-only로 검증 후 enforce\n5. 민감 작업: 재인증 또는 step-up 확인\n```\n\nSameSite는 좋은 기본 방어지만 모든 상황을 막지 않는다. OAuth/OIDC login callback, 외부 결제 redirect, embedded flow처럼 cross-site navigation이 필요한 기능에서는 `SameSite=None; Secure`가 필요할 수 있다. 이 경우 CSRF token이나 state parameter, Origin 검증 같은 별도 방어가 더 중요해진다.\n\nCSP도 마찬가지다. CSP nonce가 있으면 inline script 실행면을 줄일 수 있지만, 이미 취약한 sink에 untrusted HTML을 넣는 코드를 안전하게 바꾸는 작업을 대체하지 않는다. CSP는 escaping 실패의 피해를 줄이는 defense-in-depth로 봐야 한다.\n\n## 디버깅 질문\n\n- 공격자가 우리 페이지에서 JS를 실행했는가, 아니면 외부 사이트에서 form/fetch를 보냈는가.\n- state-changing endpoint가 token 없는 POST를 받아들이는가.\n- SameSite 설정이 login callback이나 cross-site embed 요구와 충돌하지 않는가.\n- CORS allowlist가 넓어서 credentialed response가 노출되는가.\n- CSP report에 실제 차단 후보가 쌓이는가, nonce가 response마다 바뀌는가.\n- XSS가 있으면 CSRF token을 읽거나 같은 origin 요청을 만들 수 있다는 점을 threat model에 반영했는가.\n\n이 질문 순서로 보면 XSS, CSRF, CORS, SameSite, CSP를 서로 대체하는 기술이 아니라 서로 다른 browser security boundary를 줄이는 층으로 설명할 수 있다.\n\n## 한 줄 정리\nXSS와 CSRF를 cookie 전송, SameSite, synchronizer token, CSP 관점으로 구분해 방어층을 설계하는 security perspective map이다.\n\n## 품질 관점\n보안 품질은 deny/allow correctness, stale decision, auditability, degradation policy를 함께 검증한다. 이 문서의 답변 품질은 `XSS, CSRF, SameSite, CSP Perspective Map`를 단순 용어가 아니라 선택 기준, 실패 모드, 검증 방법까지 연결하는지로 판단한다. 특히 비슷한 이웃 문서와 섞일 때 primary answer가 왜 이 문서인지 설명할 수 있어야 한다.\n\n## 성능 관점\n성능은 평균 처리량 하나로 판단하지 않는다. 입력 크기, 큐/락/캐시/네트워크/런타임 비용 중 무엇이 병목인지 분리하고, 작은 synthetic case와 production-like case를 따로 측정한다. 최적화가 correctness invariant를 흐리면 성능 개선으로 인정하지 않는다.\n\n## 레이턴시 관점\n레이턴시는 p50보다 p95/p99와 cold path, retry, queueing, lock wait, cache miss를 함께 본다. 학습 답변에는 latency budget과 관측 지표가 포함되어야 하며, 개선 전후가 같은 조건에서 비교되어야 한다.\n\n## 주제별 추가 지표\n`deny_bypass_count, stale_allow_ms, authz_p99_ms, audit_gap_count`를 최소 지표로 둔다. 이 지표들은 품질, 성능, 레이턴시를 한 화면에서 비교하기 위한 기준이며, 단순 통과/실패보다 어떤 경계가 병목인지 드러내야 한다.\n",
  "aliases": [
    "XSS CSRF SameSite CSP",
    "XSS와 CSRF 차이",
    "CSRF SameSite token 차이",
    "CORS CSRF XSS 혼동",
    "브라우저 보안 cookie CSP",
    "CSP nonce XSS 방어",
    "SameSite CSRF 방어",
    "Origin Referer CSRF 검증",
    "HttpOnly Secure SameSite",
    "script 실행 요청 위조 구분",
    "CSRF token SameSite",
    "CORS는 CSRF 방어 아님"
  ],
  "expected_queries": [
    "XSS랑 CSRF랑 SameSite CSP를 어떻게 같이 봐?",
    "CORS를 설정하면 CSRF가 해결되는 거야?",
    "XSS, CSRF, SameSite, CSP가 각각 뭘 막는지 어떻게 구분해?",
    "악성 사이트가 내 쿠키로 POST를 보내는 상황은 XSS가 아니라 CSRF로 봐야 해?",
    "CSP nonce가 있으면 CSRF token이나 escaping을 안 해도 되는지 헷갈려",
    "SameSite=Lax, HttpOnly, CSRF token, Origin 검증을 어떤 순서로 조합해야 해?"
  ],
  "symptoms": [
    "CORS를 켜면 CSRF가 막힌다고 본다",
    "CSP가 있으면 escaping이 필요 없다고 생각한다",
    "SameSite=Lax 하나로 모든 CSRF가 사라진다고 본다"
  ],
  "relations": {
    "prerequisites": [
      "security/xss-csrf-basics",
      "security/csrf-defense-synchronizer-token-vs-samesite-vs-double-submit-intermediate-bridge",
      "security/csp-xss-defense-interview-primer"
    ],
    "next_docs": [
      "security/xss-csrf-spring-security",
      "security/trusted-types-dom-xss-rollout-chooser",
      "security/csrf-in-spa-bff-architecture"
    ],
    "confusable_with": [
      "network/cors-origin-preflight-credentials-csrf-perspective-map",
      "security/cors-samesite-credentials-interview-router",
      "security/csrf-vs-samesite-session-cookie-chooser",
      "security/csp-nonces-vs-hashes-script-policy"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-27",
    "last_modified": "2026-06-18",
    "revision_history": [
      {
        "date": "2026-05-27",
        "source": "y14-batch3"
      },
      {
        "date": "2026-05-28",
        "source": "y14-closure-quality-audit"
      },
      {
        "date": "2026-05-28",
        "source": "y14-qrels-anchor-sync"
      },
      {
        "date": "2026-06-18",
        "note": "Expanded XSS CSRF SameSite CSP surfaces for batch118."
      }
    ]
  }
}
