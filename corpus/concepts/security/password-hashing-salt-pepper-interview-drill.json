{
  "id": "security/password-hashing-salt-pepper-interview-drill",
  "title": "Password Hashing Salt Pepper Interview Drill",
  "category": "security",
  "level": "intermediate",
  "summary": "이 drill은 password hashing, salt, pepper, bcrypt/Argon2를 면접 답변으로 구성한다. 암호화와 해싱 차이를 분명히 한다.",
  "body_markdown": "# Password Hashing Salt Pepper Interview Drill\n\n## 질문\n\n\"비밀번호는 왜 암호화(encryption)가 아니라 해싱(hash)해서 저장하나요? salt와 pepper는 무엇인가요?\"\n\n## 30초 답변\n\n비밀번호는 원문을 다시 알아낼 필요가 없으므로 복호화 가능한 암호화가 아니라 one-way password hashing으로 저장한다. Salt는 사용자별 random 값으로 같은 비밀번호도 다른 hash가 되게 하고, pepper는 DB와 분리된 secret으로 DB 단독 유출 시 공격 비용을 높인다. 일반 SHA-256처럼 빠른 hash가 아니라 bcrypt, scrypt, Argon2처럼 cost를 조절할 수 있는 password hashing algorithm을 써야 한다.\n\n## salt와 pepper 분리\n\n| 항목 | 위치 | 비밀 여부 | 목적 |\n|---|---|---|---|\n| Salt | password hash record와 함께 저장 | 비밀 아님 | 같은 password의 hash 중복과 rainbow table 재사용 방지 |\n| Pepper | KMS/secret manager/env 등 DB 밖 | 비밀 | DB만 유출됐을 때 offline cracking 비용 증가 |\n| Cost factor | hash metadata | 비밀 아님 | GPU/ASIC brute force 비용 증가 |\n\nSalt를 secret key처럼 설명하면 틀린 답변이다. Salt는 공격자가 알아도 된다. 핵심은 사용자마다 달라야 하고 충분히 random해야 한다는 점이다. Pepper는 운영 secret이므로 rotation, 접근 권한, 장애 시 login 영향까지 고려해야 한다.\n\n## 관찰 증거\n\nDB dump에서 password column 값이 `$2b$12$...`처럼 algorithm/cost/salt를 포함한 verifier 형식이면 bcrypt 계열 저장을 추정할 수 있다. 반대로 모든 사용자의 hash가 같은 길이의 단순 hex SHA-256이고 salt metadata가 없다면 빠른 hash 단독 저장 위험을 의심한다.\n\n로그인 검증은 사용자가 입력한 password에 같은 algorithm, salt, pepper, cost를 적용해 저장된 verifier와 비교한다. 비교는 timing side channel을 줄이도록 상수 시간 비교를 쓰는 편이 안전하다. 로그인 실패 횟수 제한과 MFA는 추가 방어층이지 password hashing을 대체하지 않는다.\n\n## algorithm 선택 기준\n\nArgon2id는 memory-hard 특성으로 최신 권장 선택지로 자주 언급된다. bcrypt는 오래 검증됐고 운영 지원이 넓지만 입력 길이 제한과 cost 조정 방식을 이해해야 한다. PBKDF2는 표준성과 FIPS 요구가 있을 때 선택될 수 있지만 GPU 방어력 관점에서 memory-hard 계열과 trade-off가 있다.\n\n면접에서는 \"bcrypt를 쓴다\"로 끝내지 말고 cost calibration을 말해야 한다. 로그인 p95 latency와 서버 CPU/메모리 예산을 보면서 사용자 경험을 망치지 않는 범위에서 공격 비용을 높인다. 장비가 빨라지면 신규 hash cost를 올리고, 기존 hash는 로그인 성공 시 재해싱하는 migration 전략을 둘 수 있다.\n\n## 운영 체크리스트\n\n1. password 원문, 임시 password, reset token을 로그에 남기지 않는다.\n2. 회원가입/비밀번호 변경 시마다 새 salt를 생성한다.\n3. pepper는 코드 저장소나 DB dump에 함께 들어가지 않게 secret manager로 분리한다.\n4. hash metadata에 algorithm과 cost를 남겨 향후 migration이 가능하게 한다.\n5. 계정 탈취 방어는 rate limit, credential stuffing 탐지, MFA와 함께 설계한다.\n\n## 흔한 오답 교정\n\n- `암호화해서 저장하고 필요할 때 복호화한다`: 비밀번호 확인에는 원문 복구가 필요 없다.\n- `salt는 숨겨야 안전하다`: salt는 비밀이 아니라 uniqueness를 위한 값이다.\n- `SHA-256을 여러 번 돌리면 충분하다`: password hashing은 공격 비용을 조절하는 검증된 KDF를 쓰는 것이 안전하다.\n\n## 약점 태그\n\n- `hash-vs-encryption`: 복호화 필요 여부를 설명하지 못함\n- `salt-secret-confusion`: salt를 secret으로 착각함\n- `fast-hash-mistake`: SHA-256 단독 저장이 왜 부족한지 말하지 못함\n\n## 실제 verify 흐름 — 코드로 보기\n\n개념만 외우면 면접에서 \"실제로 어떻게 동작해요?\"라는 질문에 막힌다. 아래는 bcrypt 기준 저장/검증 흐름의 핵심 코드다.\n\n```java\n// 회원가입: 저장\n// BCrypt는 내부적으로 랜덤 salt를 생성하고, algorithm + cost + salt + hash를 한 문자열에 합쳐 반환한다.\n// 예: \"$2b$12$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy\"\n//       ↑알고리즘  ↑cost  ↑salt(22자 base64)       ↑hash 본문\nString storedVerifier = BCrypt.hashpw(rawPassword, BCrypt.gensalt(12));\n// → DB에는 이 verifier 문자열 하나만 저장한다. salt를 별도 컬럼에 저장할 필요 없다.\n\n// 로그인: 검증\n// BCrypt.checkpw는 verifier 문자열에서 salt와 cost를 파싱해 동일 조건으로 재해싱한다.\n// 내부 비교는 timing-safe(상수 시간) 비교를 사용한다.\nboolean matches = BCrypt.checkpw(inputPassword, storedVerifier);\n```\n\n**왜 SHA-256 단독은 안 되나?**\n\n```java\n// ❌ 잘못된 예 — 절대 하지 말 것\nString bad = DigestUtils.sha256Hex(rawPassword);\n// 문제 1: 모든 사용자의 \"password123\" 해시가 동일 → rainbow table 재사용 가능\n// 문제 2: GPU 1장으로 초당 수십억 회 연산 가능 → brute force 비용이 거의 0\n```\n\nSHA-256은 빠른 것이 장점인 hash다. 파일 무결성 검증처럼 속도가 중요한 곳에 어울린다. 비밀번호는 반대로 느려야 한다.\n\n### pepper를 더하면 무엇이 달라지나\n\nsalt가 있어도 DB 전체가 유출되면 공격자는 각 계정의 verifier를 개별적으로 brute force할 수 있다. pepper는 그 공격 경로에 추가 비용을 얹는다.\n\n```text\n저장: hash = bcrypt(rawPassword + pepper, salt, cost=12)\n검증: hash_candidate = bcrypt(inputPassword + pepper, 기존_salt, cost=12)\n      timing-safe 비교(hash_candidate, stored_hash)\n```\n\npepper는 코드가 아닌 KMS나 환경 변수에 둔다. DB만 유출된 상황에서 공격자는 pepper 없이 brute force를 시작하지 못한다. 단, pepper가 함께 노출되면 이점이 사라지므로 \"salt + cost만 있어도 충분한 경우가 많다\"는 현실적인 trade-off도 있다.\n\n### 자주 막히는 지점 — \"salt만 있으면 충분하지 않나요?\"\n\nsalt가 있어도 cost factor가 낮으면 충분하지 않다. salt는 같은 비밀번호의 hash 값이 계정마다 달라지게 해서 **미리 만들어둔 rainbow table을 무력화**하지만, 공격자가 hash 하나를 brute force하는 속도 자체는 줄이지 않는다.\n\ncost를 올리면 해시 한 번 계산에 걸리는 시간이 늘어나 brute force 시도 횟수 자체가 줄어든다. 정리하면:\n\n| 방어 수단 | 막는 것 | 못 막는 것 |\n|---|---|---|\n| Salt | 미리 계산된 rainbow table, 동일 비밀번호 일괄 공격 | 개별 계정 brute force |\n| Cost factor | 개별 계정 brute force (시간 비용 증가) | 단독으론 rainbow table 재사용 |\n| Pepper | DB 단독 유출 후 offline cracking | pepper 자체가 함께 유출된 경우 |\n\n셋은 각각의 역할이 있고 서로 보완한다.\n\n## 스스로 점검\n\n- 서비스 운영 중에 bcrypt cost를 10에서 12로 올려야 한다고 판단했다. 기존에 저장된 수백만 개의 hash는 어떻게 처리할 수 있을지 생각해봐.\n- pepper를 환경 변수에 저장하는 것과 KMS에 저장하는 것의 차이는 무엇이고, 어떤 상황에서 KMS가 의미 있을까?\n\n## 복습 연결\n\n기본은 `password-hashing-basics`, 알고리즘 선택은 `password-hashing-bcrypt-vs-argon2-vs-pbkdf2-intermediate-bridge`로 이어진다.\n",
  "aliases": [
    "password hashing interview drill",
    "salt pepper password answer",
    "비밀번호 해싱 면접 답변",
    "bcrypt argon2 salt 설명"
  ],
  "expected_queries": [
    "비밀번호를 왜 암호화가 아니라 해싱해야 해?",
    "salt와 pepper 차이를 면접에서 어떻게 설명하지?",
    "bcrypt와 Argon2를 왜 쓰는지 답변 연습하고 싶어"
  ],
  "symptoms": [
    "password encryption과 hashing을 같은 말로 써요",
    "salt를 secret key라고 설명해요",
    "빠른 hash가 좋은 hash라고 오해해요"
  ],
  "relations": {
    "prerequisites": [
      "security/password-hashing-basics"
    ],
    "next_docs": [
      "security/password-hashing-bcrypt-vs-argon2-vs-pbkdf2-intermediate-bridge"
    ],
    "confusable_with": [
      "security/password-hashing-basics",
      "security/password-hashing-bcrypt-vs-argon2-vs-pbkdf2-intermediate-bridge"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-05-31",
    "legacy_path": "knowledge/cs/contents/security/password-hashing-salt-pepper-interview-drill.md",
    "revision_history": []
  }
}