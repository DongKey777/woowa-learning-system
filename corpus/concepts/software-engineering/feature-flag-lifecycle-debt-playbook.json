{
  "id": "software-engineering/feature-flag-lifecycle-debt-playbook",
  "title": "Feature Flag Lifecycle Debt Playbook",
  "category": "software-engineering",
  "level": "intermediate",
  "summary": "feature flag는 if 하나가 아니라 owner, expiry, 전파 모델, audit, 제거 조건을 가진 임시 운영 계약이다.",
  "body_markdown": "# Feature Flag Lifecycle Debt Playbook\n\n> 한 줄 요약: feature flag는 `if` 하나가 아니라 owner, expiry, 전파 모델, audit, 제거 조건을 가진 임시 운영 계약이다.\n\n**난이도: 🟡 Intermediate**\n\n> 관련 문서:\n> - [Feature Flags, Rollout, Dependency Management](./feature-flags-rollout-dependency-management.md)\n> - [Feature Flag Cleanup and Expiration](./feature-flag-cleanup-expiration.md)\n> - [Kill Switch Fast-Fail Ops](./kill-switch-fast-fail-ops.md)\n> - [Configuration Governance and Runtime Safety](./configuration-governance-runtime-safety.md)\n> - [Deployment Rollout, Rollback, Canary, Blue-Green](./deployment-rollout-rollback-canary-blue-green.md)\n\n> retrieval-anchor-keywords:\n> - feature flag lifecycle\n> - flag owner\n> - flag expiry\n> - stale flag debt\n> - kill switch\n> - config propagation\n> - audit trail\n> - flag cleanup\n> - remote config TTL\n\n## 핵심 개념\n\nfeature flag의 위험은 flag를 만드는 순간보다 **flag가 오래 남는 순간**에 커진다.\n\n좋은 flag는 생성 시점부터 아래 질문에 답한다.\n\n- 누가 owner인가?\n- 언제 만료되는가?\n- 어떤 조건에서 제거하는가?\n- 사고 시 끄는 값과 안전한 기본값은 무엇인가?\n- 변경 전파가 몇 초/몇 분 걸릴 수 있는가?\n- 누가, 왜, 어떤 범위로 바꿨는지 audit이 남는가?\n\n## flag 등록 카드\n\n새 flag를 만들 때 최소한 아래 정보를 같이 적는다.\n\n| 필드 | 예시 | 없으면 생기는 문제 |\n|---|---|---|\n| `key` | `checkout.new-flow.enabled` | 코드와 config 검색이 어려움 |\n| `type` | release / experiment / ops / permission / kill-switch | 수명과 승인 기준이 섞임 |\n| `owner` | `checkout-team` | 만료 후 제거 책임이 사라짐 |\n| `created_at` | `2026-05-15` | stale 판단 기준이 없음 |\n| `expires_at` | `2026-06-15` | 임시 flag가 영구화됨 |\n| `removal_condition` | `100% rollout stable 7 days` | 언제 PR로 지울지 모름 |\n| `default_value` | `false` | config 서버 장애 시 동작 불명확 |\n| `blast_radius` | `checkout write path` | 잘못 켰을 때 피해 범위 모름 |\n| `dependencies` | `payment-v2-ready` | flag 조합 순서가 꼬임 |\n| `audit_required` | `true` | 누가 왜 바꿨는지 추적 불가 |\n\n이 정보가 없다면 flag는 안전장치가 아니라 이름 붙은 부채가 된다.\n\n## flag 유형별 수명\n\n| 유형 | 목적 | 정상 수명 | 제거 기준 |\n|---|---|---|---|\n| release flag | deploy와 release 분리 | 짧음 | rollout 완료와 안정화 관측 후 |\n| experiment flag | A/B 실험 | 짧음 | 실험 종료와 분석 완료 후 |\n| permission flag | 사용자군/권한 분리 | 중간~장기 | 제품 정책이면 config로 승격 |\n| ops flag | 운영 tuning | 중간 | runbook과 owner가 있으면 유지 가능 |\n| kill switch | 사고 확산 차단 | 장기 가능 | critical path면 제거보다 정기 drill |\n\nkill switch는 release flag와 다르다. release flag는 기능 출시 후 제거하는 것이 기본이고, kill switch는 사고 때 피해를 끊기 위한 운영 장치라서 남겨야 할 수 있다. 대신 owner, drill, audit, safe default가 더 엄격해야 한다.\n\n## stale flag debt 신호\n\n아래 신호가 보이면 cleanup PR 후보로 잡는다.\n\n- `expires_at`이 지났다.\n- 30일 이상 값이 바뀐 적 없다.\n- rollout이 100%인데 legacy branch가 남았다.\n- owner 팀이 바뀌었거나 없다.\n- false branch가 운영에서 실행되지 않는다.\n- 테스트가 flag on/off 조합 때문에 두 배로 늘었다.\n- flag 이름은 남았지만 dashboard, runbook, ticket이 없다.\n\nstale flag debt의 비용은 단순 조건문이 아니다. 다음 사람이 코드를 읽을 때 \"두 경로가 아직 살아 있나?\"를 계속 확인하게 만드는 비용이다.\n\n## config propagation caveat\n\nremote config 기반 feature flag는 즉시 일관된 전역 변수가 아니다.\n\n| 지점 | 체크 |\n|---|---|\n| cache TTL | 인스턴스마다 언제 새 값을 보는가 |\n| polling interval | 긴급 차단에 충분히 빠른가 |\n| local fallback | config 서버 장애 시 어떤 값으로 동작하는가 |\n| consistency | 일부 인스턴스만 새 값을 본 상태를 허용하는가 |\n| versioning | 어떤 config version이 요청을 처리했는지 로그에 남는가 |\n| rollback | 이전 값으로 돌아가는 절차가 검증됐는가 |\n\nrollout flag는 몇 분 지연되어도 괜찮을 수 있다. 하지만 kill switch는 초 단위 전파나 로컬 emergency override가 필요할 수 있다. 같은 feature flag 시스템을 쓰더라도 요구 수준은 다르다.\n\n## audit trail\n\nflag 변경 audit에는 최소한 아래가 남아야 한다.\n\n| 항목 | 이유 |\n|---|---|\n| actor | 누가 바꿨는지 |\n| reason | 실험, rollout, incident, rollback 중 무엇인지 |\n| before/after | 어떤 값에서 어떤 값으로 바뀌었는지 |\n| scope | user cohort, region, service, percentage |\n| change id | ticket, incident id, release id |\n| expiry | 임시 override가 언제 끝나는지 |\n| approval | 고위험 flag면 누가 승인했는지 |\n\naudit은 책임 추궁용이 아니라 복구용이다. 사고 중에는 \"언제 어떤 값이 바뀌었나\"를 빠르게 알아야 한다.\n\n## cleanup 순서\n\n### release flag 제거\n\n1. flag가 100% rollout인지 확인한다.\n2. false branch traffic이 0인지 로그로 본다.\n3. rollback window가 지났는지 확인한다.\n4. legacy branch와 flag read를 제거한다.\n5. 테스트에서 off-path 조합을 지운다.\n6. config store와 dashboard에서 key를 제거한다.\n7. changelog나 ticket에 제거를 남긴다.\n\n### kill switch 유지\n\n1. critical side effect 경로인지 확인한다.\n2. safe default와 local fallback을 문서화한다.\n3. 정기 drill로 실제 차단이 되는지 본다.\n4. actor/reason/scope audit을 강제한다.\n5. config propagation SLO를 둔다.\n\n## 코드로 보는 부채\n\n```java\nif (flags.enabled(\"checkout.new-flow\")) {\n    return newCheckout(order);\n}\nreturn legacyCheckout(order);\n```\n\n이 코드는 rollout 중에는 안전장치다. 하지만 rollout이 끝난 뒤에도 남으면 다음 변경자는 두 checkout 경로를 모두 이해해야 한다. 테스트도 두 배로 늘고, 버그 수정이 한쪽에만 들어갈 수 있다.\n\ncleanup 후에는 아래처럼 한 경로만 남는 것이 정상이다.\n\n```java\nreturn newCheckout(order);\n```\n\n## 미션/운영 예시\n\n예약 서비스에서 새 시간표 화면을 `reservation.timeline-v2.enabled`로 열었다고 하자. 처음에는 배포와 출시를 분리하기 위해 flag가 필요하다. 문제는 100% rollout 후에도 old timeline branch가 남아 있을 때 시작된다. 다음 미션에서 예약 가능 시간 계산을 바꾸는 사람이 v1과 v2 양쪽을 모두 고쳐야 하고, 한쪽 테스트만 통과하면 실제 운영 경로와 다른 코드가 살아남는다.\n\n이때 좋은 PR 설명은 \"flag 추가\"에서 끝나지 않는다. `owner=reservation-team`, `expires_at=2026-07-01`, `removal_condition=100% rollout 후 7일간 예약 생성 오류율 변화 없음`, `default=false`, `rollback=flag off`, `cleanup_ticket=...` 정도가 보여야 한다. 학습 미션 규모에서는 ticket 대신 TODO 주석으로 충분해 보일 수 있지만, 실제 서비스에서는 이 정보가 없으면 flag가 부채인지 운영 장치인지 구분되지 않는다.\n\n결제 경로의 `payment.gateway-v2.kill-switch`는 다르게 본다. 이것은 출시 후 삭제할 release flag가 아니라 사고 때 외부 결제 호출을 빠르게 막는 장치일 수 있다. 그래서 제거보다 정기 drill, 승인 권한, audit trail, 안전한 fallback 응답이 더 중요하다. 같은 feature flag라는 이름을 쓰더라도 lifecycle policy는 type마다 달라야 한다.\n\n## cleanup PR을 안전하게 쪼개는 방법\n\nstale flag 제거는 한 번에 모든 것을 지우는 PR보다 작은 순서가 안전하다. 먼저 운영 지표로 한쪽 branch가 더 이상 실행되지 않는지 확인한다. 그 다음 application code에서 dead branch와 flag read를 지운다. 마지막으로 config store, dashboard, 문서, alert, runbook에서 key를 제거한다. 순서를 섞으면 코드가 아직 flag를 읽는데 config key가 먼저 사라지거나, dashboard에는 남았지만 실제로는 아무 효과가 없는 버튼이 생긴다.\n\n테스트도 같이 줄인다. rollout 중에는 on/off 양쪽 조합을 검증해야 하지만 cleanup 후에는 남은 경로 하나를 명확히 테스트하는 편이 좋다. 오래된 off-path 테스트를 유지하면 테스트가 품질을 보장하는 대신 과거 설계를 고정하는 역할을 한다.\n\n## 흔한 오해와 trade-off\n\n\"flag는 많을수록 안전하다\"는 말은 틀리다. flag는 배포 위험을 낮추지만, 코드 경로 수와 설정 조합 수를 늘린다. 특히 flag A가 켜져야 flag B가 안전하고, B가 켜진 상태에서만 C가 의미 있는 구조가 되면 운영자는 사고 중 어떤 조합이 안전한지 빠르게 판단하기 어렵다. dependency가 있는 flag는 rollout order와 rollback order를 문서화해야 한다.\n\n\"default false면 항상 안전하다\"도 위험하다. release flag에서는 false가 legacy path라 안전할 수 있지만, kill switch에서는 false가 외부 호출 계속 진행을 뜻할 수도 있다. safe default는 boolean 값 자체가 아니라 장애 시 피해가 작은 동작이다. config 서버가 죽었을 때 로컬 캐시를 믿을지, 보수적으로 차단할지, 일부 기능만 degrade할지는 flag type과 business criticality로 정해야 한다.\n\n\"나중에 지우면 된다\"는 결정은 보통 지워지지 않는다. owner와 expiry가 없는 flag는 다음 sprint의 급한 일에 밀리고, 몇 달 뒤에는 어떤 branch가 정답인지 아는 사람이 사라진다. 그래서 생성 PR에 cleanup 조건을 함께 넣는 것이 가장 싸다.\n\n## 운영 runbook에 들어갈 내용\n\n고위험 flag는 dashboard 버튼 하나로 끝내지 않는다. runbook에는 변경 전 확인 지표, 변경할 scope, 전파 예상 시간, rollback 방법, 알림 채널, 변경 후 관찰할 지표가 있어야 한다. 예를 들어 checkout flag를 10%에서 50%로 올릴 때는 주문 생성 성공률, 결제 승인 실패율, p95 latency, fallback count를 함께 본다.\n\n변경 직후에는 \"값을 바꿨다\"보다 \"어떤 버전의 config가 어느 인스턴스에서 적용됐는지\"가 중요하다. 로그에 `flag_key`, `flag_value`, `config_version`, `evaluation_reason`이 남으면 사고 분석에서 요청별 판단을 복원할 수 있다. 개인정보나 실험군 식별자는 최소화하되, 운영 판단을 재현할 수 있는 정도의 근거는 남긴다.\n\n## 리뷰에서 바로 쓰는 질문\n\n- 이 flag의 owner와 expiry가 PR에 보이는가?\n- release flag인가, kill switch인가, 영구 config인가?\n- default value가 안전한가?\n- config 서버 장애나 전파 지연 때 어떤 값으로 동작하는가?\n- rollout 완료 후 제거 PR이 어떤 조건으로 열리는가?\n- 변경 audit에 actor, reason, scope, before/after가 남는가?\n\n## 한 줄 정리\n\nfeature flag의 품질은 켜고 끄는 코드보다 수명, 전파, audit, 제거 조건으로 결정된다.\n",
  "aliases": [
    "feature flag cleanup",
    "stale flag cleanup",
    "flag owner expiry",
    "kill switch 운영",
    "피처 플래그 제거",
    "플래그 만료 기준",
    "런타임 설정 부채",
    "remote config 전파 지연"
  ],
  "expected_queries": [
    "피처 플래그 만들 때 owner와 만료일을 왜 적어야 해?",
    "rollout 끝난 feature flag는 어떤 순서로 제거해?",
    "kill switch랑 release flag는 수명이 어떻게 달라?",
    "remote config 전파 지연 때문에 플래그가 늦게 꺼지면 어떻게 해?",
    "오래 남은 플래그 부채를 리뷰에서 어떻게 찾을 수 있어?"
  ],
  "symptoms": [
    "feature flag를 만들 때 owner, expiry, removal condition을 기록하지 않아 출시 후에도 stale branch가 남아",
    "kill switch와 release flag를 같은 수명으로 다뤄 사고 차단용 flag까지 cleanup 대상처럼 제거하거나 반대로 임시 release flag를 영구 설정처럼 남겨",
    "remote config 전파 지연, cache TTL, audit trail 없이 플래그를 즉시 일관되게 바뀌는 전역 변수처럼 사용해"
  ],
  "relations": {
    "prerequisites": [
      "software-engineering/feature-flag-dependency-management",
      "software-engineering/configuration-governance"
    ],
    "next_docs": [
      "software-engineering/kill-switch-fast-fail",
      "software-engineering/feature-flag-cleanup-expiration",
      "software-engineering/configuration-governance"
    ],
    "confusable_with": [
      "software-engineering/feature-flag-dependency-management",
      "software-engineering/feature-flag-cleanup-expiration",
      "software-engineering/kill-switch-fast-fail",
      "software-engineering/configuration-governance"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-05-24",
    "legacy_path": "knowledge/cs/contents/software-engineering/feature-flag-lifecycle-debt-playbook.md",
    "revision_history": [
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch145"
      }
    ]
  }
}
