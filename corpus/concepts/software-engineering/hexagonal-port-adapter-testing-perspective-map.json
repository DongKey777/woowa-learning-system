{
  "id": "software-engineering/hexagonal-port-adapter-testing-perspective-map",
  "title": "Hexagonal Port, Adapter, Testing Perspective Map",
  "category": "software-engineering",
  "level": "advanced",
  "summary": "hexagonal architecture를 inbound/outbound port, adapter, domain test, contract test 관점으로 판단하는 software engineering perspective map",
  "body_markdown": "# Hexagonal Port, Adapter, Testing Perspective Map\n\n## 이 문서가 맡는 질문\nhexagonal architecture는 폴더 모양이 아니라 domain을 외부 기술에서 분리하는 dependency rule이다. port와 adapter를 나누면 테스트 경계도 달라져야 한다.\n\n## 빠른 분기\ndomain rule은 pure unit, inbound adapter는 request/response slice, outbound adapter는 contract/fake/integration test를 본다.\n\n## 실전 시나리오\n예약 service가 JpaRepository와 payment gateway client DTO를 직접 알면 domain rule 테스트가 DB와 외부 API에 묶인다. port를 두면 domain은 fake adapter로 빠르게 검증하고 adapter는 별도 contract로 검증한다.\n\n## CS 배경\nPorts and adapters는 application core가 외부 detail에 의존하지 않도록 dependency direction을 뒤집는다. Inbound adapter는 사용자의 요청을 use case로 변환하고, outbound adapter는 port interface를 외부 시스템 호출로 구현한다.\n\n## port 예시\n```java\ninterface PaymentPort {\n    PaymentResult approve(PaymentCommand command);\n}\nfinal class PaymentGatewayAdapter implements PaymentPort { ... }\n```\ncore는 PaymentPort만 알고 payment gateway SDK는 adapter에 갇힌다. test double도 port contract를 기준으로 만든다.\n\n## 깊게 볼 포인트\nport가 너무 많으면 indirection이 늘고, 너무 적으면 외부 모델이 domain에 새어 든다. adapter test는 mapping과 failure translation을 놓치면 안 된다.\n\n## 다음 행동\n1. core가 외부 framework type을 알고 있는지 확인한다\n2. inbound/outbound adapter를 분리한다\n3. port contract test를 만든다\n4. fake가 실제 adapter 제약을 숨기지 않는지 보완한다\n\n## 확인 질문\n이 의존성은 domain 안쪽으로 들어와야 하는가, adapter 바깥에서 변환되어야 하는가?\n\n## 추가 실전 포인트\nFake adapter는 빠른 core test에 유용하지만 real adapter의 timeout, serialization, unique constraint, error translation을 모두 재현하지 못한다. 그래서 port contract test를 만들어 fake와 real adapter가 같은 성공/실패 의미를 만족하는지 확인한다. inbound adapter는 HTTP status, validation, auth context mapping을 검증하고, core use case는 framework 없이 domain rule만 검증한다.\n\n## 추가 예시: port contract test\n```java\ninterface PaymentPortContract {\n    PaymentPort subject();\n\n    @Test\n    default void mapsDeclinedPayment() {\n        PaymentResult result = subject().approve(declinedCommand());\n        assertThat(result.status()).isEqualTo(PaymentStatus.DECLINED);\n    }\n}\n```\nfake adapter와 real payment gateway adapter가 같은 contract test를 통과하면 core test의 빠름과 adapter 검증의 신뢰도를 함께 얻을 수 있다.\n\n## 판단 매트릭스\n- Port: core가 필요로 하는 capability를 언어로 정의한다\n- Inbound adapter: 외부 요청을 use case input으로 변환한다\n- Outbound adapter: 외부 시스템 detail을 port 뒤에 숨긴다\n- 리뷰 질문: 테스트가 빠른 core test와 adapter contract test로 나뉘는가\n\n## 측정 지표와 레이턴시\ndomain test runtime, adapter contract failure, fake/real divergence, mapping error count, test suite p95를 본다. 품질은 boundary leak 감소, 성능은 test feedback, 레이턴시는 adapter call p95와 테스트 시간으로 판단한다.\n\n## 자주 하는 오해\n- hexagonal을 package 이름으로만 이해한다\n- port를 외부 API 모양 그대로 만든다\n- fake test만 보고 adapter contract를 검증하지 않는다\n\n이 오해를 줄이려면 먼저 실패 모드와 관찰 증거를 나누고, 그 다음 용어를 붙인다. 같은 현상이 자료구조, 런타임, 네트워크, 운영 지표에서 다르게 보일 수 있으므로 code path와 runtime signal을 함께 확인한다.\n\n## 학습자가 답변에 포함해야 할 구조\n답변은 dependency direction과 테스트 경계를 함께 설명해야 한다. 마지막에는 boundary leak, contract failure, test latency로 품질/성능/레이턴시를 검증한다고 말한다.\n\n## 실전 검증 루프\n1. domain에서 framework/import leak을 스캔한다\n2. port contract를 문서화한다\n3. fake와 real adapter를 같은 contract test에 태운다\n4. inbound slice와 outbound integration test를 분리한다\n5. 테스트 suite p95를 비교한다\n\n이 루프를 지키면 Hexagonal Port, Adapter, Testing Perspective Map 문서는 단순 정의 카드가 아니라 실제 증상을 CS 개념, 구현 경계, 운영 지표로 연결하는 학습 도구가 된다.\n\n## 한 줄 정리\nhexagonal architecture를 inbound/outbound port, adapter, domain test, contract test 관점으로 판단하는 software engineering perspective map\n\n## 품질 관점\n설계 품질은 변경 안전성, 테스트 증거, 책임 경계, 운영 관측 가능성을 함께 본다. 이 문서의 답변 품질은 `Hexagonal Port, Adapter, Testing Perspective Map`를 단순 용어가 아니라 선택 기준, 실패 모드, 검증 방법까지 연결하는지로 판단한다. 특히 비슷한 이웃 문서와 섞일 때 primary answer가 왜 이 문서인지 설명할 수 있어야 한다.\n\n## 성능 관점\n성능은 평균 처리량 하나로 판단하지 않는다. 입력 크기, 큐/락/캐시/네트워크/런타임 비용 중 무엇이 병목인지 분리하고, 작은 synthetic case와 production-like case를 따로 측정한다. 최적화가 correctness invariant를 흐리면 성능 개선으로 인정하지 않는다.\n\n## 레이턴시 관점\n레이턴시는 p50보다 p95/p99와 cold path, retry, queueing, lock wait, cache miss를 함께 본다. 학습 답변에는 latency budget과 관측 지표가 포함되어야 하며, 개선 전후가 같은 조건에서 비교되어야 한다.\n\n## 주제별 추가 지표\n`test_coverage_signal, contract_break_count, change_lead_time, defect_escape_count`를 최소 지표로 둔다. 이 지표들은 품질, 성능, 레이턴시를 한 화면에서 비교하기 위한 기준이며, 단순 통과/실패보다 어떤 경계가 병목인지 드러내야 한다.\n\n## 테스트 경계 예시\n\n헥사고날 구조에서 테스트는 빠른 core test 하나로 끝나지 않는다. Core use case는 fake outbound port를 사용해 domain rule과 orchestration을 빠르게 검증한다. Inbound adapter test는 HTTP validation, auth context mapping, status/body 변환을 본다. Outbound adapter test는 외부 SDK, serialization, timeout, error translation, retry 가능 여부를 contract나 integration test로 확인한다.\n\nFake adapter만 믿으면 실제 adapter가 만드는 실패 의미를 놓칠 수 있다. 예를 들어 fake payment adapter는 항상 `DECLINED`를 잘 돌려주지만, 실제 PG SDK는 timeout, ambiguous success, provider error code를 다른 모양으로 줄 수 있다. 그래서 port contract에는 core가 기대하는 성공/실패 의미를 적고, fake와 real adapter가 같은 의미를 만족하는지 확인한다.\n\n## 경계 누수 신호\n\nPort가 외부 API DTO 그대로라면 core가 외부 시스템 언어를 알게 된다. Domain이 `ResponseEntity`, JPA repository, PG SDK exception, Kafka record를 직접 알면 adapter가 core 안으로 들어온 것이다. 반대로 adapter가 domain 규칙을 재해석해 다른 error code로 바꾸면 contract가 어긋난다. 좋은 boundary는 core language로 port를 정의하고, adapter가 외부 세부를 그 language로 번역한다.\n",
  "aliases": [
    "hexagonal port adapter testing",
    "ports adapters test strategy",
    "inbound outbound adapter contract",
    "hexagonal architecture boundary",
    "헥사고날 포트 어댑터 테스트",
    "포트 어댑터 계약 테스트",
    "fake adapter contract test",
    "아웃바운드 어댑터 통합 테스트",
    "domain framework leak",
    "payment port adapter boundary"
  ],
  "expected_queries": [
    "hexagonal architecture에서 port와 adapter 테스트는 어떻게 나눠?",
    "fake adapter만 있으면 충분한 테스트야?",
    "Hexagonal architecture에서 port/adapter 경계와 테스트 품질을 어떻게 설계해?",
    "outbound adapter는 fake로 core test를 빠르게 하면서 real adapter contract를 어떻게 검증해?",
    "inbound adapter, application core, outbound adapter 테스트를 각각 어떤 책임으로 나눠야 해?",
    "port를 외부 API DTO 모양 그대로 만들면 hexagonal boundary가 왜 새는 거야?"
  ],
  "symptoms": [
    "hexagonal을 package 이름으로만 이해한다",
    "port를 외부 API 모양 그대로 만든다",
    "fake test만 보고 adapter contract를 검증하지 않는다"
  ],
  "relations": {
    "prerequisites": [
      "software-engineering/ports-and-adapters-beginner-primer",
      "software-engineering/hexagonal-testing-seams-primer",
      "software-engineering/inbound-adapter-test-slices-primer"
    ],
    "next_docs": [
      "software-engineering/outbox-message-adapter-test-matrix"
    ],
    "confusable_with": [
      "design-pattern/hexagonal-ports-pattern-language"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [
    {
      "pattern": "hexagonal에서 port adapter 테스트는 어떻게 나눠?",
      "match_count": 1,
      "added_at": "2026-05-27"
    },
    {
      "pattern": "fake adapter만 믿어도 돼?",
      "match_count": 1,
      "added_at": "2026-05-27"
    },
    {
      "pattern": "payment gateway adapter contract test는 왜 필요해?",
      "match_count": 1,
      "added_at": "2026-05-27"
    }
  ],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-27",
    "last_modified": "2026-06-18",
    "revision_history": [
      {
        "date": "2026-05-27",
        "source": "y14-batch4"
      },
      {
        "date": "2026-05-28",
        "source": "y14-closure-quality-audit"
      },
      {
        "date": "2026-05-28",
        "source": "y14-qrels-anchor-sync"
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch140",
        "note": "Expanded DTO/domain/entity, service responsibility, rich-domain, and hexagonal testing boundary surfaces."
      }
    ]
  }
}
