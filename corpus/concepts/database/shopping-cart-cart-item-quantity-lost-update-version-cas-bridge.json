{
  "id": "database/shopping-cart-cart-item-quantity-lost-update-version-cas-bridge",
  "title": "shopping-cart 장바구니 수량 동시 수정 ↔ Lost Update와 Version CAS 브릿지",
  "category": "database",
  "level": "intermediate",
  "summary": "shopping-cart에서 같은 cart item 수량을 두 요청이 거의 동시에 바꿀 때 핵심은 \"중복 생성\"이 아니라 이미 있는 row를 서로 덮어쓰는 lost update다. 이 장면은 UNIQUE보다 version column 기반 CAS나 충돌 재시도 규약으로 읽는 편이 맞다.",
  "body_markdown": "# shopping-cart 장바구니 수량 동시 수정 ↔ Lost Update와 Version CAS 브릿지\n\n## 한 줄 요약\n\nshopping-cart에서 같은 cart item 수량을 두 요청이 거의 동시에 바꿀 때 핵심은 \"중복 생성\"이 아니라 이미 있는 row를 서로 덮어쓰는 lost update다. 이 장면은 `UNIQUE`보다 `version` column 기반 CAS나 충돌 재시도 규약으로 읽는 편이 맞다.\n\n## 미션 진입 증상\n\n| 학습자 발화 | 미션 장면 | 이 문서에서 먼저 잡을 것 |\n|---|---|---|\n| \"장바구니 수량을 동시에 바꾸면 마지막 저장만 남아요\" | 두 탭에서 같은 cart item 수량을 수정 | existing row lost update로 보고 version/CAS를 검토한다 |\n| \"수량 증가와 삭제가 동시에 오면 결과가 이상하게 섞여요\" | 증가 버튼, 삭제 버튼, 빠른 연속 요청 race | update winner와 conflict 응답/재시도 규약을 정한다 |\n| \"수량 수정은 `UNIQUE`보다 version column이 맞다는 말이 헷갈려요\" | 중복 insert와 기존 row 갱신 충돌이 섞인 이해 | cart item identity 보호와 quantity update 충돌 감지를 분리한다 |\n\n## 미션 시나리오\n\nshopping-cart 미션에서 장바구니 수량 수정 API를 붙이면 학습자는 자주 `cartItem.getQuantity()`를 읽고 `+1`이나 `0`을 계산한 뒤 다시 저장한다. 문제는 같은 사용자가 두 탭을 열어 두었거나, 수량 증가 버튼을 빠르게 연속 클릭하면 두 요청이 같은 이전 값을 보고 각자 저장할 수 있다는 점이다.\n\n이때 PR에서는 \"마지막 저장이 우연히 이긴다\", \"삭제와 증가가 동시에 오면 어느 의도가 남는지 정의되지 않았다\"는 코멘트가 붙기 쉽다. `@Transactional`을 붙여도 읽은 뒤 계산해서 저장하는 구조 자체가 남아 있으면, 같은 cart item row에 대한 stale write 문제가 자동으로 사라지지는 않는다.\n\n## CS concept 매핑\n\n이 장면은 payment idempotency처럼 \"같은 요청을 한 번만 인정\"하는 문제가 아니다. cart item row는 이미 존재하고, 경쟁은 그 row의 현재 수량을 누가 어떤 기준으로 갱신하느냐다. 그래서 질문 축은 duplicate insert보다 lost update에 가깝다.\n\nversion CAS를 쓰면 `UPDATE cart_item SET quantity = ?, version = version + 1 WHERE id = ? AND version = ?`처럼 내가 읽은 상태가 아직 최신일 때만 저장한다. update count가 0이면 다른 요청이 먼저 바꿨다는 뜻이므로, 서버는 최신 수량 재조회 후 재시도하거나 \"수량이 이미 변경되었습니다\" 같은 충돌 응답을 돌려줄 수 있다. 수량 감소 자체를 한 SQL로 닫을 수 있으면 조건부 UPDATE가 더 단순할 수 있지만, 화면에서 사용자가 보고 있던 cart 상태와의 충돌 감지가 중요하면 version CAS가 설명력이 좋다.\n\n## 미션 PR 코멘트 패턴\n\n- \"`find -> quantity 계산 -> save` 흐름이라 두 요청이 같은 이전 값을 보고 덮어쓸 수 있습니다. cart item 갱신 충돌을 어떻게 감지할지 먼저 정해 보세요.\"\n- \"여기는 중복 주문 방지가 아니라 existing row lost update 문제라서 `UNIQUE`보다 version column이나 원자적 UPDATE 쪽이 더 직접적입니다.\"\n- \"삭제와 증가가 동시에 오면 마지막 요청이 우연히 승자가 됩니다. 충돌 시 재조회/재시도 규약을 API에서 드러내 주세요.\"\n- \"`@Transactional`은 묶어 주지만 stale write를 판정해 주지 않습니다. update count 0이나 optimistic lock 예외를 어떻게 번역할지 필요합니다.\"\n\n## 실전 예시: Lost Update 재현 + 두 가지 수정 패턴\n\n### Lost Update가 일어나는 interleaving\n\n```text\n초기 상태: cart_item(id=1, quantity=1, version=3)\n\nTx A (탭1: 수량 +1 요청)           Tx B (탭2: 수량 +1 요청)\n──────────────────────────────────────────────────────\nSELECT quantity, version\n  WHERE id=1\n→ quantity=1, version=3\n\n                                    SELECT quantity, version\n                                      WHERE id=1\n                                    → quantity=1, version=3\n\nUPDATE cart_item\n  SET quantity=2\n  WHERE id=1\n→ OK, quantity=2\n\n                                    UPDATE cart_item\n                                      SET quantity=2\n                                      WHERE id=1\n                                    → OK, quantity=2  ← Tx A 증가분 사라짐\nCOMMIT                              COMMIT\n```\n\n두 요청이 모두 `quantity=1`을 읽고 `quantity=2`로 썼다. 최종 값은 2이지만, 두 번 증가했으므로 올바른 값은 3이어야 한다. Tx A의 쓰기가 통째로 사라진 게 lost update다.\n\n### 수정 패턴 1: Version CAS (낙관적 잠금)\n\n```sql\n-- 내가 읽었던 version이 아직 유효할 때만 갱신\nUPDATE cart_item\n   SET quantity = ?,\n       version  = version + 1\n WHERE id      = ?\n   AND version = ?\n```\n\n```java\n// Spring Data JPA + @Version\n@Entity\npublic class CartItem {\n    @Id\n    private Long id;\n\n    private int quantity;\n\n    @Version\n    private Long version;\n}\n\n// 서비스 레이어\n@Transactional\npublic CartItem increaseQuantity(Long cartItemId, int delta) {\n    CartItem item = cartItemRepository.findById(cartItemId)\n        .orElseThrow(CartItemNotFoundException::new);\n    item.setQuantity(item.getQuantity() + delta);\n    return item; // flush 시 JPA가 version 조건 UPDATE 발행\n    // version 불일치 → OptimisticLockingFailureException\n}\n\n// 호출부: 충돌 재시도 (retryService는 increaseQuantity와 다른 빈이어야 한다)\npublic CartItem increaseWithRetry(Long id, int delta) {\n    int maxRetry = 3;\n    for (int i = 0; i < maxRetry; i++) {\n        try {\n            // 매 시도가 새 트랜잭션 — 직전 읽기값을 버리고 findById부터 다시 시작\n            return retryService.increaseQuantity(id, delta);\n        } catch (OptimisticLockingFailureException e) {\n            if (i == maxRetry - 1) throw e;\n            // 충돌 → 다음 루프에서 최신 version으로 다시 읽는다\n        }\n    }\n    throw new IllegalStateException(\"unreachable\");\n}\n```\n\n`update count = 0`이면 다른 요청이 먼저 version을 올렸다는 뜻이다. **재시도는 반드시 SELECT부터 다시 시작해야 한다.** 이전에 읽은 quantity를 들고 UPDATE만 다시 보내면 stale increment가 그대로 남는다.\n\n> **함정:** 위 재시도 루프와 `@Transactional increaseQuantity`를 **같은 빈에 두고 `this.increaseQuantity(...)`로 부르면 안 된다.** Spring `@Transactional`은 프록시로 동작하므로 같은 빈 내부 호출(self-invocation)은 프록시를 거치지 않아 매 시도가 새 트랜잭션으로 열리지 않는다. 그러면 한 트랜잭션 안에서 같은 persistence context를 재사용해 재시도해도 stale 1차 캐시를 다시 본다. 재시도 메서드를 **다른 빈**으로 분리하거나 `TransactionTemplate`로 매 시도마다 트랜잭션을 명시적으로 새로 연다.\n\n### 수정 패턴 2: 원자적 in-place 증가\n\n```sql\n-- 읽기 없이 DB 안에서 덧셈\nUPDATE cart_item\n   SET quantity = quantity + ?\n WHERE id = ?\n   AND quantity + ? >= 0   -- 음수 수량 방지 조건\n```\n\n```java\n@Modifying\n@Query(\"UPDATE CartItem c SET c.quantity = c.quantity + :delta \" +\n       \"WHERE c.id = :id AND c.quantity + :delta >= 0\")\nint incrementQuantity(@Param(\"id\") Long id, @Param(\"delta\") int delta);\n```\n\n두 요청이 동시에 들어와도 DB 엔진이 row-level lock으로 순서를 보장한다. 읽기-쓰기 사이에 다른 쓰기가 끼어들 틈이 없다.\n\n### 두 패턴을 언제 쓰나\n\n| 기준 | Version CAS | 원자적 in-place |\n|---|---|---|\n| 화면 표시 수량과의 충돌 감지 필요 | 적합 — 사용자가 보던 값이 바뀌었음을 알려야 할 때 | 부적합 — 충돌 여부를 서버가 알 수 없음 |\n| 단순 증감 (+1/-1) | 가능하지만 재시도 로직 필요 | 단순, 적합 |\n| 복잡한 invariant (수량 × 가격 검증 등) | 읽은 값 기준으로 검증 가능 | 한 SQL에서 복잡 조건 처리 어려움 |\n| 충돌 빈도 | 낮을 때 효율적 (락 없음) | 충돌 빈도 무관 |\n\nshopping-cart 수량 변경은 \"사용자가 보던 수량과 지금 DB 수량이 다르면 알려 줘야 해?\" 여부가 첫 분기점이다. 그렇다면 version CAS가 맞고, 단순히 버튼 클릭마다 +1이면 in-place가 더 직관적이다.\n\n## 스스로 점검\n\n- version CAS에서 `OptimisticLockingFailureException`이 발생했을 때 재시도 루프가 SELECT를 다시 하지 않고 UPDATE만 다시 보내면 어떤 일이 생길까?\n- 삭제 요청과 수량 증가 요청이 동시에 오는 상황에서는 version CAS와 in-place 증가 중 어느 쪽이 의도를 더 안전하게 표현하고, 그 이유는 뭘까?\n\n## 다음 학습\n\n- `UNIQUE`, version CAS, `FOR UPDATE`의 첫 분기를 다시 잡으려면 `database/unique-vs-version-cas-vs-for-update-chooser`\n- lost update를 anomaly 관점에서 다시 정리하려면 `database/transaction-isolation-basics`\n- row lock이 필요한 장면과 충돌 재시도 장면을 나눠 보려면 `database/lock-basics`\n- shopping-cart에서 중복 주문 방지와 재고 차감 축을 따로 보려면 `database/shopping-cart-payment-idempotency-stock-bridge`\n",
  "aliases": [
    "shopping-cart 장바구니 수량 동시 수정",
    "장바구니 수량 lost update",
    "cart item quantity race",
    "shopping-cart 수량 덮어쓰기",
    "version CAS 장바구니",
    "@Transactional"
  ],
  "expected_queries": [
    "shopping-cart에서 장바구니 수량을 두 탭에서 바꾸면 왜 한쪽 수정이 덮여?",
    "장바구니 상품 수량 증가와 삭제가 동시에 오면 DB에서 무엇을 먼저 설계해야 해?",
    "cart item quantity를 읽어서 plus one 한 뒤 저장하면 왜 race가 남아?",
    "shopping-cart 미션에서 수량 수정은 UNIQUE보다 version column이 더 맞다는 말이 무슨 뜻이야?",
    "장바구니 수량 변경 API에 낙관적 락이나 compare-and-set을 언제 붙여야 해?"
  ],
  "symptoms": [
    "장바구니 수량을 동시에 바꾸면 마지막 저장만 남아요",
    "같은 상품 수량을 두 탭에서 수정했더니 한쪽 변경이 사라져요",
    "수량 증가와 삭제가 동시에 오면 결과가 이상하게 섞여요"
  ],
  "relations": {
    "prerequisites": [
      "database/transaction-isolation-basics",
      "database/lock-basics",
      "database/unique-vs-version-cas-vs-for-update-chooser"
    ],
    "next_docs": [
      "database/unique-vs-version-cas-vs-for-update-chooser",
      "database/transaction-isolation-basics",
      "database/lock-basics"
    ],
    "confusable_with": [
      "database/shopping-cart-reclick-concurrent-write-cause-router",
      "database/shopping-cart-cart-item-merge-unique-upsert-bridge",
      "database/unique-vs-version-cas-vs-for-update-chooser",
      "database/shopping-cart-payment-idempotency-stock-bridge",
      "spring/shopping-cart-payment-transaction-boundary-bridge",
      "database/transaction-lost-update-mission-drill"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-05-31",
    "legacy_path": "knowledge/cs/contents/database/shopping-cart-cart-item-quantity-lost-update-version-cas-bridge.md",
    "revision_history": []
  }
}