{
  "id": "database/sql-joins-foundations-primer",
  "title": "SQL JOIN 기본기 — INNER/LEFT/RIGHT/FULL 그리고 ON 절",
  "category": "database",
  "level": "beginner",
  "summary": "JOIN은 두 테이블 행을 매칭해 한 줄로 합치는 연산이고, INNER는 양쪽 매칭만, OUTER는 한쪽 기준으로 모두 보존하며 못 찾은 쪽은 NULL이다.",
  "body_markdown": "# SQL JOIN 기본기 — INNER/LEFT/RIGHT/FULL 그리고 ON 절\n\n> 한 줄 요약: JOIN은 두 테이블 행을 매칭해 한 줄로 합치는 연산이고, INNER는 양쪽 매칭만, OUTER는 한쪽 기준으로 모두 보존하며 못 찾은 쪽은 NULL이다.\n\n**난이도: 🟢 Beginner**\n\n## 먼저 멘탈모델\n\n테이블 두 개가 있다고 상상해 보자. 회원 테이블 `member(id, name)`과 주문 테이블 `orders(id, member_id, amount)`이 있다. JOIN은 *어떤 회원이 어떤 주문을 했는지* 한 줄로 합쳐서 보고 싶을 때 쓰는 도구다.\n\n핵심은 두 가지 질문이다.\n\n1. **매칭 기준이 뭐냐?** — `member.id = orders.member_id`처럼 행을 연결할 조건. 이 조건은 `ON` 절에 적는다.\n2. **매칭 안 된 행은 어떻게 할 거냐?** — 버릴까(INNER), 한쪽을 살릴까(LEFT/RIGHT OUTER), 양쪽 다 살릴까(FULL OUTER).\n\n이 두 질문을 결정하면 JOIN 종류가 자동으로 정해진다.\n\n## 한 장 정리\n\n```sql\n-- INNER JOIN: 양쪽 모두 매칭되는 행만\nSELECT m.name, o.amount\nFROM member m\nINNER JOIN orders o ON o.member_id = m.id;\n\n-- LEFT (OUTER) JOIN: member는 다 살리고 orders는 매칭만, 없으면 NULL\nSELECT m.name, o.amount\nFROM member m\nLEFT JOIN orders o ON o.member_id = m.id;\n\n-- RIGHT (OUTER) JOIN: orders 기준 (LEFT와 좌우만 바뀐 것)\nSELECT m.name, o.amount\nFROM member m\nRIGHT JOIN orders o ON o.member_id = m.id;\n\n-- FULL OUTER JOIN: 양쪽 다 살리고 매칭 안 된 칸은 NULL\nSELECT m.name, o.amount\nFROM member m\nFULL OUTER JOIN orders o ON o.member_id = m.id;\n```\n\n### 결과가 어떻게 달라지나\n\n예시 데이터:\n\n```\nmember            orders\nid  name          id  member_id  amount\n1   김자몽         100  1          5000\n2   박망고         101  1          3000\n3   이수박         102  9          1000  -- 매칭 없는 외래 키 (의도적 예시)\n```\n\n| JOIN 종류 | 결과 행 수 | 어떤 행이 살아남나 |\n|---|---|---|\n| INNER JOIN | 2 | 김자몽-5000, 김자몽-3000 |\n| LEFT JOIN (member 기준) | 4 | 김자몽-5000, 김자몽-3000, 박망고-NULL, 이수박-NULL |\n| RIGHT JOIN (orders 기준) | 3 | 김자몽-5000, 김자몽-3000, NULL-1000 |\n| FULL OUTER JOIN | 5 | 위 LEFT + 추가로 NULL-1000 |\n\n`박망고`는 주문이 없어서 INNER에서는 사라지지만 LEFT에서는 `amount = NULL`로 남는다. `member_id = 9` 주문은 매칭되는 회원이 없어서 RIGHT/FULL에서만 보인다.\n\n## ON 절과 WHERE 절의 차이\n\n이 부분이 가장 헷갈리는 지점이다. INNER JOIN에서는 ON과 WHERE가 결과상 거의 같지만, **OUTER JOIN에서는 의미가 완전히 다르다**.\n\n```sql\n-- (A) ON에 조건\nSELECT m.name, o.amount\nFROM member m\nLEFT JOIN orders o ON o.member_id = m.id\n               AND o.amount > 4000;\n\n-- (B) WHERE에 조건\nSELECT m.name, o.amount\nFROM member m\nLEFT JOIN orders o ON o.member_id = m.id\nWHERE o.amount > 4000;\n```\n\n- (A) ON에 조건을 추가 — *매칭 시점에서* `amount > 4000`인 주문만 짝짓고, 매칭 안 된 회원은 그래도 NULL로 살린다. 박망고/이수박은 amount=NULL로 결과에 남는다.\n- (B) WHERE에 조건을 추가 — JOIN 결과를 먼저 만든 뒤에 *전체 결과*를 필터링한다. `amount IS NULL`은 `> 4000`이 false이므로 박망고/이수박이 통째로 빠진다. **사실상 INNER JOIN과 같아진다.**\n\n기준 테이블의 행을 살리고 싶다면 추가 조건은 ON에, 결과 전체를 거를 거면 WHERE에. 자세한 사례 분기는 `database/left-join-filter-placement-primer.md`에서 다룬다.\n\n## 자주 헷갈리는 지점\n\n- **LEFT JOIN인데 결과가 INNER처럼 나옴** — WHERE 절에 우측 테이블 칼럼 조건을 걸어버리면 NULL 행이 다 빠져나간다. ON으로 옮기거나 `IS NULL` 처리를 같이 본다.\n- **JOIN 후 행이 폭증** — 매칭 칼럼이 unique가 아니라 1:N으로 카티시안에 가까워졌다. `database/join-row-explosion-symptom-router.md` 참고.\n- **DISTINCT로 폭증을 가리는 것** — 원인을 가린 것일 뿐. GROUP BY/EXISTS 등 다른 도구를 같이 본다. `database/distinct-vs-group-by-beginner-card.md`.\n- **FULL OUTER JOIN을 너무 빨리 쓰는 것** — MySQL은 표준 FULL OUTER JOIN을 직접 지원하지 않아 `LEFT JOIN UNION RIGHT JOIN`으로 흉내내야 한다. 정말 양쪽 다 보존해야 하는 분석성 쿼리에서만 등판시킨다.\n\n## 학습자가 자주 만나는 시나리오\n\n- \"회원별 주문 합계를 보고 싶은데 주문 없는 회원도 0으로 보고 싶다\" → LEFT JOIN + `COALESCE(SUM(amount), 0)`.\n- \"주문은 있는데 회원이 사라진 데이터를 찾고 싶다\" → RIGHT JOIN 후 `WHERE m.id IS NULL`.\n- \"참여한 적이 없는 회원을 골라내고 싶다\" → LEFT JOIN 후 `WHERE o.id IS NULL` (anti-join 패턴).\n\n## 빠른 확인 질문\n\n- INNER JOIN과 LEFT JOIN의 결과 행 수가 같을 수 있는 조건은?\n  - 우측 테이블이 매칭 기준에 대해 모든 좌측 행과 매칭될 때.\n- LEFT JOIN에서 ON에 조건과 WHERE에 조건의 차이가 가장 크게 드러나는 경우는?\n  - 우측 테이블 칼럼에 추가 조건을 거는 경우(NULL 행 보존 vs 제거).\n- FULL OUTER JOIN을 MySQL에서 흉내내는 표준 패턴은?\n  - `LEFT JOIN ... UNION RIGHT JOIN ...`.\n\n## 한 줄 정리\n\nJOIN은 *매칭 기준*과 *매칭 안 된 행을 어떻게 할지* 두 결정만 보면 종류가 정해진다. 그리고 외부 조인에서는 추가 조건을 ON에 쓸지 WHERE에 쓸지에 따라 결과가 INNER로 무너질 수 있다는 점만 잊지 말자.\n\nretrieval-anchor-keywords: SQL JOIN, INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL OUTER JOIN, JOIN ON 절, JOIN WHERE 절, OUTER JOIN null padding, anti-join, JOIN 기초, 두 테이블 합치기, 매칭 기준 ON, 결과 필터 WHERE\n\n### Batch148 body floor repair\nJOIN은 두 테이블을 단순히 붙이는 문법이 아니라 관계를 따라 row 조합을 만드는 연산이다. INNER JOIN은 양쪽에 매칭되는 row만 남기고, LEFT JOIN은 왼쪽 row를 보존하면서 오른쪽이 없으면 NULL을 채운다. 예약과 회원을 조인할 때 FK가 many-to-one이면 예약 row 수는 유지되는 것이 보통이지만, 일대다 관계를 잘못 조인하면 row가 곱처럼 불어난다. 그래서 조인 결과를 볼 때는 조인 키의 유일성, 관계 방향, NULL 보존 여부를 먼저 확인한다. 성능 문제를 보기 전에도 결과 cardinality를 손으로 예측하는 습관이 중요하다.\n\n실무에서 조인 버그는 대부분 문법보다 cardinality 예측 실패에서 나온다. 회원 1명에 예약 3개를 조인하면 회원 정보가 3번 반복되는 것이 정상이다. 여기에 결제 row가 예약당 2개라면 결과는 6줄이 될 수 있다. 그래서 조인 전후 row 수를 세고, 각 key가 unique인지 확인하는 습관이 필요하다.\n\nLEFT JOIN 뒤에 WHERE right_table.id IS NOT NULL을 붙이면 사실상 INNER JOIN처럼 동작할 수 있다. 반대로 오른쪽 조건을 ON에 둘지 WHERE에 둘지에 따라 NULL 보존 여부가 달라진다. 초보자는 결과가 비어 있을 때 join type만 보지만, 실제로는 조건 위치가 outer join의 의미를 없애는지까지 확인해야 한다.\n",
  "aliases": [
    "SQL JOIN 기본",
    "INNER JOIN",
    "LEFT JOIN",
    "RIGHT JOIN",
    "FULL OUTER JOIN",
    "JOIN ON 절",
    "JOIN 결합",
    "테이블 조인",
    "INNER OUTER 기준",
    "조인 보존 기준",
    "join foundations",
    "cross product join"
  ],
  "expected_queries": [
    "INNER JOIN과 LEFT JOIN 차이가 뭐야?",
    "JOIN ON 절과 WHERE 절은 어떻게 달라?",
    "LEFT JOIN 했더니 결과에 NULL이 생기는 이유가 뭐야?",
    "두 테이블 합칠 때 어떤 JOIN을 골라야 해?",
    "FULL OUTER JOIN은 언제 쓰는 거야?",
    "JOIN 기본부터 차근차근 알려줘"
  ],
  "symptoms": [
    "INNER vs LEFT JOIN 언제 쓰는지 모름",
    "LEFT JOIN 결과가 예상보다 많거나 NULL이 섞임",
    "JOIN ON과 WHERE를 같이 못 정리함",
    "FULL OUTER JOIN이 뭔지 감이 안 옴"
  ],
  "relations": {
    "prerequisites": [
      "database/normalization-basics",
      "database/primary-foreign-key-basics"
    ],
    "next_docs": [
      "database/left-join-filter-placement-primer",
      "database/join-row-explosion-symptom-router",
      "database/join-algorithms-across-engines"
    ],
    "confusable_with": [
      "database/distinct-vs-group-by-beginner-card",
      "database/left-join-filter-placement-primer"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-06-18",
    "legacy_path": "knowledge/cs/contents/database/sql-joins-foundations-primer.md",
    "revision_history": [
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch148"
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch148-repair",
        "note": "Expanded body_markdown to satisfy Stream B/C role floor after batch validation."
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch148-repair-2",
        "note": "Added extra worked-example detail to clear batch148 body floors."
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch148-repair-3",
        "note": "Added final worked-example detail to clear batch148 body floor."
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-audit-natural-entrypoint-repair"
      }
    ]
  }
}
