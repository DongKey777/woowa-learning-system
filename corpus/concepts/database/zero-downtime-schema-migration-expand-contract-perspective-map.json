{
  "id": "database/zero-downtime-schema-migration-expand-contract-perspective-map",
  "title": "Zero-Downtime Schema Migration Expand-Contract Perspective Map",
  "category": "database",
  "level": "intermediate",
  "summary": "무중단 스키마 변경을 expand, backfill, dual write/read, contract, rollback 관점으로 나누는 perspective map이다. 무중단 migration은 DDL 한 번이 아니라 app version과 DB schema가 여러 시점에 공존하는 호환성 문제다. Expand로 새 구조를 추가하고, backfill로 데이터를 채우고, application cutover 후 contract로 옛 구조를 제거한다.",
  "body_markdown": "# Zero-Downtime Schema Migration Expand-Contract Perspective Map\n\n## 이 문서가 맡는 질문\n무중단 migration은 DDL 한 번이 아니라 app version과 DB schema가 여러 시점에 공존하는 호환성 문제다. Expand로 새 구조를 추가하고, backfill로 데이터를 채우고, application cutover 후 contract로 옛 구조를 제거한다.\n\n## 빠른 분기\n컬럼 추가는 expand, 데이터 이동은 backfill, 새/옛 코드가 동시에 돌면 dual read/write, 제거는 contract 단계로 본다. rollback 가능성이 있으면 새 코드와 옛 schema 호환성을 확인한다.\n\n## 실전 시나리오\n`users.name`을 `first_name`, `last_name`으로 나눈다고 하자. 먼저 새 컬럼을 nullable로 추가한다. application은 기존 name도 읽고 새 컬럼도 쓸 수 있게 한다. backfill 후 read path를 새 컬럼으로 바꾸고, 충분히 관찰한 뒤 name을 제거한다.\n\n## CS 배경\nSchema migration은 distributed deployment 문제다. DB schema와 application binary가 동시에 바뀌지 않으며 rolling deploy 중 old/new version이 함께 실행된다. Backfill은 long-running batch이고 lock, replication lag, write amplification을 만들 수 있다.\n\n## 깊게 볼 포인트\n위험은 lock을 오래 잡는 DDL, non-null/default rewrite, trigger/dual write 불일치, rollback 불가 변경에서 나온다. 안전한 migration은 각 단계가 forward/backward compatible해야 하고, 관찰 지표와 중단 기준이 있어야 한다.\n\n## 다음 행동\n1. old app/new app/old schema/new schema 호환 표를 만든다. 2. expand와 contract를 별도 배포로 나눈다. 3. backfill은 chunk와 throttle을 둔다. 4. dual write 검증과 rollback plan을 기록한다.\n\n## 확인 질문\n이 변경은 rolling deploy 중 old code와 new code가 동시에 읽고 써도 안전한가?\n\n## 추가 예시\nNOT NULL 컬럼을 추가하려면 nullable 추가 -> backfill -> application validation -> constraint 추가 순서가 안전하다. 바로 NOT NULL DEFAULT를 넣으면 DB별로 table rewrite나 lock 위험이 있다.\n\n## 구체 예시\n```sql\nALTER TABLE orders ADD COLUMN delivery_memo VARCHAR(255) NULL;\nUPDATE orders SET delivery_memo = '' WHERE delivery_memo IS NULL AND id BETWEEN ? AND ?;\nSELECT COUNT(*) FROM orders WHERE old_memo <> delivery_memo;\n```\n첫 줄은 expand, 두 번째는 재시작 가능한 chunk backfill, 세 번째는 shadow validation에 해당한다. contract 단계에서 old column을 제거한 뒤에는 rollback이 어려우므로, mismatch count가 0이고 old application version이 남아 있지 않은 것을 배포 시스템에서 확인해야 한다.\n\n## 판단 매트릭스\n- Expand 근거: nullable column, dual-write, backward-compatible read가 이전 버전과 함께 동작하는지 본다.\n- Migrate 근거: backfill이 idempotent하고 chunked이며 lock과 replication lag를 제어하는지 확인한다.\n- Contract 근거: old code가 완전히 사라졌고 shadow read/data validation이 통과했을 때만 제거한다.\n- 리뷰 질문: rollback 가능한 단계와 불가능한 단계를 명확히 나눴는가?\n\n## 측정 지표와 레이턴시\n핵심 지표는 DDL duration, lock wait, replication lag, backfill rows/sec, error rate, dual-write mismatch이다. 마이그레이션 레이턴시는 평소 API p95와 DB lock wait로 관찰한다. backfill이 replication lag나 buffer pressure를 키우면 즉시 throttle해야 한다. \n\n## 자주 하는 오해\n- DDL 한 번으로 migration이 끝난다고 본다\n- app version과 schema version 공존을 고려하지 않는다\n- backfill이 production latency와 lock에 주는 영향을 놓친다\n이 오해를 줄이려면 schema migration을 DDL 한 줄이 아니라 application rollout과 data movement의 조합으로 본다. DB별 online DDL 지원과 lock 특성이 다르므로 같은 ALTER라도 운영 위험이 달라진다.\n\n## 학습자가 답변에 포함해야 할 구조\n먼저 변경이 backward compatible한지, dual-read/write가 필요한지 분류한다. 그 다음 expand, backfill, verify, switch, contract 단계를 설명한다. 마지막에는 데이터 품질, write 성능, lock/replication lag, 사용자 요청 레이턴시를 검증 지표로 붙인다.\n\n## 실전 검증 루프\n1. 새 schema가 old/new application version과 모두 호환되는지 migration matrix를 만든다.\n2. backfill을 chunk 단위로 실행하고 idempotency와 재시작 가능성을 확인한다.\n3. shadow read나 checksum으로 old/new column mismatch를 측정한다.\n4. lock time, replication lag, query p95, error rate를 migration 중에 관찰한다.\n5. contract 단계 전 rollback 불가 조건과 데이터 보존 정책을 문서화한다.\n\n이 루프를 지키면 migration 문서는 배포 절차가 아니라 compatibility와 data correctness를 다루는 자료가 된다.\n",
  "aliases": [
    "zero downtime schema migration",
    "expand contract migration",
    "무중단 스키마 마이그레이션",
    "backfill dual write",
    "database expand contract",
    "rolling deploy schema compatibility",
    "DDL lock migration",
    "schema rollback plan",
    "online migration",
    "contract phase column removal"
  ],
  "expected_queries": [
    "무중단 스키마 변경에서 expand contract는 어떻게 해?",
    "컬럼을 나눌 때 zero downtime migration 순서를 알려줘",
    "backfill과 dual write는 왜 필요해?",
    "rolling deploy 중 old app new app schema 호환성을 어떻게 봐?",
    "NOT NULL 컬럼 추가를 안전하게 하는 방법은?",
    "contract 단계에서 컬럼 제거는 언제 해?",
    "무중단 스키마 변경에서 expand backfill contract 순서를 어떻게 잡아?"
  ],
  "symptoms": [
    "DDL 한 번으로 migration이 끝난다고 본다",
    "app version과 schema version 공존을 고려하지 않는다",
    "backfill이 production latency와 lock에 주는 영향을 놓친다"
  ],
  "relations": {
    "prerequisites": [
      "database/schema-migration-zero-downtime-interview-playbook",
      "database/index-and-explain",
      "system-design/monotonic-writes-ordering-primer"
    ],
    "next_docs": [
      "system-design/zero-downtime-schema-migration-platform-design",
      "database/large-scale-data-migration-deep-dive",
      "design-pattern/normalization-version-rollout-playbook"
    ],
    "confusable_with": [
      "database/generated-columns-functional-index-migration",
      "design-pattern/cursor-pagination-parity-read-model-migration",
      "database/failover-visibility-window-topology-cache-playbook"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-27",
    "last_modified": "2026-05-27",
    "revision_history": [
      {
        "date": "2026-05-27",
        "source": "y14-batch2"
      }
    ]
  }
}
