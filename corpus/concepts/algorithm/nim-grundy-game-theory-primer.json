{
  "id": "algorithm/nim-grundy-game-theory-primer",
  "title": "Nim Grundy Game Theory Primer",
  "category": "algorithm",
  "level": "advanced",
  "summary": "이 primer는 normal-play impartial game에서 winning/losing state와 Grundy number, 독립 subgame xor 합성 조건을 면접 수준으로 설명한다.",
  "body_markdown": "# Nim Grundy Game Theory Primer\n\n## 한 줄 요약\n\nnormal play의 공평 게임(impartial game)에서는 상태를 \"현재 차례의 플레이어가 이길 수 있는가\"로 보고, 서로 독립인 부분 게임(subgame)의 합은 그런디 수(Grundy number)의 xor로 합성할 수 있다.\n\n## 미션 진입 증상\n\n두 사람이 번갈아 돌을 가져가거나 말을 이동하는 문제가 나오면, 단순 DFS보다 승패 상태를 먼저 정의한다. normal play 규칙에서는 움직일 수 없는 상태가 지는 상태(losing state)다. 현재 상태에서 상대를 losing state로 보낼 수 있는 move가 하나라도 있으면 현재 상태는 winning state다.\n\n다만 \"두 사람이 번갈아 한다\"만으로 Grundy를 적용하면 안 된다. Sprague-Grundy 모델은 보통 다음 조건을 전제로 둔다.\n\n| 조건 | 의미 |\n|---|---|\n| impartial | 같은 상태에서 두 플레이어가 선택할 수 있는 move 집합이 같다 |\n| perfect information | 숨겨진 정보가 없고 양쪽이 같은 상태를 본다 |\n| deterministic | 주사위나 확률 이벤트가 다음 상태를 정하지 않는다 |\n| finite / acyclic | 무한 반복이나 draw 없이 terminal state로 내려간다 |\n| normal play | 마지막 move를 한 사람이 이기고, move가 없으면 진다 |\n| disjoint sum | 여러 subgame 중 한 턴에 정확히 하나만 건드리며 subgame끼리 상태가 섞이지 않는다 |\n\n## 핵심 mental model\n\n가장 기본 DP는 다음 형태다.\n\n```text\nwin[state] = any(not win[next] for next in moves)\n```\n\n그런디 수는 `grundy[state] = mex(grundy[next])`로 계산한다. `mex`는 등장하지 않은 가장 작은 0 이상의 정수다. Grundy 값이 0인 상태는 losing state이고, 0이 아닌 상태는 winning state다.\n\n직관적으로 `grundy[state] = k`라는 말은 이 상태가 \"크기 k의 Nim heap\"처럼 행동한다는 뜻이다. 그래서 서로 독립인 subgame들의 합은 숫자를 더하지 않고 bitwise xor로 합친다.\n\n```text\ntotal = grundy[subgame1] xor grundy[subgame2] xor ...\ntotal == 0  -> 현재 플레이어가 완벽히 대응당하는 losing state\ntotal != 0  -> 어떤 move로 total을 0으로 만들 수 있는 winning state\n```\n\n표준 Nim에서 pile 하나는 돌을 1개 이상 원하는 만큼 가져갈 수 있으므로 pile 크기 자체가 Grundy 값이 된다. 하지만 \"한 번에 1~3개만 가져가기\", \"특정 위치로만 이동\" 같은 변형 게임에서는 pile 크기를 그대로 xor하면 안 되고, 각 상태의 `mex(next Grundy)`를 다시 계산해야 한다.\n\n## 작은 예시\n\n표준 Nim 상태 `(1, 1)`은 `1 xor 1 = 0`이다. 현재 플레이어가 한 pile에서 1개를 가져가면 `(0, 1)`이 되고, 상대가 남은 1개를 가져가서 이긴다. 그래서 `(1, 1)`은 losing state다.\n\n상태 `(1, 2)`는 `1 xor 2 = 3`이라 0이 아니다. 두 번째 pile을 2에서 1로 줄이면 `(1, 1)`이 되어 xor가 0이 된다. 즉 현재 플레이어는 상대에게 losing state를 넘기는 move를 만들 수 있다.\n\n이 예시는 xor 규칙을 보여주기 위한 표준 Nim 예시다. 다른 move rule에서는 먼저 각 component의 Grundy 값을 구한 뒤 같은 xor 판단을 적용한다.\n\n## 계산 절차\n\nGrundy 문제를 풀 때는 바로 xor부터 쓰지 말고 다음 순서로 내려간다.\n\n1. 상태를 정의한다. pile 크기, 말의 위치, 남은 간선, bitmask처럼 move 결과를 완전히 표현해야 한다.\n2. terminal state를 정한다. normal play라면 더 움직일 수 없는 상태의 Grundy 값은 0이다.\n3. 각 상태에서 갈 수 있는 next state 목록을 만든다.\n4. next state들의 Grundy 값을 모아 `mex`를 계산한다.\n5. 단일 게임이면 `grundy[state] == 0`인지로 losing/winning을 판단한다.\n6. 독립 subgame이 여러 개이면 각 Grundy 값을 xor해 전체 상태를 판단한다.\n\n이 절차에서 가장 많이 틀리는 지점은 1번과 6번이다. 상태가 충분하지 않으면 같은 모양처럼 보이는 두 위치가 사실 다른 move 집합을 가질 수 있다. 또 여러 component가 있어도 한 move가 두 component를 동시에 바꾸거나 공통 자원을 소모하면 disjoint sum이 아니므로 xor 합성이 성립하지 않는다.\n\n## 독립 subgame 체크리스트\n\nxor 합성을 쓰려면 component들이 정말 독립이어야 한다.\n\n- 한 턴에 정확히 하나의 component만 선택해서 움직이는가?\n- 어떤 component를 움직여도 다른 component의 move 집합이 변하지 않는가?\n- component 사이에 공유 budget, 공유 token, 전역 쿨다운 같은 제약이 없는가?\n- 종료 조건이 component별 종료의 단순 합인가?\n- 두 플레이어가 같은 상태에서 같은 move 후보를 보는가?\n\n예를 들어 여러 pile에서 돌을 가져가는 표준 Nim은 각 pile이 독립이다. 반대로 \"전체 게임에서 딱 K번만 큰 move를 쓸 수 있다\" 같은 전역 제약이 있으면 pile별 Grundy만 계산해서 xor할 수 없다. 그때는 전역 제약을 상태에 넣은 DP나 minimax를 다시 봐야 한다.\n\n## 구현 메모\n\n상태 그래프가 DAG이면 작은 상태부터 bottom-up으로 계산할 수 있다. 자연스러운 크기 순서가 없으면 DFS + memoization으로 `grundy(state)`를 계산한다. 이때 cycle이 있거나 draw가 가능한 게임이면 단순 Sprague-Grundy 모델 밖일 수 있으므로 방문 중 상태를 만났다고 임의로 0을 주면 안 된다.\n\n`mex`는 보통 next Grundy 값이 많지 않으므로 boolean 배열이나 set으로 충분하다. pile 크기 N에서 move가 1~3개 제거라면 다음 값은 `grundy[n-1]`, `grundy[n-2]`, `grundy[n-3]`뿐이다. 반대로 상태 수가 큰 그래프 게임에서는 reachable state 수와 move 수가 병목이 되므로 memo key 설계가 더 중요하다.\n\n답안을 작성할 때는 \"Nim은 pile 크기를 xor한다\"에서 멈추지 않는다. 표준 Nim에서는 pile 하나의 Grundy 값이 pile 크기와 같기 때문에 그렇게 보이는 것이고, 변형 게임에서는 `mex(next)`로 다시 계산해야 한다고 말해야 한다. 이 한 문장이 암기 답변과 모델 이해를 가른다.\n\n## 흔한 오해\n\n모든 게임 이론 문제가 Grundy로 가는 것은 아니다.\n\n- 점수 차를 최대화해야 하면 승패 DP보다 minimax나 interval DP가 더 자연스럽다.\n- 양쪽 플레이어의 가능한 행동이 서로 다르면 impartial game이 아니다.\n- 우연, 숨겨진 정보, 동시 행동, draw가 있으면 Grundy xor만으로 승패를 말하기 어렵다.\n- misere play처럼 마지막 행동을 한 사람이 지는 변형은 base case와 Nim 전략이 달라진다.\n- subgame 사이에 자원이 공유되거나 한 move가 여러 component를 동시에 바꾸면 xor 합성 전제가 깨진다.\n\n또한 Grundy 값은 \"이길 확률\"이나 \"최소 move 수\"가 아니다. 완벽 플레이를 가정한 normal-play impartial game에서 승패 구조를 Nim heap 크기로 표현하는 값이다.\n\n## 드릴 축\n\n답변은 \"게임 조건 확인\", \"상태\", \"가능한 move\", \"terminal state\", \"winning/losing 전이\", \"Grundy=mex\", \"독립 subgame이면 xor\" 순서로 구성한다. 작은 pile 예시로 `(1, 1)`의 xor가 0이라 불리하고, `(1, 2)`의 xor가 0이 아니어서 유리하다는 식으로 손계산하면 직관이 잡힌다.\n\n면접에서는 다음 문장까지 붙이면 과장이 줄어든다.\n\n> 이 설명은 normal-play impartial game이고 subgame들이 독립이라는 전제에서 맞습니다. 그 전제가 깨지면 minimax, expectimax, 점수 DP 같은 다른 모델을 먼저 봐야 합니다.\n\n## Safe Next Docs\n\n승패 상태 정의가 약하면 `dp-state-definition-drill`, 방문 집합과 게임 상태가 섞이면 `bitmask-dp-tsp-state-chooser`로 이어진다. 두 플레이어의 행동이 다르거나 점수 평가가 핵심이면 `minimax-alpha-beta-pruning-interview-chooser`로 넘어간다.\n",
  "aliases": [
    "nim xor winning state",
    "sprague grundy theorem",
    "impartial game xor strategy",
    "normal play game dp",
    "grundy mex calculation",
    "님 게임 그런디 수",
    "공평 게임 승패 DP",
    "독립 부분게임 xor 합성"
  ],
  "expected_queries": [
    "Nim 게임에서 xor가 왜 나오지?",
    "Grundy number를 면접에서 어떻게 설명해?",
    "승패가 번갈아 바뀌는 게임 DP는 어떻게 생각해?",
    "공평 게임에서 Grundy를 언제 쓰면 안 돼?",
    "독립된 부분 게임이 여러 개일 때 그런디 수를 어떻게 합쳐?"
  ],
  "symptoms": [
    "두 사람이 번갈아 하는 문제를 minimax로만 풀려 합니다",
    "현재 상태가 winning인지 losing인지 정의하지 못합니다",
    "여러 독립 게임을 합칠 때 xor를 쓰는 이유를 모릅니다"
  ],
  "relations": {
    "prerequisites": [
      "algorithm/dp-state-definition-mission-primer"
    ],
    "next_docs": [
      "algorithm/dp-state-definition-drill",
      "algorithm/bitmask-dp-tsp-state-chooser",
      "algorithm/minimax-alpha-beta-pruning-interview-chooser"
    ],
    "confusable_with": [
      "algorithm/dp-state-definition-drill",
      "algorithm/bitmask-dp-tsp-state-chooser",
      "algorithm/minimax-alpha-beta-pruning-interview-chooser"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-06-18",
    "legacy_path": "knowledge/cs/contents/algorithm/nim-grundy-game-theory-primer.md",
    "revision_history": [
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch146"
      }
    ]
  }
}
