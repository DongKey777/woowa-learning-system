{
  "id": "algorithm/trie-string-matching-mission-chooser",
  "title": "Trie String Matching Mission Chooser",
  "category": "algorithm",
  "level": "intermediate",
  "summary": "이 chooser는 Trie, KMP/Z, Aho-Corasick을 문자열 검색 문제 신호에 맞춰 고른다. prefix search, startsWith 전체 순회, 다중 패턴 검색, 메모리 증가 같은 질문을 다룬다.",
  "body_markdown": "# Trie String Matching Mission Chooser\n\n## 10초 chooser\n\n| 문제 신호 | 후보 | 이유 |\n|---|---|---|\n| prefix로 단어를 찾음, 자동완성 | Trie | 문자열 앞부분을 공유한다 |\n| 정확한 단어 존재 여부가 많음 | HashSet 또는 Trie | prefix 질의가 있으면 Trie가 유리하다 |\n| 긴 text에서 한 pattern이 substring인지 검색 | KMP/Z | prefix가 아니라 substring matching이다 |\n| 긴 text에서 여러 pattern을 동시에 검색 | Aho-Corasick | Trie + failure link로 text를 한 번 훑는다 |\n| suffix/임의 substring 질의가 많음 | suffix automaton/array/tree 후보 | 일반 Trie는 시작 위치가 고정된다 |\n\n## 문제 선택 기준\n\nTrie는 \"저장된 단어들의 prefix\"를 빠르게 따라가는 자료구조다. 질의가 `startsWith(prefix)`, 자동완성, 전화번호 목록 prefix 충돌처럼 단어의 앞에서 시작하면 Trie가 잘 맞는다.\n\n문제가 \"text 안에 pattern이 어디든 포함되는가\"라면 prefix 질의가 아니다. 단일 pattern이면 KMP/Z가 작고 빠르다. pattern이 수천 개이고 text를 한 번만 훑고 싶으면 Aho-Corasick을 본다.\n\n미션에서 이 선택은 보통 성능 개선 리뷰로 나타난다. 모든 메뉴 이름을 리스트로 들고 매 요청마다 `name.startsWith(input)`을 전체 순회한다면 데이터가 작을 때는 괜찮지만, 자동완성처럼 같은 prefix 질의가 반복되면 Trie가 후보가 된다. 반대로 \"금칙어가 문장 어디에 포함되는지\" 찾는 문제를 Trie 하나로 끝내려 하면 시작 위치가 고정되어 실패한다.\n\n## 핵심 invariant\n\nTrie invariant는 \"root에서 어떤 node까지의 경로 문자열이 하나의 prefix\"라는 것이다. 단어 끝을 구분하려면 terminal flag나 count가 필요하다. terminal이 없으면 `app`과 `apple`을 구분하지 못한다.\n\nSubstring matching invariant는 다르다. text의 임의 위치에서 pattern이 시작될 수 있으므로, 실패했을 때 얼마나 되돌아갈지 정하는 failure function이나 automaton이 필요하다. Aho-Corasick은 Trie 위에 failure link를 얹어 여러 pattern의 prefix/suffix 겹침을 재사용한다.\n\n## worked examples\n\n### 자동완성\n\n저장 단어가 `app`, `apple`, `apply`, `banana`이고 질의가 `app`으로 시작하는 후보를 찾는다면 Trie가 자연스럽다. root에서 `a -> p -> p`를 따라간 뒤 그 아래 terminal 단어를 모으면 된다. 결과 수가 많으면 `topK`, frequency, 정렬 기준을 node에 보관할지 별도로 계산할지 정한다.\n\n### 단일 패턴 검색\n\n긴 로그 문자열에서 `ERROR42`가 한 번이라도 등장하는지 찾는다면 Trie보다 KMP/Z가 맞다. 질의는 저장 단어의 prefix가 아니라 text의 임의 위치에서 시작한다. Java의 `contains`로 충분한 크기라면 직접 KMP를 구현하지 않아도 되지만, 알고리즘 문제에서는 `O(n+m)` 근거를 설명해야 한다.\n\n### 다중 패턴 검색\n\n금지어 10,000개를 긴 채팅 메시지에서 찾는다면 각 패턴마다 KMP를 돌리는 것보다 Aho-Corasick이 유리하다. 여러 pattern을 Trie에 넣고 failure link를 만들면 text를 왼쪽에서 오른쪽으로 한 번 훑으면서 매칭을 찾는다. 이때도 기본 구조는 Trie지만, 일반 prefix Trie와 달리 실패했을 때 이동하는 링크가 핵심이다.\n\n## Wrong-bucket neighbor\n\n`startsWith`를 전체 리스트에 반복하면 입력 크기가 커질수록 느려진다. 하지만 데이터가 작고 구현 단순성이 중요하면 Trie가 과할 수도 있다. 미션 과제에서 단어가 20개뿐이고 요청도 드물다면 리스트 순회가 더 읽기 쉬운 선택일 수 있다. 성능 요구가 명확하지 않은데 자료구조부터 복잡하게 만들면 리뷰에서는 \"왜 필요한가\"를 설명해야 한다.\n\n반대로 \"포함 문자열 검색\"을 일반 Trie만으로 풀려고 하면 놓치는 위치가 생긴다. 모든 suffix를 Trie에 넣는 방법은 가능하지만 메모리가 크게 늘어 실전 기본 선택은 아니다. suffix 구조는 강력하지만 구현 난도가 높으므로, 문제 요구가 suffix/substring 질의를 반복하는지 먼저 확인한다.\n\n## 구현 선택: child 배열과 map\n\n알파벳이 `a-z`로 고정되고 데이터가 많다면 `Node[26]` 배열 child가 빠르다. 하지만 한글, 대소문자, 숫자, 특수문자가 섞이면 배열 크기와 정규화 정책이 문제가 된다. 이때는 `Map<Character, Node>`가 메모리를 아낄 수 있다. 대신 map lookup 비용과 객체 수가 늘어난다.\n\n자동완성에서 대소문자, 공백, 초성 검색, Unicode normalization이 요구되면 Trie 이전에 \"문자열을 어떤 canonical form으로 저장할지\"를 정해야 한다. `Cafe`, `café`, `카페`가 같은 후보인지 다른 후보인지 결정하지 않으면 자료구조는 맞아도 검색 품질이 흔들린다.\n\n## 반례로 확인\n\nprefix vs substring 반례: Trie에 단어 `abc`를 넣었다고 하자. root에서 `bc`를 따라가면 실패한다. `bc`는 `abc`의 substring이지만 prefix가 아니기 때문이다. `contains(\"bc\")` 질의가 필요하면 KMP/Z, suffix 구조, 또는 Aho-Corasick 계열을 봐야 한다.\n\nterminal 반례: `app`과 `apple`을 모두 저장하는데 terminal flag가 없으면 `app` 검색이 단어 존재인지 단순 prefix 존재인지 구분되지 않는다.\n\n삭제 반례: `app`과 `apple`을 넣고 `app`을 삭제할 때 경로 node를 무조건 지우면 `apple`도 망가진다. 삭제가 필요하면 pass count와 end count를 두고, 공유 prefix를 보존해야 한다.\n\n## 복잡도\n\nTrie build는 전체 문자 수를 `L`이라 할 때 `O(L)` 시간과 `O(L * childCost)` 메모리다. 배열 child는 alphabet이 작을 때 빠르지만 메모리를 많이 쓰고, map child는 sparse한 문자 집합에서 메모리를 아낀다. Prefix query는 `O(len(prefix) + results)`다.\n\nKMP/Z는 pattern 길이 `m`, text 길이 `n`에 대해 `O(n+m)`이다. Aho-Corasick은 pattern 전체 길이와 text 길이, match 수에 선형이다.\n\n## 흔한 실수\n\n- prefix search와 substring search를 구분하지 않는다.\n- terminal flag 없이 prefix 존재와 단어 존재를 섞는다.\n- alphabet 크기를 고려하지 않고 고정 배열 child를 써 메모리를 크게 늘린다.\n- 삭제나 빈도 질의가 필요한데 pass count/end count를 저장하지 않는다.\n- Unicode/대소문자/정규화 정책을 정하지 않아 같은 단어가 다른 경로로 저장된다.\n- 결과를 모두 DFS로 모으면서 `top 10` 요구에도 매번 전체 subtree를 훑는다.\n\n## 최소 drill\n\n1. `startsWith(\"pre\")` 자동완성과 `text.contains(\"pre\")` 검색 중 Trie가 직접 맞는 쪽을 고른다.\n2. `[\"app\", \"apple\"]`을 저장할 때 각 node에 어떤 flag/count가 필요한지 적는다.\n3. pattern 10,000개를 긴 로그 text 하나에서 찾는다면 Trie, KMP, Aho-Corasick 중 무엇을 고를지 설명한다.\n4. 입력 단어가 한글과 영어를 모두 포함한다면 child 자료구조와 정규화 정책을 어떻게 잡을지 말한다.\n\n## Safe Next Docs\n\nautocomplete는 `data-structure/trie-prefix-search-autocomplete`로, 단일 패턴 매칭은 `algorithm/kmp-vs-z-algorithm`으로 내려간다. 여러 패턴 동시 검색은 Aho-Corasick을 별도 주제로 잡는다.\n",
  "aliases": [
    "Trie 언제 쓰는지",
    "prefix 검색 자료구조",
    "KMP Trie 선택 기준",
    "Aho Corasick 다중 패턴",
    "자동완성 알고리즘 고르기",
    "문자열 포함 검색 구분",
    "startsWith 성능 개선",
    "패턴 매칭 선택법"
  ],
  "expected_queries": [
    "자동완성 기능이면 Trie를 써야 하는지 어떻게 판단해?",
    "문자열에 특정 패턴이 포함되는 문제와 prefix 검색은 뭐가 달라?",
    "KMP Trie Aho-Corasick 중 어떤 알고리즘을 골라야 해?",
    "여러 금지어를 긴 문장에서 한 번에 찾으려면 어떤 구조가 맞아?",
    "Trie를 썼는데 메모리가 커질 때 child 구조를 어떻게 골라?"
  ],
  "symptoms": [
    "모든 문자열을 매번 startsWith로 훑고 있어요",
    "단일 패턴과 다중 패턴 검색을 구분하지 못해요",
    "Trie를 만들었는데 메모리가 너무 커요"
  ],
  "relations": {
    "prerequisites": [
      "algorithm/string"
    ],
    "next_docs": [
      "algorithm/kmp-vs-z-algorithm"
    ],
    "confusable_with": [
      "algorithm/kmp-vs-z-algorithm",
      "data-structure/trie-prefix-search-autocomplete"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-05-24",
    "legacy_path": "knowledge/cs/contents/algorithm/trie-string-matching-mission-chooser.md",
    "revision_history": [
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch145"
      }
    ]
  }
}
