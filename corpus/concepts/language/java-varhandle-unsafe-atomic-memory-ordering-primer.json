{
  "id": "language/java-varhandle-unsafe-atomic-memory-ordering-primer",
  "title": "Java VarHandle Unsafe Atomic Memory Ordering Primer",
  "category": "language",
  "level": "advanced",
  "summary": "Java VarHandle, Unsafe, AtomicFieldUpdater, memory ordering을 JMM happens-before와 성능 기준으로 해석하는 language primer",
  "body_markdown": "# Java VarHandle Unsafe Atomic Memory Ordering Primer\n\n## 이 문서가 맡는 질문\nJava VarHandle, Unsafe, AtomicFieldUpdater, memory ordering을 JMM happens-before와 성능 기준으로 해석하는 language primer. 이 문서는 이름 정의가 아니라 실제 설계/디버깅 상황에서 어떤 불변식과 측정값으로 판단할지 연결한다.\n\n## 빠른 분기\n일반 volatile/Atomic으로 충분한지 먼저 본다. acquire/release/opaque/plain 접근이 필요한 이유가 있는지 확인한다. CAS loop는 ABA, retry churn, false sharing을 같이 본다.\n\n## 실전 시나리오\nlock-free counter를 만들려고 VarHandle을 썼지만 rare stale read와 CPU spike가 생긴다. volatile, acquire/release, compareAndSet의 의미가 섞여 있다.\n\n## CS 배경\nJava Memory Model은 happens-before로 visibility와 ordering을 설명한다. VarHandle은 plain/opaque/acquire/release/volatile 접근 모드를 노출해 더 낮은 수준의 memory ordering 선택을 가능하게 한다.\n\n## 핵심 불변식\n- visibility와 atomicity는 같은 말이 아니다\n- acquire/release는 짝이 맞아야 의미가 있다\n- CAS loop는 실패 경로와 backoff까지 설계해야 한다\n- Unsafe/VarHandle 사용은 API 표면을 최소화해야 한다\n\n## 판단 매트릭스\n- 단순 공유 상태면 volatile/Atomic 우선\n- 필드 업데이트가 필요하면 AtomicFieldUpdater/VarHandle 비교\n- ordering 비용을 줄여야 하면 acquire/release 검토\n- 자료구조 구현이 아니라면 Unsafe 직접 사용은 피한다\n\n## 예시\n```text\nVarHandle getAcquire + setRelease for publication; compareAndSet loop with retry budget\n```\n이 예시는 API 이름을 외우는 용도가 아니라 boundary와 invariant를 한 번에 드러내기 위한 것이다.\n\n## 깊게 볼 포인트\n낮은 수준 memory ordering은 빠른 코드가 아니라 더 많은 증명 책임을 뜻한다. JMM 용어로 invariant를 설명할 수 없으면 도입하면 안 된다.\n\n## 품질 관점\n품질은 Java concurrency의 happy path보다 boundary condition과 잘못된 후보 배제를 얼마나 재현 가능하게 설명하는지로 본다. 테스트는 publication, stale read, CAS contention, ABA-like update를 포함해야 하며, 실패 시 어떤 invariant가 깨졌는지 바로 드러나야 한다.\n\n## 성능 관점\n성능은 평균 처리량 하나로 보지 않는다. CAS failure rate, fence cost, false sharing, allocation-free update를 분리해서 측정하고, 작은 synthetic case와 실제 traffic shape를 나눠 benchmark한다.\n\n## 레이턴시 관점\n레이턴시는 p50보다 p95/p99와 cold path를 본다. spin retry tail, cache line bouncing, safepoint interaction, contention burst가 tail latency를 키우는지 trace span과 histogram으로 확인한다.\n\n## 주제별 추가 지표\ncas_failure_rate, retry_loop_p99, fence_cost_ns, stale_read_repro_count, false_sharing_signal를 주제별 지표로 본다. 이 지표들은 품질, 성능, 레이턴시를 한 화면에서 비교하기 위한 최소 세트다.\n\n## 자주 하는 오해\n- volatile을 atomic increment로 착각한다\n- acquire/release를 아무 곳에나 섞어도 안전하다고 생각한다\n- CAS가 lock보다 항상 빠르다고 본다\n\n## 학습자가 답변에 포함해야 할 구조\n답변은 문제 신호, 핵심 불변식, 선택지 비교, 실패 시 관측 지표, 레이턴시 예산을 차례로 말해야 한다. 마지막에는 어떤 테스트와 대시보드로 검증할지 제시한다.\n\n## 실전 검증 루프\n1. 증상을 한 문장으로 고정한다\n2. 관련 boundary와 ownership을 표시한다\n3. correctness invariant를 먼저 쓴다\n4. synthetic case와 production-like case를 나눠 측정한다\n5. 품질, 성능, 레이턴시 지표를 함께 비교한다\n6. 결과를 다음 학습 문서와 qrels에 반영한다\n\n## 한 줄 정리\nVarHandle은 성능 도구이기 전에 JMM ordering을 코드에 직접 쓰는 책임 있는 저수준 API다.\n\n## ordering 선택을 답변하는 순서\n\n먼저 공유 상태의 요구를 visibility, ordering, atomicity로 나눈다. `volatile` read/write는 visibility와 happens-before edge를 만들 수 있지만 `count++` 같은 read-modify-write 전체를 원자적으로 만들지는 않는다. 원자적 갱신이 필요하면 `Atomic*`, `AtomicFieldUpdater`, VarHandle CAS 계열을 검토하고, 단순 publication이면 acquire/release가 충분한지 본다.\n\nVarHandle의 plain/opaque/acquire/release/volatile 모드는 성능 knob처럼 보이지만 실제로는 증명 책임의 차이다. release write는 그 이전 write를 이후 acquire read가 볼 수 있게 publication 경계를 만들고, acquire read는 그 이후 연산이 앞으로 재정렬되지 않도록 한다. 둘 중 하나만 쓰거나 unrelated field에 섞으면 기대한 happens-before가 생기지 않을 수 있다.\n\n## CAS loop와 운영 위험\n\nCAS loop는 lock-free라는 이름 때문에 항상 빠르다고 오해하기 쉽다. contention이 높으면 CAS 실패가 반복되어 CPU를 태우고 p99를 올릴 수 있다. backoff, retry budget, fallback lock, striped counter, LongAdder 같은 대안을 함께 비교해야 한다. 또한 ABA 문제, false sharing, cache line bouncing은 correctness와 latency를 동시에 건드린다.\n\nUnsafe나 VarHandle은 API 표면을 좁혀야 한다. 일반 application code 곳곳에 흩어지면 JMM reasoning이 불가능해진다. 작은 concurrency primitive 안에 가두고, publication test, stress test, jcstress 같은 litmus test, contention benchmark를 함께 둬야 한다. 면접 답변에서도 \"낮은 수준 API를 쓸 때는 더 빠른지보다 어떤 happens-before를 만들고 어떤 실패를 테스트할지 먼저 말한다\"고 정리하는 편이 안전하다.\n\n## 헷갈리는 비교\n\n- `volatile int count`는 최신 값을 보게 도와도 increment lost update를 막지 않는다.\n- `lazySet`/release 계열은 즉시 전역 순서를 강제하는 volatile write와 비용/의미가 다를 수 있다.\n- opaque 접근은 순서 보장이 더 약하므로 algorithm invariant가 이를 허용할 때만 쓴다.\n- CAS 성공 경로만 보면 안 되고 실패율, retry p99, backoff, fairness를 같이 본다.\n\n## 테스트와 코드 리뷰 기준\n\n이 수준의 코드는 단위 테스트 몇 개로 안전하다고 말하기 어렵다. stale read나 reordering은 낮은 확률로만 드러날 수 있으므로, 반복 stress test와 작은 litmus test를 별도로 둔다. 값 publish 전후에 어떤 write가 보장되어야 하는지, reader가 어떤 read 이후에 그 값을 신뢰할 수 있는지, CAS 실패 후 재시도에서 같은 객체 상태를 다시 읽는지까지 코드 리뷰 체크리스트로 만든다.\n\n실무에서는 대개 library primitive를 먼저 선택한다. `ConcurrentHashMap`, `LongAdder`, `AtomicReference`, `StampedLock` 같은 상위 도구로 invariant를 표현할 수 있으면 VarHandle로 직접 내려가지 않는다. 직접 내려가는 경우는 allocation 제거, 필드 updater 비용, off-heap/array element ordering처럼 명확한 이유가 있을 때다. 이때도 성능 이득은 JMH 같은 micro benchmark와 production-like contention shape를 나눠 확인해야 한다.\n",
  "aliases": [
    "Java VarHandle JMM ordering",
    "acquire release volatile 차이",
    "opaque plain access mode",
    "CAS loop stale read",
    "AtomicFieldUpdater VarHandle 선택",
    "Unsafe 대신 VarHandle",
    "happens-before publication",
    "false sharing CAS retry",
    "memory ordering 면접",
    "lock-free visibility atomicity"
  ],
  "expected_queries": [
    "Java VarHandle의 acquire/release와 volatile 접근은 어떻게 골라야 해?",
    "CAS loop가 느리거나 stale read가 보이면 무엇을 봐?",
    "Unsafe 대신 VarHandle을 쓸 때 어떤 invariant를 증명해야 해?",
    "Java VarHandle에서 acquire/release와 volatile 접근은 어떻게 구분해?",
    "volatile은 visibility만 주고 atomic increment는 보장하지 않는다는 걸 JMM happens-before로 설명해줘",
    "getAcquire setRelease publication에서 release와 acquire 짝이 맞지 않으면 어떤 stale read가 생겨?"
  ],
  "symptoms": [
    "varhandle stale read",
    "CAS loop CPU spike",
    "memory ordering confusion"
  ],
  "relations": {
    "prerequisites": [
      "language/memory-barriers-varhandle-fences",
      "language/varhandle-unsafe-atomics",
      "language/java-volatile-atomic-synchronized-jmm-perspective-map"
    ],
    "next_docs": [
      "language/java-memory-model-happens-before-volatile-final"
    ],
    "confusable_with": [
      "language/volatile-synchronized-atomic-visibility-chooser"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [
    {
      "pattern": "Java VarHandle의 acquire/release와 volatile 접근은 어떻게 골라야 해?",
      "match_count": 1,
      "added_at": "2026-05-27"
    },
    {
      "pattern": "CAS loop가 느리거나 stale read가 보이면 무엇을 봐?",
      "match_count": 1,
      "added_at": "2026-05-27"
    },
    {
      "pattern": "Unsafe 대신 VarHandle을 쓸 때 어떤 invariant를 증명해야 해?",
      "match_count": 1,
      "added_at": "2026-05-27"
    }
  ],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-27",
    "last_modified": "2026-06-18",
    "revision_history": [
      {
        "date": "2026-05-27",
        "source": "y14-batch6"
      },
      {
        "date": "2026-05-28",
        "source": "y14-qrels-anchor-sync"
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch143"
      }
    ]
  }
}
