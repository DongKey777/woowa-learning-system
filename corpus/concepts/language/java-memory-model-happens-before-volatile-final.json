{
  "id": "language/java-memory-model-happens-before-volatile-final",
  "title": "Java Memory Model, Happens-Before, volatile, final",
  "category": "language",
  "level": "intermediate",
  "summary": "이 문서는 Java Memory Model에서 happens-before가 가시성과 관찰 순서를 어떻게 정의하는지, volatile이 visibility와 ordering에는 관여하지만 compound atomicity는 보장하지 않는 이유, final field가 safe publication에 왜 중요한지 설명하는 intermediate primer다.",
  "body_markdown": "# Java Memory Model, Happens-Before, `volatile`, `final`\n\n> 한 줄 요약: `happens-before`는 JMM이 허용하는 관찰 순서를 제한하는 핵심 규칙이고, `volatile`은 같은 변수의 write/read 사이에 가시성/순서 edge를 만들지만 복합 연산을 원자화하지 않으며, `final`은 올바른 생성 패턴에서 final 필드 초기화 값을 안전하게 보이게 한다.\n\n**난이도: 🟡 Intermediate**\n\n관련 문서:\n\n- [카테고리 README](./README.md)\n- [우아코스 백엔드 CS 로드맵](../../JUNIOR-BACKEND-ROADMAP.md)\n- [연결 입문 문서](../data-structure/backend-data-structure-starter-pack.md)\n\n기준 명세:\n\n- [JLS 17: Threads and Locks](https://docs.oracle.com/javase/specs/jls/se17/html/jls-17.html)\n\n\nretrieval-anchor-keywords: java memory model happens before volatile final basics, java memory model happens before volatile final beginner, java memory model happens before volatile final intro, java basics, beginner java, 처음 배우는데 java memory model happens before volatile final, java memory model happens before volatile final 입문, java memory model happens before volatile final 기초, what is java memory model happens before volatile final, how to java memory model happens before volatile final, volatile vs synchronized 차이, volatile count++ 깨지는 이유, happens-before 의미, final 필드 safe publication, memory barrier reordering 영향, atomicity vs visibility vs ordering, JMM 처음 배우는데, AtomicInteger vs volatile, data race 발생 조건\n> retrieval-anchor-keywords: happens-before, JMM, visibility, ordering, safe publication, volatile semantics, final field semantics, memory barrier, reordering, publication safety, data race, cache coherence\n\n## 핵심 개념\n\n자바 동시성에서 자주 헷갈리는 지점은 세 가지다.\n\n- `값을 썼다`\n- `다른 스레드가 그 값을 본다`\n- `그 값이 어떤 순서로 보인다`\n\n이 셋은 같지 않다.\nJava Memory Model(JMM)은 바로 이 차이를 정의한다.\n\n### happens-before는 무엇인가\n\n`happens-before`는 두 동작 사이의 **가시성 + 순서 보장 관계**다.\nJLS 표현으로는 한 action이 다른 action보다 happens-before이면 앞 action은 뒤 action에 visible하고 ordered-before다.\n다만 이것은 \"벽시계상 먼저 실행됐다\"가 아니라, 해당 edge를 공유하는 코드에서 어떤 write를 관찰할 수 있는지 제한하는 규칙이다.\n\n중요한 점은 \"실제로 CPU가 먼저 실행했다\"와 \"다른 스레드가 그렇게 관찰한다\"는 다르다는 것이다.\nJMM은 관찰 가능한 순서를 규정한다.\n\n### `volatile`은 무엇을 보장하는가\n\n`volatile`은 단순한 \"무조건 최신값 읽기\"가 아니다.\nvolatile read/write는 synchronization action이고, 모든 synchronization action에는 전역 synchronization order가 있다.\nJLS는 같은 volatile 변수에 대한 write가 synchronization order상 이후 read와 synchronizes-with 관계를 만들고, 그 관계가 happens-before edge가 된다고 정의한다.\n\n- 같은 volatile 변수의 write/read 사이에 가시성 edge를 만든다\n- volatile write 이전의 일반 write가 reader의 후속 action에 보이도록 transitive edge를 만들 수 있다\n- 상태 플래그, 종료 신호, one-writer-many-reader 신호 패턴에 적합하다\n\n하지만 `volatile`은 복합 연산을 원자적으로 만들지 않는다.\n즉 `count++`는 여전히 깨질 수 있다.\n\n### `final`은 왜 중요한가\n\n`final` 필드는 생성자에서 값을 세팅하고, 생성자가 끝나기 전에 `this`나 그 객체 참조를 다른 스레드가 볼 수 있게 만들지 않았을 때 특별한 초기화 보장을 받는다.\n그 조건을 지키면 다른 스레드가 객체를 볼 때 final 필드의 올바른 초기화 값을 보게 된다.\n즉 `final`은 \"불변 객체를 만들 때 좋다\" 수준이 아니라, 올바른 생성 패턴에서 **final 필드 초기화 가시성**을 보강한다.\n\n다만 `final`은 깊은 불변성을 자동으로 보장하지 않는다.\n\n- `final List<String> names`는 참조가 바뀌지 않는다는 뜻이지\n- 리스트 내부 원소가 바뀌지 않는다는 뜻은 아니다\n- 같은 객체의 non-final 필드까지 자동으로 안전하게 보인다는 뜻도 아니다\n\n## 깊이 들어가기\n\n### 1. 왜 재정렬이 문제인가\n\n컴파일러와 CPU는 성능을 위해 명령을 재정렬할 수 있다.\nJMM은 단일 스레드 의미를 깨지 않는 한 다양한 변환을 허용하므로, synchronization edge가 없으면 코드상으로는\n\n1. 데이터를 쓰고\n2. 준비 완료 플래그를 세운다\n\n처럼 보이더라도, 다른 스레드에서는 플래그를 보고도 데이터를 늦게 보거나 초기값을 볼 수 있다.\n\n이 차이가 바로 JMM을 공부해야 하는 이유다.\n`volatile`, lock, thread start/join 같은 synchronization edge는 이런 관찰 순서를 제어하는 도구다.\n\n### 2. safe publication이 왜 필요한가\n\n객체를 생성한 뒤 다른 스레드가 그 객체를 볼 때, reference 전달과 초기화 사이에 happens-before edge가 없으면 문제가 생긴다.\n대표적인 경우는:\n\n- 생성 중인 객체를 `this` escape 시키는 경우\n- 초기화가 끝나기 전에 공유 저장소에 reference를 넣는 경우\n- reference는 보이지만 non-final 필드 초기화는 보장되지 않는 경우\n\n`final` 필드는 이 중 final 필드의 초기화 가시성 위험을 줄인다.\n특히 생성자 안에서 값을 세팅하고 생성자 완료 전 escape가 없으면, JLS final field semantics에 따라 final 필드는 올바르게 관찰된다.\n하지만 객체 reference 자체의 publish, non-final 필드, 이후 mutable state 변경에는 여전히 별도 synchronization이 필요하다.\n\n### 3. `volatile`과 `synchronized`의 관계\n\n둘 다 가시성에 관여하지만 성격이 다르다.\n\n- `volatile`은 가벼운 신호 전달에 좋다\n- `synchronized`는 상호배제와 가시성을 함께 제공한다\n\n즉 `volatile`은 \"상태 플래그\"에 가깝고, `synchronized`는 \"임계영역\"에 가깝다.\n\n### 4. JMM과 OS 캐시 계층은 어떻게 이어지는가\n\nJMM은 언어 규칙이고, 실제 원인은 하드웨어 계층과 연결된다.\n\n- CPU 캐시가 코어마다 다르게 보일 수 있다\n- 캐시 coherence는 최신성의 문제다\n- memory barrier는 순서의 문제다\n\n이 관계를 이해하면, `volatile`이 단순 문법이 아니라 **캐시와 배리어를 통한 관찰 순서 제어**라는 점이 보인다.\n\n관련해서 [CPU Cache, Coherence, Memory Barrier](../operating-system/cpu-cache-coherence-memory-barrier.md)와 [false sharing](../operating-system/false-sharing-cache-line.md)을 같이 보면 좋다.\n\n### 5. 실전에서 자주 헷갈리는 경계\n\n- `volatile`은 같은 volatile 변수의 write/read에 order/visibility edge를 주지만, 원자적 증가를 보장하지 않는다\n- `final`은 참조 고정과 final 필드 초기화 가시성에 도움이 되지만, 객체 그래프 전체를 불변으로 바꾸거나 non-final 필드를 보장하지는 않는다\n- `happens-before`는 \"우연히 잘 되는 것\"이 아니라, 관찰 가능한 순서를 설계하는 기준이다\n\n## 실전 시나리오\n\n### 시나리오 1: 종료 신호를 전달하고 싶다\n\n백그라운드 스레드를 멈추는 가장 흔한 방법은 `volatile boolean stop`이다.\n\n- 한 스레드가 `stop = true`를 쓴다\n- 다른 스레드가 루프에서 그 값을 읽는다\n\n이때 `volatile`이 없으면 루프가 종료 신호를 끝까지 못 볼 수 있다.\n그래서 단순 플래그에는 `volatile`이 자주 맞는다.\n\n### 시나리오 2: 설정 객체를 여러 스레드에 안전하게 넘기고 싶다\n\n설정 값이 생성 후 바뀌지 않는다면 `final` 필드로 묶는 게 좋다.\n\n- 생성 시점에 값을 모두 세팅\n- 생성자가 끝나기 전에는 `this`나 객체 참조를 노출하지 않음\n- 생성이 끝난 뒤 외부로 노출하고, 이후 mutable state가 없도록 설계\n\n이런 패턴은 안전한 게시와 잘 맞는다.\n`final`은 \"한 번 정해지면 바뀌지 않는다\"는 의도를 코드에 드러내면서 final 필드 초기화 값을 안전하게 관찰하게 하는 수단이다.\n\n### 시나리오 3: `count++`가 왜 틀리는지 설명해야 한다\n\n`count++`는 읽기 -> 수정 -> 쓰기라서 원자적이지 않다.\n즉 `volatile`을 붙여도 lost update는 여전히 생길 수 있다.\n\n이런 경우는 `AtomicInteger`, `synchronized`, `VarHandle` 중 하나를 선택해야 한다.\n관련 문서는 [VarHandle, Unsafe, Atomics](./java/varhandle-unsafe-atomics.md)를 참고하면 좋다.\n\n### 시나리오 4: double-checked locking을 안전하게 쓰고 싶다\n\n싱글턴 지연 초기화는 JMM 이해가 없으면 위험하다.\n객체 생성이 끝나기 전에 참조가 보이면 부분 초기화 상태가 노출될 수 있기 때문이다.\n\n이때는 `volatile`과 올바른 초기화 패턴이 필요하다.\n동시성 검증은 [JCStress Concurrency Testing](./java/jcstress-concurrency-testing.md)로 확인하는 게 가장 설득력 있다.\n\n### 시나리오 5: 성능 문제를 동기화 탓으로만 보면 안 된다\n\n느린 이유가 항상 락은 아니다.\n\n- false sharing\n- cache line bouncing\n- 잘못된 publication\n- 불필요한 volatile 읽기 과다\n\n같은 원인도 많다.\n그래서 Java 코드만 보지 말고 OS 관점 문서도 함께 봐야 한다.\n\n## 코드로 보기\n\n### 1. `volatile`은 종료 신호에 적합하다\n\n```java\npublic class Worker {\n    private volatile boolean stop;\n\n    public void requestStop() {\n        stop = true;\n    }\n\n    public void run() {\n        while (!stop) {\n            doWork();\n        }\n    }\n\n    private void doWork() {\n        // 실제 작업\n    }\n}\n```\n\n이 코드는 \"상태 플래그\" 용도로는 좋다.\n하지만 `stop`이 아니라 `count`였다면 `volatile`만으로는 부족하다.\n\n### 2. `final` 필드는 안전한 게시의 기초다\n\n```java\npublic final class AppConfig {\n    private final String baseUrl;\n    private final int timeoutMillis;\n\n    public AppConfig(String baseUrl, int timeoutMillis) {\n        this.baseUrl = baseUrl;\n        this.timeoutMillis = timeoutMillis;\n    }\n\n    public String baseUrl() {\n        return baseUrl;\n    }\n\n    public int timeoutMillis() {\n        return timeoutMillis;\n    }\n}\n```\n\n생성 시점에 끝내고, 이후에는 읽기만 하는 객체에 잘 맞는다.\n이런 객체는 여러 스레드에 넘겨도 해석이 단순하다.\n\n### 3. `volatile`이 있어도 복합 연산은 깨진다\n\n```java\npublic class Counter {\n    private volatile int value;\n\n    public void increment() {\n        value++;\n    }\n\n    public int get() {\n        return value;\n    }\n}\n```\n\n이 코드는 `value`의 volatile read/write에 가시성/순서 규칙을 적용하지만, `increment()`는 안전하지 않다.\n증가 연산은 읽기와 쓰기가 분리되므로 경합에서 값을 잃을 수 있다.\n\n### 4. 이런 경우는 `AtomicInteger`나 `synchronized`를 쓴다\n\n```java\nimport java.util.concurrent.atomic.AtomicInteger;\n\npublic class SafeCounter {\n    private final AtomicInteger value = new AtomicInteger();\n\n    public int increment() {\n        return value.incrementAndGet();\n    }\n}\n```\n\n또는\n\n## 코드로 보기 (계속 2)\n\n```java\npublic class LockedCounter {\n    private int value;\n\n    public synchronized int increment() {\n        return ++value;\n    }\n}\n```\n\n둘 중 무엇이 맞는지는 경합 수준과 복합 상태 여부에 따라 달라진다.\n\n### 5. JCStress로 publication을 검증할 수 있다\n\n```java\n// 개념 예시:\n// - writer가 데이터를 쓰고 ready를 true로 바꾼다\n// - reader가 ready를 본 뒤 data를 읽는다\n// - forbidden outcome이 나오지 않아야 한다\n```\n\n실전에서는 [JCStress Concurrency Testing](./java/jcstress-concurrency-testing.md)처럼 가능한 결과를 열거해 검증한다.\n\n## 트레이드오프\n\n| 선택지 | 장점 | 단점 | 언제 선택하는가 |\n|---|---|---|---|\n| `volatile` | 단일 신호의 가시성/순서 edge를 만들기 쉽다 | 복합 연산 원자성은 없다 | 종료 신호, 상태 플래그 |\n| `final` | final 필드 초기화 가시성과 가독성이 좋다 | 생성 후 변경 불가, non-final/mutable state는 별도 보호 필요 | 불변 객체, 설정값 |\n| `synchronized` | 가시성 + 상호배제를 함께 준다 | 경합이 크면 비용이 있다 | 복합 상태 변경 |\n| `Atomic*` | 원자 연산을 쉽게 쓴다 | 복잡한 상태는 어렵다 | 단일 카운터, CAS |\n| `VarHandle` | 저수준 제어가 가능하다 | 학습 비용이 높다 | 라이브러리, 런타임 |\n\n판단 기준은 단순하다.\n\n- 단일 신호의 가시성/순서가 필요하면 `volatile`\n- 생성 후 바뀌지 않는 final 필드 초기화 보장이 중요하면 `final`\n- 여러 상태를 함께 묶어 바꿔야 하면 `synchronized`\n- 단일 값 경쟁만 풀면 `Atomic*`\n- JMM/배리어까지 제어해야 하면 `VarHandle`\n\n## 꼬리질문\n\n> Q: `volatile`만 붙이면 `count++`가 안전해지나요?\n> 의도: 가시성과 원자성을 구분하는지 확인\n> 핵심: `volatile`은 같은 변수의 write/read 사이에 가시성/순서 edge를 만들지만, 읽기-수정-쓰기 전체의 원자성을 보장하지 않는다.\n\n> Q: `final` 필드는 왜 안전한 게시와 연결되나요?\n> 의도: 생성 시점의 초기화 가시성을 이해하는지 확인\n> 핵심: 생성자에서 final 필드를 쓰고 생성자 완료 전 reference escape가 없으면, 다른 스레드는 final 필드의 올바른 초기화 값을 보장받는다. non-final 필드나 이후 변경까지 자동 보장되는 것은 아니다.\n\n> Q: happens-before는 그냥 \"먼저 실행\"과 같은 뜻인가요?\n> 의도: 실행 순서와 관찰 순서를 구분하는지 확인\n> 핵심: 실제 실행 순서가 아니라, 다른 스레드가 관찰해야 하는 순서를 규정한다.\n\n> Q: `synchronized`와 `volatile` 중 무엇을 먼저 떠올려야 하나요?\n> 의도: 문제 유형별 도구 선택을 보는 질문\n> 핵심: 단순 플래그는 `volatile`, 복합 상태는 `synchronized`를 먼저 생각한다.\n\n> Q: JMM 문제를 코드로 검증하려면 무엇을 써야 하나요?\n> 의도: 동시성 테스트 도구를 아는지 확인\n> 핵심: 일반 JUnit보다 [JCStress Concurrency Testing](./java/jcstress-concurrency-testing.md)이 훨씬 적합하다.\n\n## 한 줄 정리\n\n`happens-before`는 자바 동시성의 관찰 순서를 정의하고, `volatile`은 같은 변수 write/read의 가시성/순서를, `final`은 올바른 생성 패턴에서 final 필드 초기화 가시성을 제공한다.\n",
  "aliases": [
    "JMM",
    "happens-before가 뭐예요",
    "Java Memory Model 기초",
    "volatile 최신값 원자성 차이",
    "volatile 키워드",
    "final 필드",
    "안전 발행",
    "메모리 배리어",
    "명령 재정렬",
    "atomic 연산",
    "happens-before",
    "volatile"
  ],
  "expected_queries": [
    "volatile은 최신값을 보장하는데 왜 count++ 원자성은 보장하지 못해?",
    "final 필드가 safe publication에 도움이 되는 이유를 설명해줘",
    "Java happens-before가 가시성과 순서를 보장한다는 말이 무슨 뜻이야?",
    "JMM과 CPU cache coherence, memory barrier가 어떻게 연결돼?",
    "double checked locking에서 volatile이 왜 필요한지 알고 싶어",
    "Java Memory Model에서 final 필드가 안전한 공개와 어떤 관련이 있어?"
  ],
  "symptoms": [
    "volatile을 붙였는데 count++가 왜 여전히 깨지는지 모르겠어",
    "happens-before가 실제 실행 순서와 어떻게 다른지 헷갈려",
    "final 필드가 불변성 말고 safe publication과 왜 연결되는지 궁금해"
  ],
  "relations": {
    "prerequisites": [
      "operating-system/process-thread-basics",
      "operating-system/cpu-cache-coherence-memory-barrier"
    ],
    "next_docs": [
      "language/varhandle-unsafe-atomics",
      "language/jcstress-concurrency-testing",
      "operating-system/cpu-cache-coherence-memory-barrier",
      "operating-system/false-sharing-cache-line"
    ],
    "confusable_with": [
      "language/varhandle-unsafe-atomics",
      "operating-system/cpu-cache-coherence-memory-barrier",
      "operating-system/false-sharing-cache-line",
      "language/java-volatile-counter-atomicity-cause-router",
      "language/jvm-gc-jmm-overview",
      "language/class-initialization-ordering"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-06-26",
    "legacy_path": "knowledge/cs/contents/language/java-memory-model-happens-before-volatile-final.md",
    "revision_history": [
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch151"
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-audit-natural-entrypoint-repair"
      },
      {
        "date": "2026-06-26",
        "source": "cycle4-w160-y14-recall",
        "note": "added happens-before observation-order recall surface"
      }
    ]
  }
}
