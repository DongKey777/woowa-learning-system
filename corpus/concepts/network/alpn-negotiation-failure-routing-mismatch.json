{
  "id": "network/alpn-negotiation-failure-routing-mismatch",
  "title": "ALPN Negotiation Failure, Routing Mismatch",
  "category": "network",
  "level": "advanced",
  "summary": "이 문서는 TLS 종료 지점과 프록시 체인에서 ALPN 합의가 어긋날 때 왜 HTTP/2 대신 1.1로 내려가고 gRPC 라우팅까지 틀어지는지 처음 잡는 primer다. gRPC만 깨지는데 일반 API는 됨, 앞단을 지나면 낮은 버전으로 붙음, TLS 끝나는 위치마다 다른 backend로 감, health check는 되는데 실트래픽만 실패, 프로토콜 보고 분기했는데 경로가 엇갈림 같은 자연어 paraphrase가 본 문서의 핵심 개념에 매핑된다.",
  "body_markdown": "# ALPN Negotiation Failure, Routing Mismatch\n\n> 한 줄 요약: ALPN은 TLS handshake에서 어떤 프로토콜을 쓸지 합의하는 단계라서, 여기서 어긋나면 HTTP/1.1, HTTP/2, gRPC 라우팅이 전부 틀어질 수 있다.\n\n**난이도: 🔴 Advanced**\n\n> 관련 문서:\n> - [TLS Certificate Chain, OCSP Stapling Failure Modes](./tls-certificate-chain-ocsp-stapling-failure-modes.md)\n> - [TLS Session Resumption, 0-RTT, Replay Risk](./tls-session-resumption-0rtt-replay-risk.md)\n> - [gRPC vs REST](./grpc-vs-rest.md)\n> - [API Gateway, Reverse Proxy 운영 포인트](./api-gateway-reverse-proxy-operational-points.md)\n> - [HTTP/2 멀티플렉싱과 HOL blocking](./http2-multiplexing-hol-blocking.md)\n> - [h2c, Cleartext Upgrade, Prior Knowledge, Routing](./h2c-cleartext-upgrade-prior-knowledge-routing.md)\n> - [h2c Operational Traps: Proxy Chain, Dev/Prod Drift](./h2c-operational-traps-proxy-chain-dev-prod.md)\n\nretrieval-anchor-keywords: ALPN, protocol negotiation, h2, http/1.1, grpc, TLS handshake, routing mismatch, protocol fallback, ssl_preread, ALPN 의미와 동작, ALPN negotiation 실패 원인, gRPC h2 요구 사항, ALPN vs SNI 구분, protocol downgrade 추적, proxy ALPN passthrough, HTTP/2 prior knowledge (h2c), TLS ClientHello ALPN extension, backend ALPN 라우팅 분기\n\n<details>\n<summary>Table of Contents</summary>\n\n- [핵심 개념](#핵심-개념)\n- [깊이 들어가기](#깊이-들어가기)\n- [실전 시나리오](#실전-시나리오)\n- [코드로 보기](#코드로-보기)\n- [트레이드오프](#트레이드오프)\n- [꼬리질문](#꼬리질문)\n- [한 줄 정리](#한-줄-정리)\n\n</details>\n\n## 핵심 개념\n\nALPN(Application-Layer Protocol Negotiation)은 TLS handshake 동안 **어떤 상위 프로토콜을 사용할지** 합의하는 메커니즘이다.\n\n- `h2`를 선택하면 HTTP/2를 쓴다\n- `http/1.1`을 선택하면 HTTP/1.1을 쓴다\n- gRPC는 사실상 HTTP/2 위에 올라가므로 ALPN 결과가 중요하다\n\n### Retrieval Anchors\n\n- `ALPN`\n- `protocol negotiation`\n- `h2`\n- `http/1.1`\n- `grpc`\n- `TLS handshake`\n- `routing mismatch`\n- `protocol fallback`\n- `ssl_preread`\n\n## 깊이 들어가기\n\n### 1. ALPN이 왜 필요한가\n\nTLS는 암호화만 하는 게 아니다.\n연결 이후 어떤 HTTP 계층을 쓸지도 같이 정해야 한다.\n\n- 브라우저는 HTTP/2를 선호할 수 있다\n- 일부 클라이언트는 HTTP/1.1만 지원한다\n- gRPC는 HTTP/2가 없으면 성립하지 않는다\n\n### 2. failure가 어떻게 보이나\n\nALPN 협상이 실패하면 흔히 이런 형태로 보인다.\n\n- 의도한 프로토콜보다 낮은 버전으로 fallback된다\n- gRPC 호출이 HTTP/1.1로 들어가 실패한다\n- proxy가 HTTP/2를 못 받아 downstream mismatch가 생긴다\n\n### 3. routing mismatch는 왜 생기나\n\n앞단 LB나 proxy가 TLS를 종료하면서 프로토콜 판단을 잘못하면:\n\n- HTTP/2 전용 backend로 보내야 할 요청이 다른 backend로 간다\n- gRPC path가 REST path로 오인된다\n- health check는 되는데 실제 트래픽만 실패한다\n\n### 4. ssl_preread와의 관계\n\nTLS를 종료하기 전에 SNI/ALPN을 보고 라우팅하는 경우가 있다.\n\n- `ssl_preread`로 대략의 프로토콜을 읽는다\n- 이후 backend 분기 여부를 결정한다\n- 설정이 틀리면 프로토콜과 라우팅이 어긋난다\n\n### 5. 운영에서 더 중요한 점\n\nALPN 문제는 종종 \"네트워크\"처럼 보이지만 실제론 라우팅/배포 문제다.\n\n- LB 설정\n- backend pool 분리\n- TLS 종료 지점\n- 프록시 패스스루 여부\n\n## mental model과 misconception\n\nALPN은 \"TLS가 성공했는가\"가 아니라 \"TLS 이후 어떤 application protocol로 말할 것인가\"를 정하는 합의다. 그래서 certificate가 정상이어도 gRPC가 깨질 수 있고, HTTP/1.1 health check가 살아 있어도 HTTP/2 실트래픽은 실패할 수 있다.\n\n| 헷갈리는 말 | 실제로 먼저 볼 것 | 왜 구분해야 하나 |\n|---|---|---|\n| \"TLS handshake가 됐으니 ALPN도 된 것 아닌가?\" | negotiated protocol 값 | 암호화 연결 성공과 `h2` 선택은 다른 결과다 |\n| \"SNI로 backend를 골랐으니 protocol도 맞겠지\" | SNI routing과 ALPN routing을 따로 확인 | SNI는 hostname, ALPN은 protocol 선택이다 |\n| \"REST는 되니 gRPC도 될 것\" | gRPC 경로의 `h2` 협상 | REST는 HTTP/1.1 fallback으로 살아 있을 수 있다 |\n| \"브라우저에서 HTTP/2가 보인다\" | proxy 뒤 downstream protocol | edge와 backend 사이에서 다시 HTTP/1.1로 내려갈 수 있다 |\n\n검증은 hop별로 나눠야 한다. `openssl s_client -alpn h2`는 edge에서 어떤 protocol이 협상됐는지 보여준다. 하지만 그것만으로 backend까지 `h2`가 유지됐다고 증명하지는 못하므로 proxy access log, upstream protocol metric, backend handler log를 같이 본다.\n\n## hop별 확인 순서\n\nALPN 문제는 한 번의 `curl --http2` 결과만 보고 닫으면 안 된다. TLS를 어디서 종료하는지에 따라 edge, gateway, sidecar, backend가 각각 다른 protocol contract를 가질 수 있기 때문이다. 그래서 먼저 요청 경로를 hop 단위로 쪼개고, 각 hop에서 \"클라이언트가 제안한 protocol\", \"그 hop이 협상한 protocol\", \"다음 upstream으로 보낸 protocol\"을 따로 적는다.\n\n| hop | 확인할 값 | 흔한 오판 |\n|---|---|---|\n| client -> edge | ClientHello ALPN list와 negotiated protocol | TLS 성공을 `h2` 성공으로 착각한다 |\n| edge -> gateway | upstream protocol, route name, backend cluster | edge에서 `h2`가 보여서 backend도 `h2`라고 단정한다 |\n| gateway -> service | gRPC route matcher, content-type, HTTP version | REST health check 성공을 gRPC 성공으로 본다 |\n| service log | handler, protocol label, rejected reason | application error와 protocol mismatch를 섞는다 |\n\n운영 로그에는 최소한 negotiated protocol, upstream protocol, route name, backend cluster, grpc status를 같이 남기는 편이 좋다. 이 다섯 값이 없으면 \"gRPC만 깨진다\"는 증상이 certificate, SNI, path rewrite, HTTP/2 downgrade 중 어디에 속하는지 계속 흔들린다. 특히 gateway가 `h2` client connection을 받고도 upstream은 HTTP/1.1로 재시도하거나, h2c backend에 TLS ALPN 결과를 그대로 기대하는 구성이 섞이면 장애가 hop 사이에서 사라진 것처럼 보인다.\n\n중단 기준도 명확해야 한다. client edge에서 `h2`가 협상되고, edge access log가 gRPC route를 탔으며, upstream metric이 backend까지 HTTP/2 또는 h2c로 연결됐고, backend handler가 같은 request id를 받았다는 네 증거가 모일 때 ALPN/routing 버킷을 닫을 수 있다. 반대로 어느 한 hop이라도 protocol label이 비어 있거나 route가 default REST cluster로 떨어지면 애플리케이션 코드를 보기 전에 proxy 설정을 먼저 본다.\n\n## 실전 시나리오\n\n### 시나리오 1: gRPC 호출만 실패한다\n\nALPN이 `h2`로 협상되지 않으면 gRPC는 성립하지 않는다.\n\n### 시나리오 2: 브라우저는 되는데 API gateway 뒤에서만 이상하다\n\ngateway가 ALPN으로 protocol fallback을 잘못 처리했을 수 있다.\n\n### 시나리오 3: 일부 리전만 HTTP/2가 안 된다\n\n리전별 LB 설정이나 cert bundle이 달라서 negotiation 결과가 달라질 수 있다.\n\n## 코드로 보기\n\n### openssl로 ALPN 확인\n\n```bash\nopenssl s_client -connect api.example.com:443 -servername api.example.com -alpn h2\n```\n\n### curl로 프로토콜 확인\n\n```bash\ncurl -I --http2 https://api.example.com\n```\n\n### 관찰 포인트\n\n```text\n- negotiated protocol이 무엇인가\n- 기대한 backend로 라우팅되었는가\n- fallback이 의도된 것인가\n```\n\n## 트레이드오프\n\n| 선택지 | 장점 | 단점 | 언제 쓰는가 |\n|--------|------|------|-------------|\n| ALPN 기반 협상 | 프로토콜 선택이 자동화된다 | 설정 오류가 라우팅 문제로 번진다 | HTTPS 다중 프로토콜 |\n| 고정 프로토콜 | 단순하다 | 유연성이 낮다 | 내부 통제 환경 |\n| fallback 허용 | 호환성이 높다 | 성능/기능이 예상과 달라질 수 있다 | 점진 전환 |\n\n핵심은 협상이 실패했을 때 **조용히 틀린 경로로 가지 않게** 하는 것이다.\n\n## 꼬리질문\n\n> Q: ALPN은 무엇을 결정하나요?\n> 핵심: TLS handshake 이후 사용할 애플리케이션 프로토콜을 결정한다.\n\n> Q: 왜 gRPC에 중요하나요?\n> 핵심: gRPC는 HTTP/2가 사실상 필요하기 때문이다.\n\n> Q: negotiation 실패가 왜 routing 문제로 보이나요?\n> 핵심: TLS 종료 지점이 프로토콜에 따라 backend를 다르게 골라야 하기 때문이다.\n\n## 한 줄 정리\n\nALPN은 TLS 위에서 사용할 프로토콜을 정하는 협상이므로, 실패하면 단순 암호화 문제가 아니라 라우팅과 backend 선택 문제로 이어진다.\n",
  "aliases": [
    "ALPN h2 routing mismatch",
    "gRPC h2 negotiation failure",
    "TLS ALPN protocol fallback",
    "SNI ALPN routing split",
    "proxy ALPN passthrough",
    "backend protocol mismatch",
    "ALPN 협상 실패",
    "gRPC HTTP2 라우팅 오류",
    "TLS 종료 지점 프로토콜 분기",
    "프록시 h2 fallback 진단"
  ],
  "expected_queries": [
    "gRPC만 실패하고 REST는 되면 앞단에서 무엇을 먼저 확인해야 하나요?",
    "브라우저는 문제없는데 gateway 뒤 호출만 HTTP/2가 안 잡히는 이유가 뭔가요?",
    "TLS 종료 지점에서 잘못된 backend로 보내지는 현상을 어떻게 읽나요?",
    "h2로 붙어야 하는 서비스가 자꾸 낮은 버전으로 내려갈 때 어디를 봐야 하나요?",
    "ALB나 프록시를 지난 뒤 프로토콜 계층이 달라지는 이유를 설명해 주세요",
    "ssl preread로 나눈 라우팅이 실제 요청 처리와 어긋날 수 있나요?"
  ],
  "symptoms": [
    "gRPC 호출만 깨지고 같은 호스트의 일반 HTTP 요청은 살아 있다",
    "HTTP/2로 붙어야 하는데 앞단을 지나면 계속 1.1처럼 동작한다",
    "TLS를 끝내는 지점마다 backend가 다르게 골라지는 것 같다"
  ],
  "relations": {
    "prerequisites": [
      "network/http-https-basics",
      "network/http1-http2-http3-beginner-comparison",
      "network/tls-loadbalancing-proxy"
    ],
    "next_docs": [
      "network/browser-http-version-selection-alpn-alt-svc-fallback",
      "network/http2-http3-downgrade-attribution-alt-svc-udp-block",
      "network/sni-routing-mismatch-hostname-failure"
    ],
    "confusable_with": [
      "network/sni-routing-mismatch-hostname-failure",
      "network/browser-http-version-selection-alpn-alt-svc-fallback",
      "network/h2c-operational-traps-proxy-chain-dev-prod",
      "network/http2-http3-downgrade-attribution-alt-svc-udp-block",
      "network/certificate-rotation-sni-blast-radius"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-06-18",
    "legacy_path": "knowledge/cs/contents/network/alpn-negotiation-failure-routing-mismatch.md",
    "revision_history": [
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch149",
        "changes": [
          "alias-query-contract-repair",
          "body-floor-repair"
        ]
      }
    ]
  }
}
