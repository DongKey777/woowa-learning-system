{
  "id": "network/webrtc-ice-stun-turn-nat-traversal-primer",
  "title": "WebRTC ICE, STUN, TURN, NAT Traversal Primer",
  "category": "network",
  "level": "intermediate",
  "summary": "이 문서는 WebRTC 연결 수립을 ICE candidate, STUN, TURN, NAT traversal 관점으로 설명하는 primer다. ICE는 가능한 경로 후보를 모아 우선순위를 두고 연결을 시도하는 절차이고, STUN은 내 public address를 알아내는 데 도움을 주며, TURN은 직접 연결이 막힐 때 relay 역할을 한다. UDP hole punching과 방화벽, symmetric NAT 때문에 직접 연결이 실패할 수 있음을 다룬다.",
  "body_markdown": "# WebRTC ICE, STUN, TURN, NAT Traversal Primer\n\n## 핵심 흐름\nWebRTC peer는 host candidate, server-reflexive candidate, relay candidate를 모은다. STUN server는 NAT 밖에서 보이는 주소를 알려주어 server-reflexive candidate를 만든다. TURN server는 직접 연결이 실패할 때 traffic을 relay한다. ICE는 후보 쌍을 우선순위대로 검사해 실제로 통하는 경로를 고른다.\n\n## 빠른 분기\n같은 네트워크에서는 되지만 외부망에서 안 되면 NAT traversal을 본다. 회사망이나 방화벽에서만 실패하면 UDP 차단과 TURN fallback을 본다. TURN 사용량이 급증하면 직접 P2P 연결 실패가 늘어난 신호일 수 있다.\n\n## 후보 타입을 먼저 나누기\n\nICE 로그를 볼 때는 candidate 개수보다 candidate type을 먼저 본다. `host` candidate는 로컬 인터페이스 주소라 같은 LAN이나 VPN에서는 유용하지만, 일반 인터넷 반대편 peer가 그대로 접근하지 못할 수 있다. `srflx` candidate는 STUN으로 알아낸 NAT 바깥 주소다. 직접 P2P 경로가 가능할 때 핵심 후보가 된다. `relay` candidate는 TURN 서버를 경유하는 경로라 성공률은 높지만 서버 bandwidth 비용과 RTT가 늘어난다.\n\n| candidate type | 만들어지는 방식 | 의미 | 흔한 진단 |\n|---|---|---|---|\n| host | 로컬 네트워크 인터페이스 | 내부망 직접 후보 | 같은 네트워크에서만 성공하면 여기까지는 정상일 수 있다 |\n| srflx | STUN 응답 | NAT 바깥에서 보이는 주소 | public 후보가 없으면 STUN reachability를 본다 |\n| relay | TURN allocation | 서버 중계 후보 | relay만 선택되면 직접 경로가 막힌 것이다 |\n\n따라서 \"ICE candidate가 있다\"는 말만으로는 부족하다. 어떤 후보가 수집됐고, 어떤 candidate pair가 selected 되었고, 실패한 pair의 에러가 무엇인지까지 봐야 한다.\n\n## 실패를 분리하는 기준\n\nWebRTC 장애는 signaling 실패와 media path 실패가 자주 섞인다. Offer/answer가 교환되지 않았다면 ICE 이전 문제다. Offer/answer와 candidate 교환은 끝났는데 영상이 안 나온다면 connectivity check, UDP 차단, TURN allocation, codec/DTLS/SRTP 쪽을 따로 본다.\n\n운영 로그는 최소한 `iceConnectionState`, selected candidate pair type, local/remote candidate type, TURN allocation error, setup latency를 함께 남기는 편이 좋다. 같은 증상이어도 `checking`에서 timeout 나는 경우와 `connected` 뒤 packet loss가 커지는 경우는 대응이 다르다. 전자는 NAT traversal과 방화벽을 먼저 보고, 후자는 네트워크 품질과 congestion, TURN region 배치를 같이 본다.\n\n## 흔한 오해\nSTUN은 relay가 아니다. TURN은 relay라서 비용과 latency가 커진다. ICE candidate가 많다고 모두 연결되는 것은 아니며 connectivity check 결과가 중요하다.\n\n## 실전 시나리오\n두 브라우저가 화상 연결을 시도하지만 회사망에서만 실패한다고 하자. Signaling은 offer/answer와 ICE candidate를 서로 전달할 뿐 media path를 대신 만들어주지 않는다. STUN은 public address 후보를 알아내고, ICE connectivity check가 후보 pair를 시험한다. Symmetric NAT나 UDP 차단 환경에서는 직접 연결이 실패해 TURN relay로 fallback된다. TURN 비율이 갑자기 늘면 네트워크 정책 변경이나 STUN reachability를 봐야 한다.\n\n```text\n연결 실패 진단 순서\n1. offer/answer 교환이 끝났는가?\n2. host/srflx/relay candidate가 수집됐는가?\n3. selected candidate pair가 relay로만 잡히는가?\n4. TURN allocation 실패, UDP 차단, symmetric NAT 신호가 있는가?\n```\n\n## 다음 행동\n- signaling 성공과 media connectivity 성공을 분리해서 로그로 남긴다.\n- trickle ICE에서 candidate가 늦게 도착해도 처리되는지 확인한다.\n- TURN fallback rate, connection setup latency, packet loss를 운영 지표로 본다.\n\n## 확인 질문\n이 문제는 signaling 메시지가 오가지 않는 문제인가, 후보는 교환됐지만 ICE connectivity check가 실패하는 문제인가?\n\n## CS 배경: NAT traversal와 connectivity establishment\nNAT는 private address를 public address와 port mapping으로 변환한다. UDP hole punching은 양쪽 endpoint가 동시에 packet을 보내 NAT mapping을 열어 두는 기법이다. STUN은 client가 자신의 reflexive candidate를 알게 해 주고, TURN은 직접 경로가 실패할 때 relay server를 제공한다. ICE는 여러 candidate pair를 모아 connectivity check로 가장 좋은 경로를 선택하는 algorithm이다.\n\nWebRTC 연결은 signaling plane과 media plane이 분리된다. Offer/answer는 session description을 교환하고, ICE candidate는 가능한 network path를 표현한다. 실제 RTP media는 선택된 candidate pair로 흐른다. Symmetric NAT, UDP blocking, firewall policy는 direct path를 막아 relay cost와 latency를 증가시킨다.\n\n## 깊게 볼 포인트\nICE는 단순히 STUN/TURN 서버 주소를 넣는 설정이 아니라 candidate pair selection 알고리즘이다. Candidate에는 host, server reflexive, relay 같은 type이 있고 priority가 있다. ICE agent는 pair를 만들고 connectivity check를 보내며 nomination 과정을 통해 selected pair를 정한다. Trickle ICE는 candidate를 한 번에 모두 모을 때까지 기다리지 않고, 발견되는 대로 signaling으로 보내 setup latency를 줄인다. TURN은 연결 성공률을 높이지만 relay bandwidth 비용과 latency를 만든다. 그래서 WebRTC 품질은 call setup time, selected candidate type, RTT, packet loss, jitter, TURN ratio를 함께 봐야 한다.\n\n## 품질 관점\n프로토콜 상태, timeout, retry, 중간 장비, client/server 관측값을 분리해 같은 증상을 재현한다. 이 문서의 답변 품질은 `WebRTC ICE, STUN, TURN, NAT Traversal Primer`를 단순 용어가 아니라 선택 기준, 실패 모드, 검증 방법까지 연결하는지로 판단한다. 특히 비슷한 이웃 문서와 섞일 때 primary answer가 왜 이 문서인지 설명할 수 있어야 한다.\n\n## 성능 관점\n성능은 평균 처리량 하나로 판단하지 않는다. 입력 크기, 큐/락/캐시/네트워크/런타임 비용 중 무엇이 병목인지 분리하고, 작은 synthetic case와 production-like case를 따로 측정한다. 최적화가 correctness invariant를 흐리면 성능 개선으로 인정하지 않는다.\n\n## 레이턴시 관점\n레이턴시는 p50보다 p95/p99와 cold path, retry, queueing, lock wait, cache miss를 함께 본다. 학습 답변에는 latency budget과 관측 지표가 포함되어야 하며, 개선 전후가 같은 조건에서 비교되어야 한다.\n\n## 주제별 추가 지표\n`request_p99_ms, retransmit_or_retry_count, timeout_count, connection_reuse_ratio`를 최소 지표로 둔다. 이 지표들은 품질, 성능, 레이턴시를 한 화면에서 비교하기 위한 기준이며, 단순 통과/실패보다 어떤 경계가 병목인지 드러내야 한다.\n\n## TURN을 쓰는 판단\n\nSTUN은 내 공인 후보 주소를 알아내는 데 도움을 주지만, 대칭 NAT나 엄격한 방화벽에서는 직접 연결이 실패할 수 있다. TURN은 미디어를 relay하므로 비용과 지연이 늘지만 연결 성공률을 높인다. 운영에서는 TURN 사용 비율이 갑자기 늘면 네트워크 정책 변화나 ICE 후보 수집 실패를 의심한다.\n",
  "aliases": [
    "WebRTC ICE 흐름",
    "STUN TURN 차이",
    "ICE 후보 수집",
    "TURN 릴레이 fallback",
    "NAT traversal WebRTC",
    "UDP hole punching",
    "server reflexive candidate",
    "relay candidate",
    "symmetric NAT 연결 실패",
    "WebRTC 외부망 실패"
  ],
  "expected_queries": [
    "WebRTC에서 ICE가 후보를 모으고 연결 경로를 고르는 흐름을 설명해줘",
    "STUN과 TURN은 각각 어떤 문제를 해결하고 비용은 어떻게 달라?",
    "같은 와이파이에서는 WebRTC가 되는데 외부망에서 실패하면 무엇을 봐야 해?",
    "TURN relay 비율이 늘었을 때 NAT나 방화벽 관점에서 어떻게 진단해?",
    "server reflexive candidate와 relay candidate는 로그에서 어떻게 구분해?",
    "signaling 성공과 media 연결 성공을 왜 따로 봐야 해?"
  ],
  "symptoms": [
    "STUN을 relay server로 착각한다",
    "TURN fallback이 늘어난 것을 정상 직접 연결로 오해한다",
    "candidate 수만 보고 실제 connectivity check를 보지 않는다"
  ],
  "relations": {
    "prerequisites": [
      "network/udp-hole-punching-nat-traversal-stun-turn-primer",
      "network/tcp-udp-basics"
    ],
    "next_docs": [
      "network/hairpin-nat-loopback-behavior",
      "network/nat-keepalive-tuning-connection-lifetime",
      "network/packet-loss-jitter-reordering-diagnostics",
      "network/nat-conntrack-ephemeral-port-exhaustion"
    ],
    "confusable_with": [
      "network/udp-hole-punching-nat-traversal-stun-turn-primer",
      "network/nat-conntrack-ephemeral-port-exhaustion",
      "network/hairpin-nat-loopback-behavior"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-27",
    "last_modified": "2026-06-18",
    "revision_history": [
      {
        "date": "2026-05-27",
        "source": "y14-batch1"
      },
      {
        "date": "2026-05-28",
        "source": "y14-closure-quality-audit"
      },
      {
        "date": "2026-05-28",
        "source": "y14-qrels-anchor-sync"
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch146"
      }
    ]
  }
}
