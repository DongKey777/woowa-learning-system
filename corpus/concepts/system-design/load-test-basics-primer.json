{
  "id": "system-design/load-test-basics-primer",
  "title": "Load Testing Scope and Safety Primer",
  "category": "system-design",
  "level": "beginner",
  "summary": "부하 테스트를 단순히 동시에 요청을 많이 보내는 행동이 아니라, 목적별 테스트 유형, 목표 부하, 시나리오 비율, 중단 기준, production 안전장치를 정해 실제 운영 위험을 확인하는 연습으로 설명하는 입문 문서다.",
  "body_markdown": "# Load Testing Scope and Safety Primer\n\n## 한 줄 요약\n\n부하 테스트는 서버를 괴롭히는 이벤트가 아니라, 정해진 기간 동안 정해진 요청 패턴을 보내면서 목표 처리량, 응답 시간, 오류율, 자원 사용률이 안전한 범위에 있는지 확인하는 운영 연습이다.\n\n## 먼저 구분할 테스트 종류\n\n처음에는 이름을 아래처럼 나누면 충분하다.\n\n| 종류 | 목적 | 예시 |\n|---|---|---|\n| smoke test | 테스트 스크립트와 환경이 정상인지 확인 | 1분 동안 아주 낮은 RPS로 로그인, 조회, 주문 흐름이 동작하는지 본다 |\n| load test | 예상 정상 트래픽을 견디는지 확인 | 평소 피크의 1.0배 또는 1.2배를 20분 동안 보낸다 |\n| stress test | 한계와 병목이 어디인지 찾기 | RPS를 단계적으로 올려 오류율이나 p95가 급격히 나빠지는 지점을 찾는다 |\n| spike test | 갑작스러운 유입을 버티는지 확인 | 30초 안에 5배 트래픽으로 올렸다가 다시 낮춘다 |\n| soak test | 오래 버틸 때 문제가 누적되는지 확인 | 몇 시간 동안 일정 부하를 걸어 memory leak, connection leak, 로그 적재 지연을 본다 |\n\n이 다섯 가지를 섞어서 말하면 결과 해석이 흐려진다. `이번 테스트는 정상 피크 검증인지, 한계 탐색인지, 장시간 누적 문제 확인인지`를 먼저 말해야 한다.\n\n## 목표 부하는 숫자와 시나리오가 같이 있어야 한다\n\n`동시 요청을 많이 보낸다`는 말만으로는 부족하다. 최소한 target RPS, VU, scenario mix를 함께 적는다.\n\n- target RPS: 초당 몇 개의 요청을 시작할지 또는 완료할지에 대한 목표다.\n- VU: virtual user 수다. 사용자가 생각하고 기다리는 시간까지 포함한 사용자 흐름을 흉내 낼 때 쓴다.\n- scenario mix: 조회 80%, 쓰기 15%, 결제 5%처럼 실제 서비스 사용 비율을 담는다.\n\n예를 들어 `300 RPS, 20분, 조회 85% / 생성 10% / 결제 5%, p95 500ms 이하, 오류율 1% 이하`처럼 적으면 무엇을 검증하는지 분명해진다. 반대로 `동시 사용자 1000명`만 말하면 각 사용자가 몇 초에 한 번 요청하는지, 어떤 API를 치는지, 로그인 세션을 재사용하는지 알 수 없다.\n\n## 테스트 대상, 기간, 중단 기준\n\n부하 테스트 계획에는 다음 네 줄이 있어야 한다.\n\n1. 대상: staging인지 production인지, 단일 API인지 전체 사용자 흐름인지 정한다.\n2. 기간: ramp-up, steady state, ramp-down 시간을 나눈다.\n3. 관측 지표: RPS, latency p50/p95/p99, error rate, CPU, memory, DB connection, queue depth, downstream 오류를 본다.\n4. 중단 기준: 오류율, timeout, DB CPU, 결제/알림 같은 외부 시스템 영향이 어느 선을 넘으면 즉시 멈출지 정한다.\n\n초급 단계에서 중요한 것은 복잡한 수식보다 `무엇을 보내고, 얼마나 오래 보내고, 어떤 신호가 나오면 멈출지`다. 중단 기준이 없으면 테스트가 운영 장애 훈련이 아니라 실제 장애 유발이 될 수 있다.\n\n## production 안전성\n\nproduction에서 테스트해야 할 때는 더 보수적으로 접근한다. 읽기 전용 API부터 시작하고, 테스트 계정과 테스트 데이터만 쓰며, 결제, 문자, 이메일, 재고 차감 같은 부작용은 차단하거나 별도 sandbox로 보낸다. rate limit, feature flag, kill switch, 담당자 모니터링 채널도 미리 준비한다.\n\n또한 테스트 트래픽에는 별도 header나 user agent를 붙여 로그와 metric에서 실제 사용자 트래픽과 구분한다. 테스트가 끝난 뒤에는 캐시 오염, 큐 적체, 테스트 데이터 잔여, downstream quota 소모를 확인한다.\n\n## 면접 답변 축\n\n좋은 답변은 `많이 보낸다`에서 멈추지 않는다. 먼저 smoke/load/stress/spike/soak 중 목적을 고르고, target RPS와 VU, scenario mix를 정한다. 그 다음 대상 환경, 기간, 관측 지표, 중단 기준을 말한다. production이면 부작용 차단, 트래픽 라벨링, kill switch, 담당자 모니터링까지 붙여야 한다. 이 정도가 잡힌 뒤에야 더 깊은 측정 왜곡이나 용량 공식으로 넘어갈 수 있다.\n",
  "aliases": [
    "load testing basics",
    "load test planning",
    "부하 테스트가 뭐야",
    "처음 하는 트래픽 테스트",
    "부하 테스트 계획",
    "동시 요청 부하 테스트",
    "RPS랑 VU 차이",
    "부하 테스트 유형 차이",
    "운영 부하 테스트 안전장치",
    "부하 테스트 중단 기준"
  ],
  "expected_queries": [
    "부하 테스트로 동시 요청을 많이 보낸다는 게 뭐야",
    "부하 테스트를 처음 계획할 때 RPS랑 VU 중 뭘 정해야 해?",
    "부하 테스트에서 smoke, load, stress, spike, soak는 어떻게 달라?",
    "운영 환경에서 부하 테스트를 하면 어떤 안전장치를 둬야 해?",
    "동시 사용자 수만 정하면 부하 테스트 목표가 충분한 거야?",
    "부하 테스트 중단 기준은 어떤 지표로 잡아야 해?"
  ],
  "symptoms": [
    "부하 테스트를 단순히 요청을 많이 보내는 것으로만 이해합니다",
    "RPS, VU, 시나리오 비율을 구분하지 못합니다",
    "production 테스트에서 부작용 차단과 중단 기준을 빠뜨립니다"
  ],
  "relations": {
    "prerequisites": [
      "network/latency-bandwidth-throughput-basics"
    ],
    "next_docs": [
      "system-design/littles-law-capacity-planning-primer",
      "system-design/coordinated-omission-load-test-playbook"
    ],
    "confusable_with": [
      "language/jmh-benchmarking-pitfalls"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-06-19",
    "last_modified": "2026-06-19",
    "revision_history": [
      {
        "date": "2026-06-19",
        "source": "foundation-ladder-batch205"
      }
    ]
  }
}
