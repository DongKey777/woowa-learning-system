{
  "id": "system-design/webhook-retry-dedup-deadletter-playbook",
  "title": "Webhook Retry Dedup Dead Letter Playbook",
  "category": "system-design",
  "level": "intermediate",
  "summary": "이 playbook은 webhook retry, deduplication, dead letter queue, replay safety를 다룬다. 같은 webhook 두 번 처리, 처리 실패 유실, DLQ replay 부작용 중복 같은 운영 증상을 다룬다.",
  "body_markdown": "# Webhook Retry Dedup Dead Letter Playbook\n\n## 핵심 invariant\n\nWebhook 수신자는 외부 시스템의 delivery 결과를 완전히 통제할 수 없다. 같은 event가 여러 번 도착하거나, 발신자는 실패로 보았지만 수신자는 이미 처리한 상태가 정상적으로 발생한다. 따라서 invariant는 \"중복 수신이 중복 side effect로 이어지지 않는다\"이다.\n\n- 성공 응답은 처리 결과가 durable하게 기록된 뒤에만 반환한다.\n- `event_id`, provider event id, business key 중 하나를 idempotency key로 고정한다.\n- key별 상태 전이는 `received -> processing -> succeeded` 또는 `received -> failed_retryable/failed_terminal`처럼 단조롭게 기록한다.\n- 외부 결제 승인, 포인트 적립, 이메일 발송 같은 side effect는 상태 전이 guard 뒤에 둔다.\n- DLQ replay도 최초 수신과 같은 idempotency path를 통과해야 한다.\n\n## 흔한 오개념\n\n| 오개념 | 왜 위험한가 | 더 안전한 판단 |\n|---|---|---|\n| \"Webhook은 한 번만 온다\" | 대부분 provider는 at-least-once delivery를 택한다 | 중복은 정상 입력으로 테스트한다 |\n| \"HTTP 200을 보냈으니 끝났다\" | 응답 직전 crash, provider timeout, network split로 관찰이 갈릴 수 있다 | durable status와 provider event id로 reconciliation한다 |\n| \"retry 횟수를 늘리면 유실이 해결된다\" | retry는 일시 실패에는 유효하지만 poison payload나 코드 버그에는 실패를 반복한다 | retryable, terminal, manual review를 분류한다 |\n| \"DLQ replay는 별도 관리자 기능이라 dedup이 없어도 된다\" | replay가 실제 주문/결제 side effect를 다시 만들 수 있다 | replay도 동일 key와 상태 전이 guard를 사용한다 |\n\n## Chooser table\n\n| 상황 | 우선 선택 | 이유 | 주의할 점 |\n|---|---|---|---|\n| provider가 같은 event id를 재전송 | dedup store + idempotent status read | 정상 중복 수신을 빠르게 흡수 | dedup TTL이 provider retry window보다 짧으면 안 된다 |\n| 처리 중 downstream timeout | retryable failure + backoff | 성공 여부가 불확실할 수 있음 | 외부 상태 조회로 이미 반영됐는지 확인할 hook을 둔다 |\n| payload schema가 깨짐 | terminal failure 또는 quarantine | 재시도해도 성공 가능성이 낮음 | provider 계약 변경인지 consumer 호환성 버그인지 분리 |\n| consumer 버그 배포 후 실패 누적 | DLQ 보관 + fix-forward 후 canary replay | 코드 수정 전 replay는 실패를 반복 | replay budget, stop condition, idempotency 검증 필요 |\n| business state와 webhook 상태 불일치 | reconciliation job | webhook만으로 truth를 복구하기 어려움 | provider API 조회와 내부 ledger 비교가 필요 |\n\n## 실무 지표와 증거\n\n- `webhook_received_total`, `webhook_duplicate_total`, `webhook_dedup_hit_ratio`\n- key별 `processing` 체류 시간, `failed_retryable` 재시도 횟수, `failed_terminal` 비율\n- provider별 delivery latency, retry interval, signature verification failure rate\n- DLQ depth, DLQ age, replay success/failure ratio, replay 후 duplicate side effect count\n- business evidence: 주문/결제/정산 ledger의 event id, provider API 조회 결과, 내부 상태 전이 로그\n\n중복률이 높아도 항상 장애는 아니다. 장애 신호는 duplicate 수신 자체보다 `duplicate side effect`, `processing stuck`, `retry storm`, `DLQ age 증가`에 가깝다.\n\n## 재현 조건\n\n외부 시스템이 webhook을 at-least-once로 보내고, 수신 서버가 성공 처리 직후 응답 전에 timeout되거나 crash한다고 가정한다. 같은 event가 다시 도착했을 때 내부 side effect가 한 번만 발생해야 한다.\n\n## 검증 순서\n\n1. 같은 provider event id를 두 번 전송해서 두 번째 요청이 기존 `succeeded` 상태를 읽고 no-op이 되는지 확인한다.\n2. 첫 요청이 `processing` 기록 후 timeout되는 상황을 만들고, 재전송 요청이 lock, lease, stale processing timeout 정책을 따르는지 본다.\n3. downstream timeout 후 provider API나 내부 ledger로 실제 반영 여부를 reconciliation한다.\n4. schema 오류 payload를 보내 retryable queue가 아니라 terminal/quarantine 경로로 빠지는지 확인한다.\n5. DLQ에 들어간 이벤트를 fix-forward 후 소량 replay하고, 같은 idempotency key로 dedup되는지 검증한다.\n6. replay 중 `replay_failure_ratio`, `duplicate_side_effect_count`, `DLQ re-entry rate`가 중단 기준을 넘으면 즉시 멈춘다.\n\n## 운영/테스트 fixture\n\n- 동일 event 2회 수신\n- 성공 처리 후 응답 timeout으로 provider가 재전송\n- consumer crash 후 `processing` 상태가 오래 남는 케이스\n- provider schema 변경 또는 필수 필드 누락\n- DLQ replay 중 이미 처리된 event와 아직 처리되지 않은 event 혼합\n- 외부 provider 상태는 성공인데 내부 read model은 미반영인 reconciliation 케이스\n\n## 운영 판단: provider retry는 정상 경로다\n\nWebhook provider는 2xx 응답을 못 받으면 같은 event를 다시 보낼 수 있다. 우리 서버가 실제로 처리했지만 응답 timeout이 났을 수도 있고, processing 상태에서 crash가 났을 수도 있다. 그래서 event id 또는 provider delivery id를 idempotency key로 저장하고, 상태를 `received`, `processing`, `succeeded`, `failed_retryable`, `failed_terminal`, `dead_lettered`처럼 나눈다.\n\n같은 event가 다시 오면 먼저 저장된 상태를 읽는다. `succeeded`면 side effect를 다시 실행하지 않고 저장된 결과를 재사용하거나 2xx로 응답한다. `processing`이 너무 오래됐다면 lease timeout과 reconciliation을 거쳐 재시도할지 정한다. `failed_terminal`은 schema mismatch나 signature failure처럼 재시도해도 성공하지 않는 경우이고, `failed_retryable`은 downstream 429/timeout처럼 backoff 후 다시 볼 수 있는 경우다.\n\n## DLQ replay 안전장치\n\nDLQ replay는 실패한 payload를 다시 넣는 버튼이 아니다. replay 전에 원인 수정 여부, schema compatibility, provider 상태, 내부 ledger 반영 여부, idempotency 상태를 확인해야 한다. 이미 성공한 event와 아직 처리되지 않은 event가 섞여 있어도 같은 event id guard가 있으면 중복 부작용을 막을 수 있다. replay 중 duplicate side effect가 하나라도 보이면 중단하고 reconciliation으로 돌려야 한다.\n\n## replay 중단 기준\n\nDLQ replay는 작은 batch로 시작하고 중단 기준을 먼저 정한다. `duplicate_side_effect_count`, `provider_status_mismatch`, `schema_decode_failure`, `DLQ_reentry_count`, `processing_lease_timeout`이 임계치를 넘으면 즉시 멈춘다. replay worker는 정상 consumer와 같은 idempotency table을 사용해야 하며, 별도 bypass 경로로 side effect를 직접 호출하면 사고를 다시 만든다.\n\n운영자는 replay 결과를 succeeded, already_processed, retryable_failed, terminal_failed로 나눠야 한다. 이미 처리된 event가 많으면 dedup이 잘 동작한다는 증거이고, terminal failure가 많으면 payload/schema 수정 없이 replay를 계속하면 안 된다는 신호다.\n\n",
  "aliases": [
    "webhook retry dedup DLQ",
    "webhook idempotency replay",
    "웹훅 재시도 중복 DLQ",
    "webhook dedup replay",
    "provider retry storm",
    "event id dedup",
    "DLQ replay safety",
    "웹훅 중복 수신",
    "웹훅 실패 재처리",
    "dead letter webhook",
    "processing 상태 재시도",
    "webhook status contract"
  ],
  "expected_queries": [
    "webhook이 중복으로 오거나 실패하면 retry와 dedup을 어떻게 설계해?",
    "외부 webhook 처리 실패를 dead letter queue에 넣어야 할까?",
    "webhook replay가 안전하려면 어떤 idempotency key가 필요해?",
    "결제 provider가 같은 webhook을 여러 번 보내는 retry storm을 어떻게 방어해?",
    "DLQ에 넣은 webhook을 다시 처리할 때 event id dedup을 먼저 확인해야 하는 이유는?",
    "웹훅 처리 실패를 그냥 버리지 않고 재처리 큐로 보내야 하는 기준을 알려줘",
    "webhook 처리 후 응답 timeout으로 provider가 재전송하면 succeeded 상태를 어떻게 재사용해야 해?"
  ],
  "symptoms": [
    "같은 webhook event가 두 번 처리돼요",
    "처리 실패한 webhook을 잃어버려요",
    "DLQ replay 후 부작용이 중복돼요"
  ],
  "relations": {
    "prerequisites": [
      "system-design/idempotency-reconciliation-playbook"
    ],
    "next_docs": [
      "system-design/webhook-delivery-platform-design"
    ],
    "confusable_with": [
      "system-design/webhook-delivery-platform-design",
      "system-design/idempotency-reconciliation-playbook"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-06-18",
    "legacy_path": "knowledge/cs/contents/system-design/webhook-retry-dedup-deadletter-playbook.md",
    "revision_history": [
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch130",
        "note": "Expanded webhook delivery retry surfaces for batch130."
      }
    ]
  }
}
