{
  "id": "system-design/redisson-pubsub-lock-watchdog-primer",
  "title": "Redisson Pub/Sub Lock and Watchdog Primer",
  "category": "system-design",
  "level": "advanced",
  "summary": "Redisson lock은 Redis 기반 lock 위에 pub/sub wakeup과 watchdog renewal을 얹어 polling 증폭과 짧은 TTL 문제를 줄인다. 하지만 waitTime/leaseTime 선택, GC pause에 따른 renewal failure, stale owner write, fencing token/DB idempotency는 별도 설계해야 한다.",
  "body_markdown": "# Redisson Pub/Sub Lock and Watchdog Primer\n\n> 한 줄 요약: Redisson은 Redis lock에 pub/sub wakeup과 watchdog renewal을 제공하지만, lease 만료와 stale owner write까지 자동으로 해결하는 correctness 마법은 아니다.\n\n**난이도: 🔴 Advanced**\n\n관련 문서:\n\n- [Redis SET NX Distributed Lock Beginner Primer](./redis-setnx-distributed-lock-beginner-primer.md)\n- [Publish-Subscribe Messaging 기초](./publish-subscribe-messaging-basics-primer.md)\n- [Redis TTL과 키 만료 메커니즘 입문](./redis-ttl-key-expiration-mechanics-primer.md)\n- [Distributed Lock 설계](./distributed-lock-design.md)\n- [Distributed Lock, Lease, Fencing Perspective Map](./distributed-lock-lease-fencing-perspective-map.md)\n- [Lease Fencing Token Split Brain Deep Dive](./lease-fencing-token-split-brain-deep-dive.md)\n\nretrieval-anchor-keywords: Redisson lock watchdog, Redisson pubsub lock, Redis lock pub/sub wakeup, lock watchdog renewal, Redisson leaseTime, Redisson waitTime, Redis distributed lock watchdog, spin lock polling vs pubsub, Redisson stale owner, GC pause lock expiry, fencing token DB idempotency, Redisson 분산락, 락 watchdog\n\n---\n\n## 왜 pub/sub이 나오나\n\n단순 Redis lock에서 lock 획득에 실패한 client가 짧은 주기로 `SET NX`나 Lua acquire script를 반복 호출하면 hot lock key에 polling 부하가 몰린다. 대기자가 많을수록 Redis CPU, network round trip, application thread wakeup이 함께 늘어나고, lock이 풀리는 순간 여러 client가 동시에 재시도하는 thundering herd도 생긴다.\n\nRedisson의 pub/sub 대기 경로는 이 busy spin을 줄이기 위한 wakeup 최적화다. 실패한 client는 바로 계속 때리는 대신 unlock channel을 구독하고, owner가 unlock할 때 publish된 신호나 timeout을 계기로 다시 acquire를 시도한다.\n\n```text\nA holds lock\nB fails acquire and subscribes to unlock channel\nA unlocks and publishes wakeup\nB wakes, rechecks Redis, and tries acquire again\n```\n\n중요한 점은 pub/sub message가 lock 권한 자체가 아니라는 것이다. 메시지는 다시 시도해도 된다는 힌트이고, 최종 소유권은 Redis의 owner token과 acquire script 결과가 결정한다. pub/sub은 polling storm과 평균 대기 비용을 줄여 주지만, fairness, fencing, stale owner 방지는 별도 문제다.\n\n---\n\n## waitTime, leaseTime, watchdog renewal을 분리해서 보기\n\nRedisson lock에서 자주 섞이는 시간이 세 가지 있다.\n\n| 항목 | 의미 | 설계 질문 |\n|---|---|---|\n| waitTime | lock 획득을 기다릴 최대 시간 | 요청 timeout보다 짧은가? 실패 시 caller가 무엇을 반환하는가? |\n| leaseTime | 획득 후 lock이 유지될 최대 시간 | 작업이 더 길어지면 stale owner가 될 수 있는가? 너무 길어 장애 복구를 늦추지는 않는가? |\n| watchdog renewal | 명시 leaseTime 없이 잡은 lock의 TTL을 Redisson이 주기적으로 연장 | pause, partition, client death를 관측하고 멈출 수 있는가? |\n\n일반적인 `RLock` 사용에서 명시적 leaseTime을 주지 않으면 Redisson watchdog이 내부 TTL을 주기적으로 늘린다. 반대로 `tryLock(waitTime, leaseTime, unit)`처럼 leaseTime을 직접 주면, 이 시간 뒤 자동으로 풀리는 상한을 둔 것이다. 그래서 leaseTime은 작업 소요 시간 예측과 failure recovery trade-off이고, waitTime은 사용자 latency budget이다.\n\n이 둘을 혼동하면 lock을 너무 오래 기다려 request timeout을 넘기거나, 작업은 계속 도는데 leaseTime이 먼저 끝나 다른 client가 같은 자원을 수정하는 운영 문제가 생긴다.\n\n---\n\n## watchdog renewal 실패 타임라인\n\nwatchdog은 긴 작업을 편하게 만들지만, renew가 Redis에 도달해야만 의미가 있다. JVM stop-the-world GC, host freeze, 긴 safepoint, network partition, Redis latency spike가 renewal window보다 길면 lock TTL은 만료될 수 있다.\n\n```text\n00s A acquires lock without explicit leaseTime\n10s A enters long GC pause\n30s watchdog renewal is missed and Redis key expires\n31s B wakes by timeout/pubsub path and acquires lock\n35s B writes state version 42\n45s A resumes and writes stale version 41\n46s A unlock fails or no-ops because owner token is gone\n```\n\n이 타임라인에서 문제는 A의 unlock 실패보다 A가 다시 깨어난 뒤 protected resource에 쓰는 stale owner write다. Redisson instance나 thread-local state가 과거 소유자처럼 보이더라도, Redis lock은 이미 B에게 넘어갔을 수 있다. watchdog은 TTL을 연장하는 보조 장치이지, downstream write에 순서를 강제하는 장치가 아니다.\n\n---\n\n## stale owner를 막는 마지막 방어선\n\n중요한 write path는 lock library 밖에서 한 번 더 검증되어야 한다.\n\n- fencing token: lock 획득 시 단조 증가 token을 받고, DB나 downstream service가 이전 token보다 큰 요청만 허용한다.\n- DB version/CAS: `UPDATE ... WHERE version = expected`처럼 현재 상태가 예상과 같을 때만 변경한다.\n- idempotency key: 같은 business operation이 retry되어도 한 번만 반영되게 한다.\n- unique constraint/invariant: 중복 주문, 중복 예약, 중복 정산처럼 절대 깨지면 안 되는 조건은 DB 제약으로 고정한다.\n- short critical section: lock 안에 긴 외부 API, 긴 DB transaction, blocking I/O를 넣지 않는다.\n\n즉 Redisson lock은 동시 진입 확률과 대기 비용을 낮추는 도구이고, 데이터 정합성의 마지막 책임은 fencing token, version check, idempotency, DB invariant가 가져야 한다.\n\n---\n\n## 운영 체크리스트\n\n- lock acquire waitTime이 request timeout보다 길지 않은가?\n- explicit leaseTime을 준 경우 watchdog renewal을 기대하지 않고 있는가?\n- 작업 최악 시간이 leaseTime보다 길어질 때 stale owner write가 막히는가?\n- watchdog renewal 실패 로그, Redis latency spike, GC pause 시간을 함께 보고 있는가?\n- unlock 실패, lease lost, renewal timeout이 metric으로 남는가?\n- hot lock key에 대기자가 몰릴 때 pub/sub wakeup이 polling storm을 줄이는가?\n- critical section 내부에 외부 API, 긴 DB transaction, blocking I/O가 들어가 있지 않은가?\n- downstream write가 fencing/version/idempotency/DB constraint로 보호되는가?\n\n---\n\n## 흔한 오해\n\n- Redisson을 쓰면 Redis 분산락의 모든 실패 모드가 사라진다고 본다.\n- pub/sub wakeup을 correctness 보장으로 이해한다. pub/sub은 대기 효율화다.\n- watchdog이 있으니 작업 시간이 아무리 길어도 괜찮다고 본다. renewal failure와 pause를 고려해야 한다.\n- leaseTime과 waitTime을 구분하지 않는다.\n- lock을 잡았으니 DB unique constraint나 idempotency key가 필요 없다고 생각한다.\n\n## 한 줄 정리\n\nRedisson lock은 Redis lock 사용성을 높여 주지만, pub/sub은 대기 효율화이고 watchdog은 lease renewal 보조일 뿐이므로 중요한 write는 fencing, idempotency, DB invariant로 마지막 안전선을 둬야 한다.\n",
  "aliases": [
    "Redisson 분산락",
    "Redisson pub/sub 락",
    "Redisson lock watchdog",
    "락 watchdog renewal",
    "Redis lock pub/sub wakeup",
    "spin polling vs pub/sub",
    "Redisson leaseTime waitTime",
    "Redisson tryLock waitTime",
    "lock renewal failure",
    "GC pause lock expiry",
    "Redisson stale owner",
    "fencing token DB idempotency"
  ],
  "expected_queries": [
    "Redisson 쓰면 lock 못 잡은 요청이 계속 Redis에 SET NX 날리는 건 아니야?",
    "pub/sub으로 깨우는 거랑 그냥 spin polling 하는 거 차이가 뭐야?",
    "watchdog이 TTL을 계속 늘려준다는데 GC 멈추면 락이 어떻게 꼬일 수 있어?",
    "tryLock의 waitTime이랑 leaseTime을 같이 주면 각각 어떤 시간으로 봐야 해?",
    "Redisson 락 잡고 DB에 쓰면 fencing token이나 idempotency key는 왜 또 필요해?",
    "작업이 오래 걸리는 API에서 Redisson lock을 쓰면 stale owner write를 어떻게 막아야 해?"
  ],
  "symptoms": [
    "Redisson을 쓰면 Redis 분산락 correctness가 자동으로 완성된다고 생각한다.",
    "pub/sub wakeup을 lock safety 보장으로 오해한다.",
    "명시적 leaseTime과 watchdog renewal이 같은 동작이라고 생각한다.",
    "waitTime, leaseTime, request timeout, business timeout을 한 타임라인에 놓지 않는다.",
    "GC pause나 network partition 뒤 stale owner write를 downstream fencing 없이 막을 수 있다고 생각한다."
  ],
  "relations": {
    "prerequisites": [
      "system-design/redis-setnx-distributed-lock-beginner-primer",
      "system-design/publish-subscribe-messaging-basics-primer",
      "system-design/redis-ttl-key-expiration-mechanics-primer"
    ],
    "next_docs": [
      "system-design/distributed-lock-lease-fencing-perspective-map",
      "system-design/lease-fencing-token-split-brain-deep-dive",
      "database/db-lease-fencing-coordination"
    ],
    "confusable_with": [
      "system-design/distributed-lock-design",
      "system-design/publish-subscribe-messaging-basics-primer",
      "database/for-update-vs-advisory-lock-vs-lease-fencing-decision-guide"
    ],
    "forbidden_for_queries": [
      "Redis SET NX PX lock의 owner token과 Lua unlock 기초만 설명해줘",
      "fencing token split brain deep dive를 자세히 설명해줘"
    ]
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-06-17",
    "last_modified": "2026-06-18",
    "revision_history": [
      {
        "date": "2026-06-17",
        "source": "y14-expansion-batch56-stream-a-redis-runway"
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch152"
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-audit-natural-entrypoint-repair"
      }
    ]
  }
}
