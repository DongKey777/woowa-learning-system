{
  "id": "system-design/stateful-stream-processor-state-store-checkpoint-recovery-design",
  "title": "Stateful Stream Processor State Store / Checkpoint Recovery 설계",
  "category": "system-design",
  "level": "advanced",
  "summary": "이 문서는 system-design 카테고리에서 Stateful Stream Processor State Store / Checkpoint Recovery 설계를 다루는 playbook 문서다. stateful stream processor의 state store와 checkpoint recovery는 연산 상태를 지속적으로 스냅샷하고 장애 후 복구·재스케일을 가능하게 만들어, 실시간 처리와 상태 정합성을 함께 유지하는 스트림 플랫폼 핵심 기술이다. 검색 질의가 stateful stream processor, state store, checkpoint recovery, savepoint처럼 들어오면 확장성, 일관성, 장애 격리, 운영 검증 관점으로 연결한다.",
  "body_markdown": "# Stateful Stream Processor State Store / Checkpoint Recovery 설계\n\n> 한 줄 요약: stateful stream processor의 state store와 checkpoint recovery는 연산 상태를 지속적으로 스냅샷하고 장애 후 복구·재스케일을 가능하게 만들어, 실시간 처리와 상태 정합성을 함께 유지하는 스트림 플랫폼 핵심 기술이다.\n\nretrieval-anchor-keywords: stateful stream processor, state store, checkpoint recovery, savepoint, barrier alignment, keyed state, operator state, incremental checkpoint, changelog, state restore, rescaling\n\n**난이도: 🔴 Advanced**\n\n> 관련 문서:\n> - [Streaming Analytics Pipeline 설계](./streaming-analytics-pipeline-design.md)\n> - [Historical Backfill / Replay Platform 설계](./historical-backfill-replay-platform-design.md)\n> - [Distributed Scheduler 설계](./distributed-scheduler-design.md)\n> - [Backup, Restore, Disaster Recovery Drill 설계](./backup-restore-disaster-recovery-drill-design.md)\n> - [Distributed Tracing Pipeline 설계](./distributed-tracing-pipeline-design.md)\n> - [Shard Rebalancing / Partition Relocation 설계](./shard-rebalancing-partition-relocation-design.md)\n\n## 핵심 개념\n\nstateful stream job은 메시지만 흘려보내는 것이 아니다.\n윈도우 집계, 세션화, 조인, dedup 같은 연산은 내부 상태를 계속 쌓는다.\n\n실전에서는 다음을 같이 만족해야 한다.\n\n- 장애 후 상태를 복구할 수 있어야 함\n- replay 시 중복 결과를 억제해야 함\n- rescale 시 state를 새 parallelism에 재배치해야 함\n- checkpoint 때문에 processing latency가 무너지지 않아야 함\n- source retention과 sink commit 경계가 복구 + replay catch-up 시간보다 충분히 길어야 함\n\n즉, 스트림 처리의 안정성은 compute보다 **state lifecycle 관리**에 더 가깝다.\n\n## 깊이 들어가기\n\n### 1. 어떤 state가 있는가\n\n대표적인 state:\n\n- keyed state\n- operator state\n- window buffer\n- timer state\n- dedup index\n- join side cache\n\n같은 stream job이라도 어떤 state를 갖느냐에 따라 checkpoint 비용과 restore 전략이 완전히 달라진다.\n\n### 2. Capacity Estimation\n\n예:\n\n- 입력 이벤트 초당 200만 건\n- keyed state 총 6 TB\n- checkpoint 주기 2분\n- restore 목표 15분\n\n이때 봐야 할 숫자:\n\n- checkpoint bytes\n- alignment delay\n- checkpoint duration\n- restore throughput\n- state compaction ratio\n- replay catch-up time\n\nstate store는 디스크 용량보다 checkpoint IO와 restore bandwidth가 병목이 되기 쉽다.\n\n### 3. Checkpoint와 savepoint의 차이\n\n둘은 비슷해 보여도 목적이 다르다.\n\n- **checkpoint**: 장애 복구를 위한 주기적 스냅샷\n- **savepoint**: 운영자가 업그레이드, 재배치, 마이그레이션을 위해 의도적으로 찍는 상태 스냅샷\n\n실전에서는 savepoint가 stateful cutover와 롤백 경로의 핵심이 된다.\nCheckpoint는 보통 최신 장애 복구용이라 자동 삭제/retention 정책을 탄다. Savepoint는 운영자가 보관 기간과 호환성 검증을 명시적으로 관리한다. 둘 다 새 코드의 serializer/schema 호환성, 외부 side effect 정합성까지 자동 보장하지는 않는다.\n\n### 4. Barrier alignment와 exactly-once 환상\n\n많은 엔진은 barrier를 흘려 checkpoint 경계를 맞춘다.\n하지만 exactly-once는 단순하지 않다.\n\n- source offset snapshot\n- operator state snapshot\n- sink commit coordination\n\n셋이 함께 맞아야 \"복구 후 중복 없는 것처럼 보이는\" 결과를 낼 수 있다.\n그래서 실무에서는 sink idempotency나 transactional sink가 여전히 중요하다.\nBarrier는 checkpoint 경계이고, event-time watermark와는 다른 개념이다. Window가 언제 닫히는지와 state snapshot이 언제 durable해지는지를 섞으면 late event 처리와 복구 판단이 틀어진다.\n\n### 5. Incremental checkpoint와 changelog\n\nfull snapshot만 찍으면 비용이 너무 크다.\n대응:\n\n- incremental checkpoint\n- remote checkpoint store\n- state changelog\n- local cache + remote durable copy\n\n이 구조는 checkpoint 비용을 줄여 주지만, restore 경로가 더 복잡해질 수 있다.\nIncremental checkpoint는 이전 checkpoint 파일과 manifest chain에 의존한다. Retention/compaction 정책이 꼬이면 최신 checkpoint metadata는 있어도 필요한 shared state file이 지워져 restore가 실패할 수 있다.\n\n### 6. Rescaling과 state redistribution\n\nparallelism을 바꾸면 state도 다시 나눠야 한다.\n이때 필요한 것:\n\n- key group mapping\n- operator state repartition\n- timer migration\n- restore compatibility\n\n즉, rescale은 stateless 배포와 다르게 \"바이너리를 바꾸는 일\"이 아니라 \"상태를 다시 배치하는 일\"이다.\nKeyed state는 key-group 기준으로 비교적 명확히 재분배할 수 있지만, operator state는 union/list repartition semantics를 코드가 의도대로 다뤄야 한다. Timer state와 in-flight records까지 검증하지 않으면 rescale 뒤 중복/누락이 뒤늦게 드러난다.\n\n### 7. Corruption, slow checkpoint, recovery drill\n\nstateful 플랫폼에서 흔한 문제:\n\n- checkpoint가 너무 오래 걸림\n- barrier alignment 때문에 backlog 급증\n- remote store corruption\n- restore는 되지만 catch-up이 너무 느림\n\n대응:\n\n- checkpoint timeout과 failover 정책\n- state TTL / compaction\n- 주기적 savepoint 검증\n- isolated recovery drill\n\n### 8. 증상 -> 원인 -> 트레이드오프 -> 다음 확인\n\n운영 중에는 \"checkpoint가 실패한다\"는 표현만으로 판단하면 늦다.\n먼저 증상이 어느 축에서 시작됐는지 나누고, 복구보다 더 큰 부작용을 만들지 않는지 확인한다.\n\n| 증상 | 가능한 원인 | 선택 트레이드오프 | 다음 확인 |\n|---|---|---|---|\n| checkpoint duration이 주기보다 길어진다 | state size 증가, compaction 지연, remote store 쓰기 병목 | 주기를 늘리면 IO 압박은 줄지만 복구 시 replay 범위가 커진다 | state bytes/key, upload throughput, checkpoint alignment time |\n| barrier alignment 때문에 backlog가 커진다 | 특정 partition skew, slow operator, backpressure 전파 | unaligned checkpoint는 지연을 줄일 수 있지만 snapshot 크기와 복구 복잡도가 늘 수 있다 | subtask별 input queue, busy time, skewed key distribution |\n| restore는 됐지만 catch-up이 느리다 | replay backlog 과대, sink commit 병목, local cache cold start | 병렬도를 키우면 catch-up은 빨라지지만 state redistribution 비용이 생긴다 | replay rate vs live input rate, sink throttling, restore bandwidth |\n| 최근 checkpoint가 깨져 복구 실패 | artifact corruption, manifest 불일치, partial upload | 이전 checkpoint로 가면 안전할 수 있지만 RPO가 커진다 | manifest checksum, previous checkpoint usability, source retention |\n| rescale 뒤 결과 중복/누락이 보인다 | key group mapping 오류, timer migration 누락, sink idempotency 부재 | 즉시 rollback보다 savepoint 기준 재복원이 안전할 수 있다 | key-group coverage, timer count, sink transaction status |\n| late event 처리 결과가 복구 전후 다르다 | watermark와 checkpoint 경계 혼동, state TTL 과소, handler version drift | TTL을 늘리면 정확도는 좋아질 수 있지만 state size와 checkpoint 비용이 커진다 | watermark lag, allowed lateness, expired state count, handler version |\n\noverclaim guard:\n\n- checkpoint가 성공했다고 end-to-end exactly-once가 자동으로 보장되는 것은 아니다. source offset, state snapshot, sink commit 경계가 같이 맞아야 한다.\n- savepoint는 안전한 전환점을 만들지만, 새 코드의 state serializer 호환성이나 side effect까지 대신 검증하지 않는다.\n- corrupt checkpoint fallback은 가능성을 높이는 수단이지, source retention이 끊겼거나 sink가 non-idempotent라면 데이터 보정 절차가 필요할 수 있다.\n- state TTL/compaction은 비용 최적화이면서 correctness 정책이다. 너무 짧으면 late event나 join partner가 복구 후 사라진 상태를 만나 결과가 달라진다.\n- unaligned checkpoint는 backpressure 상황에서 alignment delay를 줄일 수 있지만 in-flight buffer까지 snapshot하므로 저장 비용과 restore 검증 범위가 늘어난다.\n\n## 실전 시나리오\n\n### 시나리오 1: session window 집계 job 장애\n\n문제:\n\n- 수십 분 동안 쌓인 keyed state를 잃으면 집계가 틀어진다\n\n해결:\n\n- 최근 checkpoint에서 state를 restore한다\n- source offset부터 replay를 재개한다\n- sink는 idempotent write로 중복을 억제한다\n\n### 시나리오 2: parallelism 64에서 128로 증설\n\n문제:\n\n- 입력량이 늘어 rescale이 필요하다\n\n해결:\n\n- savepoint를 찍는다\n- key group을 새 parallelism으로 재분배한다\n- restore compatibility를 검증한 뒤 cutover한다\n\n### 시나리오 3: checkpoint store 일부 손상\n\n문제:\n\n- 최근 checkpoint artifact가 깨져 restore가 실패한다\n\n해결:\n\n- 이전 checkpoint나 savepoint로 fallback한다\n- 손상 구간 이후 이벤트는 replay로 따라잡는다\n- checkpoint manifest와 checksum 검증을 정기화한다\n\n## 코드로 보기\n\n```pseudo\nfunction checkpoint(job):\n  barrier = source.injectBarrier()\n  operators.align(barrier)\n  snapshot = stateStore.flushIncremental()\n  manifest = buildManifest(snapshot, source.currentOffsets(), sink.precommitState())\n  remoteStore.writeStateFiles(snapshot)\n  remoteStore.commitManifestAtomically(manifest)\n\nfunction recover(job, checkpoint):\n  stateStore.restore(checkpoint.stateFiles)\n  source.seek(checkpoint.offsets)\n  sink.restoreOrFence(checkpoint.sinkCommits)\n  resumeProcessing()\n```\n\n```java\npublic void restore(CheckpointMetadata metadata) {\n    stateBackend.restore(metadata.stateHandles());\n    sourceCoordinator.seek(metadata.sourceOffsets());\n}\n```\n\n## 트레이드오프\n\n| 선택지 | 장점 | 단점 | 언제 선택하는가 |\n|---|---|---|---|\n| Stateless stream job | 단순하다 | 풍부한 연산이 어렵다 | 간단한 변환 |\n| Full checkpoint | 구현이 직관적이다 | 비용이 크다 | 상태가 작을 때 |\n| Incremental checkpoint | 운영 비용이 낮다 | restore 경로가 복잡하다 | 대규모 state |\n| Transactional sink | 복구 정합성이 좋다 | sink 제약이 크다 | 금융, 정산, 권한 |\n| Savepoint-based upgrade | cutover가 안전하다 | 운영 절차가 필요하다 | stateful 업그레이드 |\n\n핵심은 stateful stream processor가 이벤트 처리 엔진이 아니라 **state snapshot, replay, rescale, restore를 함께 다루는 상태 운영 플랫폼**이라는 점이다.\n\n## 꼬리질문\n\n> Q: checkpoint만 있으면 exactly-once가 되나요?\n> 의도: source-state-sink 조합 이해 확인\n> 핵심: 아니다. source offset, operator state, sink commit semantics가 함께 맞아야 한다.\n\n> Q: savepoint는 왜 별도로 필요한가요?\n> 의도: 운영자 주도 cutover와 장애 복구 차이 이해 확인\n> 핵심: checkpoint는 자동 복구용이고, savepoint는 업그레이드·재배치·검증 같은 의도적 전환에 더 적합하다.\n\n> Q: rescale이 어려운 이유는 무엇인가요?\n> 의도: state redistribution 이해 확인\n> 핵심: parallelism을 바꾸면 상태, timer, offset 경계까지 함께 다시 배치해야 하기 때문이다.\n\n> Q: state store가 느리면 어떤 현상이 보이나요?\n> 의도: 운영 징후 파악 확인\n> 핵심: checkpoint duration 증가, alignment 지연, backlog, recovery 시간 악화로 나타난다.\n\n## 한 줄 정리\n\nStateful stream processor의 state store와 checkpoint recovery는 상태 스냅샷, replay, rescale, restore를 통해 실시간 처리와 장애 복구를 동시에 가능하게 하는 스트림 플랫폼의 핵심 운영 기술이다.\n",
  "aliases": [
    "stateful stream processor",
    "state store",
    "checkpoint recovery",
    "savepoint",
    "barrier alignment",
    "keyed state",
    "operator state",
    "incremental checkpoint",
    "state restore",
    "상태 저장 스트림 처리",
    "체크포인트 복구",
    "세이브포인트 전환"
  ],
  "expected_queries": [
    "상태 있는 스트림 처리기가 체크포인트를 잃었을 때 어디까지 다시 읽고 어디부터 버려야 해?",
    "state store 복구에서 exactly-once 보장과 처리 지연 사이를 어떻게 판단해?",
    "checkpoint가 오래됐을 때 duplicate output과 missing output을 어떻게 줄여?",
    "stream processor recovery 중 특정 partition만 격리하려면 어떤 절차가 필요해?",
    "stateful stream job이 장애 후 checkpoint에서 복구할 때 어떤 순서로 replay해야 해?",
    "checkpoint와 savepoint는 운영 전환에서 어떻게 다르게 써야 해?"
  ],
  "symptoms": [
    "Stateful Stream Processor State Store / Checkpoint Recovery 설계 관련 장애나 마이그레이션 리스크가 발생해 단계별 대응이 필요하다"
  ],
  "relations": {
    "prerequisites": [
      "system-design/streaming-analytics-pipeline-design",
      "system-design/historical-backfill-replay-platform-design"
    ],
    "next_docs": [
      "system-design/backup-restore-disaster-recovery-drill-design",
      "system-design/shard-rebalancing-partition-relocation-design"
    ],
    "confusable_with": [
      "system-design/distributed-scheduler-design"
    ],
    "forbidden_for_queries": []
  },
  "learner_query_patterns": [],
  "metadata": {
    "schema_version": "v2",
    "created_at": "2026-05-24",
    "last_modified": "2026-06-18",
    "legacy_path": "knowledge/cs/contents/system-design/stateful-stream-processor-state-store-checkpoint-recovery-design.md",
    "revision_history": [
      {
        "date": "2026-06-18",
        "source": "y14-expansion-batch148"
      },
      {
        "date": "2026-06-18",
        "source": "y14-expansion-audit-natural-entrypoint-repair"
      }
    ]
  }
}
